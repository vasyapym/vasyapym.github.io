# HANDOFF — Hero saga, passes 21-31 (closed; stub)

The landing-hero saga is **closed**: the shipped hero is the Pass 31 type-led Unbounded
composition with rAF scroll-math card reveal (`HeroTexture.tsx`, commit `ec6991d`), refined
by later landing passes (see `docs/portfolio-redesign-handoff.md` and the main graph).
Raw pre-compaction handoffs are preserved: the four tracked files below survive in git
history (deleted at the compaction checkpoint); `HANDOFF-pass21-silky-trails.md` raw is at
`docs/archive/handoffs/`.

| Doc (raw in git history) | Outcome |
| --- | --- |
| `docs/hero-zeitgeist-handoff.md` | Pass 30 curl-noise flow field rejected as "not zeitgeist"; direction settled type-led (n68) |
| `docs/hero-drafts-handoff.md` | Pass 31 shipped (`ec6991d`) then owner-rejected; drafts route opened, then closed in favor of the final hero (n71) |
| `docs/hero-scroll-handoff.md` | Scroll-math card reveal as source of truth; IO demoted to trigger; Chromium IO top-straddle root cause (n68-n69) |
| `docs/hero-atmosphere-handoff.md` | Canvas2D atmosphere replaces instruments; superseded by the glyph/texture line (n54-n56) |
| `docs/archive/handoffs/HANDOFF-pass21-silky-trails.md` | GPGPU particle trails root-cause fix; the HeroField GPGPU line that later lost the push-integration collision (n45, n56) |

Durable survivors of the saga: --hero-exit scroll-fade contract, hover systems, the
reduced-motion static-render pattern, the probe toolbox, and the scroll-math reveal fix.
