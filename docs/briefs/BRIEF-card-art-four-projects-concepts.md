# Relay prompt — card-art four projects (minimize-iteration)

Rule
 xxxx created<tool_call>write<arg_key>content</arg_key><arg_value># Relay prompt — card-art four projects × 3 (minimize-iteration, randomized routing model)

Round R001 continuation: 3 concepts per project (was 5), prompt minimized per /minimize-iteration. Outcome recorded per ROUNDS.md; delete this file once integrated.

---

comprehensive code - 12 decorative svg marks, react tsx, one export function per mark returning a single `<svg viewBox="0 0 260 160">`: three different-subject concepts per four portfolio projects, flat spot-ink language on a near-black card ground — a soft halftone-dot glow ellipse behind the subject, confident solid shapes, generous negative space, 1-3 tiny white square glints, no text/numerals, no gradients, no imports, every pattern/clip id prefixed `<Name>-` — each mark must read at one glance on the dark ground alone: RaftA..C live raft consensus mechanisms (accent coral #ff6a5f, each must read distributed-systems instantly, avoid the incumbent's diagonal shift-register stage cascade), ForestA..C quiet 8-bit woodland dusk walk scenes (teal #4fd1a5, avoid treline+canopy-band+moon incumbent), BlastA..C ordered explosion frames (ember #ffb347 family, avoid shard-fan-around-dark-disc incumbent), PlanckA..C cosmic-history scrub moments (violet #a98cff, avoid nested epoch ripple bands incumbent) — reply = `### <Name> — <concept name>` + one `why:` line + fenced tsx block, complete code no ellipses, nothing else.
