# BRIEF — Compaction round 7: STATE.md re-compaction, second checkpoint pass (policy locked)

Input = the full pre-round STATE.md (8.4KB) — NOT duplicated here; retrievable at git `4d90bff`. Policy and outcome only.

## Policy (as delivered to the chat model)

- Keep the exact section skeleton (`# STATE —`, `## Now`, `## Open threads`, `## Protocols (unchanged by compaction)`, `## Where things live`, `## Checkpoint record`).
- Protocols survive essentially verbatim (constitution). Where-things-live may tighten phrasing, never change paths.
- Open threads: same five numbered threads; every owner-pending item and device-checklist item preserved; verdict trails compact; per-pass narration, per-pass parameter values, and probe-count growth stories dropped (keep current gate count).
- Checkpoint record rewritten for the second checkpoint; first-checkpoint node `compaction-cfcb7196` retained.
- Never invent facts (r9 in flight at brief time — no r9 status written).

## Outcome

- Chat model returned the compacted STATE (~46 lines, from 40); integrated with three factual fixes at integration: explosion line now "three modes shipped (…+ Cinder Fault, n28–n31)"; kitty-run Now line now records the shipped ashen qualitative pass R1–R6 (n84–n87); checkpoint record carries the concrete sha `4d90bff` for pre-second-checkpoint state.
