comprehensive code - react 19 with vite and typescript page, dark swiss archive editorial: IBM Plex only, sans 600 and 700 for display and prose, mono lowercase chrome, ochre as the only accent over near-black ink, hairline borders, 999px pill controls, 20px panel radius, solid ink cards with a quiet hover lift. banned: uppercase chrome, weight 300 text, any non-ochre accent, boxless rule-list cards, invented palettes. structure: five tiers of lesson cards on pg- prefixed classes; the big tier holds five folders that enter a volume view with a crumb row; a page-level graph overlay maps global concept co-occurrence and its canvas shows only top-ranked nodes. edits: 1. hero copy becomes two lowercase lines, "archive of ai outputs" then "teaching stuff.", second line ochre, retune the h1 clamp divisor for the longer copy. 2. strip the band badges, max high medium low thinking, everywhere including the panel head; the suffix already lives in the names. 3. card summaries clamp to two lines, drop their min-height, and card padding rises from 24 to about 30 on desktop and 16 to about 20 on mobile. 4. the crumb hierarchy flips: a dominant "go back" pill keeps the class pg-crumb-back, and the volume title renders smaller than it. 5. delete the hero rail and its russian hint; a graph control joins each card foot beside the "open lesson" pill, which stays the only pg-pill in the card while the graph control takes its own class; it opens the overlay scoped to that lesson, and because lesson concepts are single-lesson vocabulary that ranks off-canvas, the focused node set is the lesson's concepts plus their strongest neighbors, with the readout naming the source lesson; escape and the close button still work. 6. mobile spacing at the 900, 700 and 560 breakpoints tightens: margins, gaps and paddings down, tap targets of 44px and up stay. 7. tier rows render smaller and quieter; names are 1.15rem at 600 with roomy padding today. 8. the pill radius token is referenced but undefined, so define it as 999px on the practice-map-field root. reply: one line per edit with the decision and exact values, plus code fragments only for the card foot, the crumb row and the focus hook.
