# chat-model prompt 31 — profile readme, three takes (spread draw)

Follow-up to the freedom round (prompt 30); no owner verdict returned for
30 (no data — no S12 row). Varied signal this round: draw spread — three
independent takes per language so the owner picks by taste (the S10
two-sets precedent, sized down for prose). Raw material unchanged from
prompt 30; only the contract and the take-diversity line change.

## Paste this

```text
comprehensive code - my github profile readme, written in english and in russian; freedom round with a spread: three independent takes, every choice yours - structure, rhythm, what earns a sentence and what gets cut, the voice. the takes must commit to different bets, not polish one draft three times - e.g. one leans on quiet facts, one on small concrete stories, one almost bare - name each. treat the notes below as raw material, not a checklist; write like a writer with taste, not like a form filled correctly. the text must read as written with intention - a developer doing what he loves; no pitch, no hype, no corporate landing energy; short because everything in it earns its place, not because a limit forced compression. raw material, none of it mandatory, order free: one react site, vasyapym.github.io, eight self-contained projects each with its own tests; quicknotes - markdown notes where quickness is the whole point: instant google-login sync across devices, copy .md button, .md and txt views; spine - a peek behind frontend: a go core compiled to wasm driving a flexbox/grid layout editor with clean html+css export; waste of tokens - llms are brilliant only sometimes; the real pull is watching how a model thinks and reproduces language (author comes from linguistics, worth one quiet mention), its picture of the world assembled from the texts it learns from; kitty-run - a chill endless runner born from a friend's love of hello kitty and the author's fondness for dark souls' tone; simple mechanics carrying complex ones; raft-cluster - rust compiled to wasm; crash the leader, watch consensus elect a new term - system design made visible; evening-forest - an 8-bit first-person dusk walk that exists because the author loves 8-bit; explosion - physics and math: a paper-lantern moon detonating into 600 gpu-driven shards; planck-to-now - scrubbing cosmic history on a log time scale, a reminder that we are all cosmic dust. site details you may use freely: quiet ink catalogue design, wcag aa, reduced-motion fallbacks; an opt-in realm "the deep" - projects as bioluminescent creatures steered by a warm lantern through an abyss; work orchestrated agentically - glm 5.3-flash distributes scoped briefs to claude/fable/gpt models and reconciles their outputs. links: project pages https://vasyapym.github.io/projects/<id>/, ids quicknotes, spine, practice-map (display name waste of tokens), kitty-run, raft-cluster, evening-forest, explosion, planck-to-now; email vasyapym@gmail.com. each take's opening 2-3 sentences must stand alone (reused later as an about-blurb). challenge assumptions and failure cases before answering; rationale = the why lines. reply: exactly 6 blocks - "## en n - name" + one fenced markdown doc (h1 Vasily Argounov) then "## ru n" + one fenced markdown doc (same take in native russian prose, not a translated english), for n = 1,2,3 - nothing else.
```
