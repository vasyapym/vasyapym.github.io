# BRIEF — Compaction round 2: main-page graph node summaries, part 1 of 2 (policy locked)

You cannot see the repository. This brief is self-contained. You are rewriting verbose event-log summaries into compact, high-value ones as part of a "Compaction Checkpoint" milestone. Do NOT write code. Output format is specified at the end.

## Context

The repo drives a multi-agent workflow. The main page keeps an append-only JSONL event log (the "portfolio graph"): nodes (kind: snapshot/iteration/decision/milestone; fields: id, actor, title, summary, status, artifacts, meta) and typed edges. Agents orient by folding this log; its prose cost is the problem. The milestone consolidates prior state into compact form; the workflow continues identically. Compaction rules: summarize similar/redundant entries; compact (never remove) important ones — preserve decisions, rationale, open threads, dependencies; maximize value-per-token.

This round covers the **main-page portfolio graph** (the landing shell under portfolio/shell): design-iteration "Passes" that rebuilt the landing (ink catalogue, card artwork, hero field/atmosphere), the card-artwork saga (gemstones → quiet specimen → dither rejected → tiger-bright → Ember Spine → Raft rounds 1-7, ending with the vault "seven-key majority" adopted as the Raft mark, then the cybernetic round with candidate H adopted into production), and hero passes 21-31 (isoline field → marching squares → fog atmosphere → particle trails → silk → type-led). Node ids are permanent (commit messages reference them); you never see or change ids. Part 2 of this graph arrives in a separate round; cross-node context should land in the earliest carrier WITHIN this part.

## Checklist — what a kept summary must contain

KEEP (the durable spine):
- Final verdict/decision state — adopted / rejected / pending — including pending→adopted transitions (both states if the trail matters).
- Quality-gate result (tsc/build/probe pass|fail) — usually already in meta; don't duplicate.
- Lesson or hard constraint born in this node (e.g. a ban list, a law, a root-cause fix).
- Open thread / explicit next-step, only if unresolved as of the checkpoint.
- Durable pointers (brief filename, doc-anchor, commit sha) are already in `artifacts` — do not restate them in prose.

DROP (reconstructable or duplicated):
- Step-by-step narration of implementation passes (what file changed in what order).
- Owner mood/color commentary — unless the quote itself is the decision (then keep it in one clause).
- Rationale duplicated in a linked doc or in meta.
- Enumerations restatable from the referenced brief.
- "Nothing committed / keep-delivery-separate" boilerplate — collapse to a `verdict: pending` fact.

## Input

JSONL, one object per node: `id, kind, actor, title, ts, status, meta, artifacts, summary`. The `summary` field is your target; `title` may also be tightened. `status`/`meta`/`artifacts` are context only — you output neither.

## Output contract

- One JSONL line per input node, same order: `{"id":"n20","newSummary":"..."}` (optionally add `"newTitle":"..."`).
- `newSummary`: ≤2 sentences, ≤450 characters. If `meta.verdict` / `meta.gate` already carry the outcome, one short sentence is ideal.
- Empty string `""` = drop the summary entirely (title + meta suffice) — prefer this when nothing else is lost.
- `newTitle`: only if the original is bloated or narrates steps; otherwise omit the key.
- Never invent facts; never lose a verdict, a lesson, or an open thread. Cross-node redundancy should land in the EARLIEST node that carries it; later nodes shrink to their delta.
- If you approach output limits, end with the line `TRUNCATED — reply CONTINUE` and finish the remaining ids in the same order after I reply.

## Input nodes

{"id":"n20","kind":"iteration","actor":"design-iteration","title":"Pass 15 - Beneath-the-surface list: clean fonts, rebalanced layout, clickable index, curated order","ts":"2026-08-24","status":"active","meta":{"verified:build":"tsc+vite pass","verified:headless-chrome":"16/16 checks (order, anchors, desktop click scroll, mobile tap scroll, font load, filter removal)"},"artifacts":[],"summary":"Removed SVG sea-warp filter from project-row text (Windows rendering was jagged; kept on decorative hairline). Moved Google Fonts @import to preconnect+stylesheet links in shell/index.html. Panel widened 360->450px cap, centered in a wider right hero column (minmax(280px,0.58fr)), type scaled up (titles 0.98->1.08rem). Rows became real anchors (#project-<id>) scrolling to per-project card anchors with scroll-margin; aria-hidden dropped from the panel. Curated order: kitty-run first, evening-forest second, rest alphabetical."}
{"id":"n21","kind":"iteration","actor":"design-iteration","title":"Pass 15b - Hero index scroll slowed ~1.3x native with JS animation","ts":"2026-08-24","status":"active","artifacts":[],"summary":"Replaced CSS scroll-behavior anchor jumps with rAF animation (easeInOutCubic) whose duration = 1.3 x Chrome's native estimate (220ms + 0.15ms/px, clamped 360-1400). Targets via offsetTop chain so card reveal transforms don't deflect aim; per-frame retarget absorbs late layout shifts; 2.4s settle chase; wheel/touchstart cancels; prefers-reduced-motion falls back to instant scrollIntoView. Verified headless: short hop ~516ms vs ~290ms native, long ~539ms, mobile tap lands at card, wheel cancels clean."}
{"id":"n23","kind":"iteration","actor":"design-iteration","title":"Pass 16 - Ink interior / Planck to Now converted","ts":"2026-08-24","status":"active","artifacts":[],"summary":"Converted /projects/planck-to-now and the standalone /planck-to-now/ HUD to the ink catalogue system: planck-field wrapper (--ink tokens, dark color-scheme, ochre focus ring), facts as mono notation rows, mono meta row instead of the simulation heading, footer removed; HUD got IBM Plex Mono, ink palette, lowercase epoch, beneath-panel scrim for flash contrast, ochre timeline ramp; copy diet H1 '13.8 billion years. / One scrub.', blurb 'Scrub cosmic history вЂ” from the Planck epoch to now.'; epoch descriptions kept (user decision). Verified: typecheck, builds, planck verify, smoke 200 x3, headless 1440/1024/390 no overflow, ochre focus ring."}
{"id":"n25","kind":"iteration","actor":"design-iteration","title":"Pass 18 - Ink interior / Practice Map converted","ts":"2026-08-24","status":"active","meta":{"gate":"interior reads as one ink catalogue system with the frame chrome; copy diet applied to all chrome","verified":"typecheck+build pass; smoke 200 x2; headless 1440/1024/390 no overflow; field #0b1317 dark; mono labels; ochre focus ring; overlay open/tabs/Esc pass"},"artifacts":[],"summary":"Converted /projects/practice-map to the ink catalogue system via a practice-map-field wrapper (--ink tokens, dark color-scheme, bright-ochre focus ring, single-element full-bleed field with centered padding). Chrome copy diet to EN lowercase mono: hero 'Twenty topics, / one route.' (ochre second line), eyebrow+intro removed, export 'copy review notes / copied вњ“', metrics cards/in progress/applied/revisit, nav 'areas', area kicker '01 В· linux', statuses queued/in progress/revisit/applied, 'open lesson в†’', details 'practice path/try/check', 'status', 'leave feedback', 'note' + short placeholder, lesson tabs problem/model/mechanics/pitfalls/when not, 'lesson NN В· complexity N/5', 'objectives', 'examples', 'references:', footer 'local В· no account'; FEEDBACK_LABELS lowercased. Landing blurb dieted to 'Concepts, exercises, and notes on one quiet map.' Curriculum lessons (RU) untouched as instrument content; progress.ts logic, localStorage, card artwork, frame unchanged. Verified: typecheck, build, dev-server smoke 200 x2, headless 1440/1024/390 no overflow, field bg #0b1317 + color-scheme dark, mono labels, ochre focus ring on Tab, lesson overlay open/tabs/Esc-close pass, landing blurb live, zero console errors."}
{"id":"n26","kind":"iteration","actor":"design-iteration","title":"Pass 19 - Mobile hero / depth layers","ts":"2026-08-24","status":"active","meta":{"gate":"mobile hero reads as one ink catalogue; motion transform/opacity only, frozen under reduced motion; rows keep 44px targets, AA contrast, focus; desktop unchanged","artifact":"/mobile-hero-directions draft board (19 unchosen treatments kept as alternates)"},"artifacts":[],"summary":"User picked draft 11 (Depth layers) from the 20-variant live board /mobile-hero-directions. At <=560px the beneath panel dissolves into plain catalogue rows (border/fill/blur/label/warp rule removed) over three parallax glow layers (teal/ochre/bright-ochre, blur 26px) driven by a rAF scroll-progress custom property --hero-p, gated by prefers-reduced-motion and the 560px query; one-time staggered entrance (kicker/H1/CTA/rows, layers fade) with explicit to keyframes. >=561px keeps the Pass 12 static hero and boxed panel. Verified: typecheck, build, git diff --check; headless / at 390/768/1440 zero console errors and overflow; 390px panel border 0, label none, far layer 0 -> -19.2px after 700px scroll; reduced-motion leaves layers at identity with --hero-p unset; 768/1440 panel unchanged."}
{"id":"n30","kind":"iteration","actor":"code-iteration","title":"Cat Runner rebrand + touch dash pad + rose accent pass","ts":"2026-08-26","meta":{"checks":"tsc+build+check+sim+shots","quality-gate":"pass: chip gone, dash pad live w/ cooldown ring, no violet left, no attribution, title Cat Runner"},"artifacts":["file=portfolio/projects/kitty-run/web/KittyRunPage.tsx","file=portfolio/projects/kitty-run/web/scene/GameLoop.tsx","file=portfolio/projects/kitty-run/web/kitty-run.css","file=portfolio/projects/kitty-run/project.ts"],"summary":"Removed the echo HUD chip (ghost race itself stays, reads on stage), added a thumb-zone DASH pad with a game-loop-driven conic cooldown ring, recolored the violet family to bow-red (pilot chip, card pills), removed the Sanrio attribution footer, renamed the project to Cat Runner (page h1, catalog title/description, READMEs; route id kitty-run kept for URL stability). Verified: tsc clean, vite build, kitty-run.check.ts 46 ok, kitty-run.sim.ts all ok, headless shots at 1440/390 (menu, running, autopilot) with zero console errors."}
{"id":"n31","kind":"iteration","actor":"code-iteration","title":"Bullet-time dash: deterministic time dilation + camera/vignette/streak FX","ts":"2026-08-26","meta":{"checks":"tsc+build+check+sim+shots(bullettime)","quality-gate":"pass: vignette blooms on dash, lockstep preserved, checks pin dip depth/recovery"},"artifacts":["file=portfolio/projects/kitty-run/web/scene/step.ts","file=portfolio/projects/kitty-run/web/scene/GameLoop.tsx","file=portfolio/projects/kitty-run/web/scene/RunCanvas.tsx","file=portfolio/projects/kitty-run/web/scene/bursts.ts","file=portfolio/projects/kitty-run/web/lib/tuning.ts","file=portfolio/projects/kitty-run/web/lib/replay.ts","file=portfolio/projects/kitty-run/tests/kitty-run.shots.mjs"],"summary":"Every dash now dips the whole simulation clock to 0.35x and eases back (TUNING.bulletTimeScale/bulletRecovery). GameLoop scales one delta for player sim, echo sim, particles and dust so both sims stay in exact lockstep; step.ts dips on dash trigger (next-step onset) and recovers exponentially in sim time. FX: camera FOV punch proportional to the dip, a rose vignette overlay driven per-frame from world.timeScale, and speed-line particles tearing backward while dipped. Reduced-motion skips FOP/vignette/streaks but keeps the slow-mo. Replay storage bumped to v2 (timestamps recorded under the old constant clock mistime on replay). Shots probe now fires a dash and asserts the vignette blooms. Verified: tsc clean, build, checks 49 ok, sim all ok, headless bullet-time screenshot shows vignette + drained cooldown ring."}
{"id":"n32","kind":"iteration","actor":"code-iteration","title":"Adaptive generative soundtrack (Web Audio lookahead scheduler)","ts":"2026-08-26","meta":{"checks":"tsc+build+check+sim+shots(sound)","quality-gate":"pass: soundtrack runs error-free through start/pause/resume, sfx and music share one mute"},"artifacts":["file=portfolio/projects/kitty-run/web/lib/music.ts","file=portfolio/projects/kitty-run/web/lib/audio.ts","file=portfolio/projects/kitty-run/web/scene/GameLoop.tsx","file=portfolio/projects/kitty-run/tests/kitty-run.shots.mjs"],"summary":"New web/lib/music.ts: a procedural chiptune bed sequenced by a 140ms lookahead scheduler over a C-Am-F-G loop (pentatonic-safe against the pickup chimes). The run conducts: tempo 96-132 bpm follows speed, arp/hats gate on an intensity mix of speed+combo, kick on downbeats, combo>=8 adds a sparkle line, hits duck the bed 0.45s, pause/over/mute fade to silence with the sequencer parked to avoid stale-note dumps on resume. Shares the Sfx AudioContext and master bus (new context/output getters); created lazily inside the first gesture; GameLoop conducts it once per frame and ducks on hit events. Shots probe gained a sound pass (click start, pause/resume, assert error-free + progress). Verified: tsc clean, build, checks, sim, full shots run green."}
{"id":"n33","kind":"iteration","actor":"code-iteration","title":"Cat Runner docs + catalog pitch pass (session wrap)","ts":"2026-08-26","meta":{"checks":"tsc+build+check+sim+shots+index-probe","quality-gate":"pass: portfolio-ready visuals at 390/1440, zero console errors, all suites green"},"artifacts":["file=portfolio/projects/kitty-run/README.md","file=portfolio/projects/kitty-run/project.ts","file=portfolio/README.md"],"summary":"Landing-card description now sells the three engineering hooks (bullet-time dash, best-run ghost, adaptive soundtrack) and lists deterministic simulation as a technology. Project README rewritten around the same three features; mechanics and implementation notes cover bullet time, the dash pad, the music engine and the updated reduced-motion policy. Verified end-to-end after all edits: tsc clean, vite build, checks 49 ok, sim all ok, full headless shots suite (desktop/mobile menu+running, bullet-time, autopilot, sound) green, landing index shows Cat Runner with intact kitty card art."}
{"id":"n34","kind":"iteration","actor":"code-iteration","title":"Forgiving dash: wider window, shorter cooldown, 0.28s input buffer","ts":"2026-08-26","meta":{"checks":"tsc+build+check+sim+shots","quality-gate":"pass: early presses land, stale presses expire, determinism holds"},"artifacts":["file=portfolio/projects/kitty-run/web/lib/tuning.ts","file=portfolio/projects/kitty-run/web/scene/step.ts","file=portfolio/projects/kitty-run/tests/kitty-run.sim.ts"],"summary":"Dash timing complaints addressed three ways: invulnerable window 0.22s -> 0.32s (covers a full hazard crossing at top speed), cooldown 1.4s -> 1.2s, and a new input buffer вЂ” a press during the cooldown is remembered 0.28s and fires the instant the dash returns (near-miss presses become slightly-early dashes instead of dead inputs). Buffer is sim-state (kitty.dashBufferT) consumed in step.ts via an extracted fireDash helper, so replays and the echo stay deterministic. Sim gained two checks: a press just before cooldown end fires on the dot, and a stale buffer expires without firing. README mechanics + touch bullets updated. Verified: tsc clean, build, checks 49 ok, sim all ok (incl. new buffer checks), full shots suite green."}
{"id":"n38","kind":"iteration","actor":"code-iteration","title":"Pass 22 - no worms: speed-gated silk, parallel hero entrance, compact mobile hero","ts":"2026-08-26","meta":{"verified":"dpr1,390x844@3x,768x1024@2x,reduced-motion","quality-gate":"typecheck+pass22-probe:pass","pending":"real-device check on iPhone/Windows by user"},"artifacts":["git-commit=uncommitted-working-tree","file=portfolio/shell/src/shell/HeroField.tsx","file=portfolio/shell/src/shell/LandingPage.tsx","file=portfolio/shell/src/styles.css"],"summary":"Trail deposits are now gated by particle speed (smoothstep 0.08-0.4), so slow particles draw nothing - idle 'worms' on Windows/iPhone are gone physically, not cosmetically. Base flow x1.25 keeps the field alive; an autonomous Lissajou pointer drift (touch devices + idle >4s, energy 0.8) makes the silk dance without any input. Particle grid sized by hero area (20/26/32). Hero text no longer waits for WebGL: heroLive fires at 150ms and the canvas fades in on its own clock, overlapping the project-list reveal; card reveal delays are per-batch (0/90/180ms). Mobile hero compacted (302px, h1 21.4px, field-note hidden) while keeping vertical air. Verified: typecheck clean; pass22 probe green (dpr1 idle/sweep silk, 390@3x drift motion 3/3, h1 starts ~89ms after live, canvas fade overlaps rows, tablet 676 particles, reduced-motion safe, zero console errors)."}
{"id":"n45","kind":"iteration","actor":"code-iteration","title":"Pass 22 - platform-sharp hero, unbounded light, card hover, five custom artworks","ts":"2026-08-27","status":"active","artifacts":[],"summary":"Blur on Windows/mobile root-caused to DPR_CAP 1.75 x RENDER_SCALE 0.85 under-native sampling; now DPR_CAP 2.0 + frame-time governor (ladder 1/0.85/0.72/0.6, mobile ceiling 0.85) + high-performance context, probe-verified densities native at dpr 1/1.25/2, 1.70 at dpr3. Hero edge fixed via shared edgeFade() in wash/direct/composite shaders + CSS mask on depth layers, no cut line bottom/right. Card hover polish: whole-card artwork activation, accent hairline, title/link/description response. Five new flat-SVG center artworks (filetree/fox/blast/spiral/trail), ProjectCenter union extended. Gates: tsc clean, build ok, desktop+mobile screenshot matrices."}
{"id":"n49","kind":"iteration","actor":"design-iteration","title":"Hero field review - Windows mud verified, WebGL fog field rejected as approach","ts":"2026-08-27","meta":{"gate":"review pass: issue reproduced and measured, no code changed","evidence":"temp hero-shots: win-dpr1/hi-dpr2/iphone-dpr3 hero+crop pngs"},"artifacts":[],"summary":"User reports blur: iPhone = smoke (acceptable), Windows = mud. Verified on Windows (Edge/ANGLE D3D11, Intel UHD): canvas backing 922x320 for a 1280x444 CSS hero at DPR 1 (RENDER_SCALE 0.72 x DPR cap 1.75) - the 5-octave domain-warped value-noise FBM renders 28% under native res and is bilinear-upscaled; DPR 2 renders 1.44x and looks finer; DPR 3 phone (451x575 for 358x457) reads as soft smoke. Three compounding causes: (1) sub-native rasterization, (2) the FBM fog itself has no high-frequency structure so nothing survives upscaling - low-alpha dark palette reads as dirt, (3) stacked CSS blur(26px) depth blobs + backdrop-filter. Commit 46e90d7 (aurora hero) was never recorded in this graph (hooks not armed on this clone). Verdict: do not patch - the fog-field idea cannot be made crisp by adding pixels. Candidates offered: A) crisp vector isoline field (SVG hairlines, native raster at any DPR, catalogue-native), B) baked hi-res video loop (sharpness fixed at encode, deterministic cross-platform, zero runtime GPU), C) native-res shader rewrite (rejected by default - still fog, still platform-variance, iGPU perf risk is why 0.72 exists). Awaiting user pick."}
{"id":"n50","kind":"decision","actor":"design-iteration","title":"Vector isoline hero field","ts":"2026-08-27","meta":{"pick":"A vector isolines"},"artifacts":[],"summary":"User picked candidate A from the mud/smoke review (n49): replace the WebGL FBM fog field + blur(26px) depth blobs with crisp SVG contour hairlines. Carried forward: depth-layer parallax spirit from Pass 19 (far/mid/near bands) but expressed as transform-only band offsets without any blur; the static two-glow ink gradient stays the base. Sharpness by construction: SVG rasterizes at native resolution at any DPR, non-scaling 1px strokes, no filters, no WebGL. Motion = one-time stroke draw-in + slow band drift + pointer parallax via existing --hero-mx/--hero-my; all transform/opacity, frozen under prefers-reduced-motion. Reverses the aurora treatment (commit 46e90d7, never graphed; aurora was also a rejected Pass 19 mobile alternate) and retires the depth blur blobs. Rejected here: baked video loop (binary asset, no live response) and native-res shader rewrite (still fog, iGPU cost, platform variance)."}
{"id":"n51","kind":"iteration","actor":"design-iteration","title":"Pass 22 - Hero field / vector isolines","ts":"2026-08-27","meta":{"gate":"passed: hairlines subordinate to copy, motion transform/opacity/dash only, reduced motion = static drawn frame, no WebGL failure surface","verified":"headless Edge D3D11 at DPR1 1440, DPR2 1440, DPR3 390: 14 paths, non-scaling-stroke, canvas+depth gone, zero console errors, zero h-overflow, drift live, reduce static","evidence":"iso-shots dir, session temp"},"artifacts":[],"summary":"Implemented the picked direction (n50): deleted the WebGL fog field (HeroField.tsx) and the blur(26px) depth blobs; new HeroIsolines.tsx renders 14 seeded SVG contour hairlines in three parallax depth bands with a one-time pathLength dash draw-in, slow band drift, and pointer parallax via --hero-mx/--hero-my; LandingPage drops lazy/fieldReady/--hero-p scroll listener; styles.css replaces the field/depth blocks with .signal-index-hero-lines under a no-preference motion media query. Sharpness is structural: native-res SVG rasterization + non-scaling-stroke at every DPR."}
{"id":"n52","kind":"iteration","actor":"design-iteration","title":"Pass 23 - Hero field / live topographic marching-squares canvas","ts":"2026-08-27","status":"active","meta":{"gate":"passed: hairlines subordinate (0.11/0.20/0.34), reduced-motion = single static render + HUD hidden, touch probe releases on lift, no WebGL/dep additions, loop pauses offscreen","verified":"typecheck+build+git diff --check green; headless Edge DPR1/2/3: backing css*dpr exact, 60fps, idle drift + ripple frame-diff, HUD live, zero console errors, zero h-overflow, reduce static","evidence":"iso-shots/field-* in session temp"},"artifacts":["doc-anchor=docs/portfolio-redesign-handoff.md#pass-23--hero-field--live","file=portfolio/shell/src/shell/HeroIsolines.tsx","file=portfolio/shell/src/shell/LandingPage.tsx","file=portfolio/shell/src/styles.css"],"summary":"Rewrote HeroIsolines.tsx from seeded SVG harmonics into a Canvas2D field engine: value-noise fBm heightfield (16px grid, drift + 26s tide), marching-squares iso-contours each frame at native DPR (cap 3), pointer probe (Gaussian sigma 120, amp 1.15, smoothed) that bends contours, tap/click charge rings (240px/s, 2.1s TTL), peak survey diamonds with elevation text, mono HUD readout, staggered intro fade, rAF pauses offscreen/hidden, adaptive octave downgrade. LandingPage drops --hero-mx/--hero-my parallax; styles.css swaps lines/band blocks for canvas+HUD. Pass 22 crispness guarantee kept (1 physical px hairlines, no WebGL, no new deps)."}
{"id":"n53","kind":"iteration","actor":"design-iteration","title":"Pass 24 - Hero field edge fade, no more scissor cuts","ts":"2026-08-27","status":"active","meta":{"gate":"passed: hairline alphas unchanged (0.11/0.20/0.34 ceiling), no JS/TS changes, no new deps, no h-overflow","verified":"typecheck+build green; headless Edge DPR1/1440x900 + DPR3/390x844: 60fps, idle drift, HUD live, zero console errors, zero h-overflow; reduced-motion static clean","evidence":"iso-shots/fade-dpr1-*, fade-dpr3-* in session temp"},"artifacts":["doc-anchor=docs/portfolio-redesign-handoff.md#pass-24--hero-field-edge-","file=portfolio/shell/src/styles.css"],"summary":"Owner feedback: the field's hard left/right edges (canvas spans the shell, isolines stopped on two vertical lines) should not exist. Fix is pure CSS on .signal-index-hero-canvas: horizontal mask-image fade (transparent -> #000 120px -> #000 calc(100% - 120px) -> transparent), 56px per side <=700px, so contours dissolve into the dark before the canvas boundary. Zero JS changes; applies to live loop and reduced-motion static render alike; considered and rejected: full-bleed 100vw canvas (cost + probe math) and heightfield edge envelope (touches field math for no visible gain)."}
{"id":"n54","kind":"decision","actor":"design-iteration","title":"Hero is an atmosphere, not an instrument panel","ts":"2026-08-27","status":"superseded","meta":{"carries-forward":"pointer responsiveness, calm-behind-copy ceiling, reduced-motion static render, offscreen pause, edge fade (Pass 24)"},"artifacts":[],"summary":"Owner verdict after Pass 23/24: the topographic-instrument identity feels clunky and generic ('too many instruments'), and the owner misses the old WebGL hero's mud-like animation (which only ever rendered right on macOS). Direction: hero backdrop = slow organic atmosphere (mud/ink in water) that still answers the pointer, with zero readouts/markers/rings. WebGL stays rejected for platform variance - the feel is reproduced deterministically with Canvas2D. Supersedes the instrument direction of n52 (its probe/charge/HUD work is retired; its pause/resume, reduced-motion, frame-budget and interaction patterns carry into the successor)."}
{"id":"n55","kind":"iteration","actor":"design-iteration","title":"Pass 25 - Hero atmosphere, the mud returns without WebGL","ts":"2026-08-27","status":"active","meta":{"gate":"passed: fog alpha ceiling 0.45 over #0b1317, copy+catalogue contrast kept in shots, no WebGL, no new deps, reduced-motion static, zero console errors, zero h-overflow","verified":"typecheck+build green; headless Edge DPR1/1440x900 + DPR3/390x844: 60fps, fog frame-diff, idle drift, 0 errors; backing low-res by design (320x111, 90x115)","evidence":"iso-shots/fog2-dpr1-*, fog2-dpr3-*, fog2-dpr1r in session temp","decision":"n40"},"artifacts":["doc-anchor=docs/portfolio-redesign-handoff.md#pass-25--hero-atmosphere-","file=portfolio/shell/src/shell/HeroAtmosphere.tsx","file=portfolio/shell/src/shell/LandingPage.tsx","file=portfolio/shell/src/styles.css"],"summary":"Implemented the n54 direction: new HeroAtmosphere.tsx replaces deleted (untracked) HeroIsolines.tsx - Canvas2D domain-warped fog (3-octave fBm through two 2-octave warp fields, +/-95px marble, 26s tide, slow drift) rendered at 1px per 4 css px, browser-upscaled + CSS blur(12px)/9px, teal->amber palette, alpha ceiling 0.45, 30fps cadence, pointer = gentle heat swirl (sigma 150, amp 0.5), offscreen/hidden pause + octave downgrade + reduced-motion static carried from Pass 23. Instruments removed: HUD, peak diamonds, charge rings. Pass 24 side fade kept. LandingPage import updated; styles.css swaps HUD rules for filter+fade. Verified: typecheck+build green, headless Edge DPR1/DPR3 60fps, idle drift, 0 errors, 0 overflow, reduced static clean. Evidence iso-shots/fog2-*."}
{"id":"n56","kind":"decision","actor":"design-iteration","title":"Owner reconciles the two hero lines: local Canvas2D atmosphere wins","ts":"2026-08-27","status":"superseded","artifacts":[],"summary":"During push integration the two parallel hero lines collided: remote main carried the GPGPU HeroField evolution (code-iteration n45, perf n47 - silk trails, native density, quality ladder), this machine carried the Canvas2D atmosphere (n49-n55). Owner verdict: 'in github is my old version, in this computer - newer version' - the atmosphere is the current hero; the remote HeroField files are superseded in the working tree (their commits remain in history). Recorded per the append-only rule instead of silently overwriting the other line."}
{"id":"n58","kind":"iteration","actor":"code-iteration","title":"feat(shell): hero consolidated, full-viewport, scroll exit fade + reveal pass","ts":"2026-08-28","status":"active","artifacts":[],"summary":"Gulled delegation pass executed via chat-model brief + agent integration. Hero: three stacked CSS generations folded into one consolidated block (styles.css), legacy class signal-index-hero-refraction renamed to signal-index-hero-atmosphere (owner verdict n55 lineage), static ::before glow band from the superseded Refraction-sea direction deleted, identity header moved inside the hero section and overlaid absolute (no bottom border), hero min-height 100vh/100svh. Scroll: whole hero (canvas, copy, header, beneath panel) fades via --hero-exit progress var (rAF-throttled passive scroll listener, disabled under prefers-reduced-motion); projects section reveal via dedicated IntersectionObserver (threshold 0 - tall section cannot reach ratio 0.12) draws the grid top hairline in once; card reveal transition retuned to the hero entrance family (680ms cubic-bezier(0.22,1,0.36,1)) plus inner topline->copy->artwork stagger gated under signal-index-reveal-ready. No new dependencies; HeroAtmosphere constants untouched (owner verdict 'reads as blur' deferred to a future tuning pass, handoff open thread 1a). Verified: typecheck green, vite build green, headless Edge probes (dpr1 1440x900, dpr3 390x844, reduced) - hero 100svh, exit var 0->1 with opacity 1->0 and restore on scroll-top, hairline scaleX 0->1, section is-revealed, cards staggered, zero console errors, zero horizontal overflow, atmosphere loop intact (60fps, idle drift, static under reduce)."}
{"id":"n60","kind":"iteration","actor":"code-iteration","title":"feat(shell): hero glyph field - fog retired for a live mono glyph rasterizer + grain/card-bloom polish","ts":"2026-08-28","status":"active","artifacts":[],"summary":"Delegated chat-model brief, agent-integrated (Pass 27). Owner ask: make the main page showcase an advanced technology feature (helloclaude.ru effects approach). HeroAtmosphere.tsx deleted; new HeroGlyphField.tsx renders the same seeded domain-warped fBm field (drift/tide/pointer heat carried over) as IBM Plex Mono glyphs via a DPR-aware offscreen atlas (13px cells desktop, 11px mobile, ramp .:;+=*#%@, 6 teal-to-amber tiers, quiet-zone floor 0.2, alpha ceiling 0.55, copy column dimmed to 0.35 over left 52% for contrast); hot path is one drawImage per visible cell, atlas rebuilt on document.fonts.ready; 30fps cadence, frame-budget octave downgrade, offscreen/tab pause, reduced-motion static render carried over. Mono caption added under the CTA (joins entrance choreography, reduce kill list, nbsp-glued separators). CSS: fog canvas block + keyframes deleted, .signal-index-hero-glyphs added (same side masks, NO blur); static SVG-turbulence film grain on .signal-index::after (0.06, soft-light); cursor-tracked ochre card bloom via delegated rAF pointermove writing --mx/--my + .signal-index-card::after radial (pointer-fine gated, keyboard falls back to centered glow, ::before hairline untouched). No new dependencies. Integration deviations: dead COLOR_TIERS===1 guard removed (TS2367), caption nbsp polish, CSS distributed to section layout, note folded into existing reduce kill list. Verified: typecheck + build + git diff --check green; headless Edge at 1440x900 dpr1 and 390x844 dpr3 - field animating, caption/grain/glow live, exit fade and card reveals intact, reduced-motion static drawn frame, zero console errors, zero horizontal overflow; screenshots in glyph-shots (session temp)."}
{"id":"n62","kind":"iteration","actor":"design-iteration","title":"Pass 28 - Mobile hero / the full catalogue in the first screen","ts":"2026-08-28","artifacts":[],"summary":"Owner ask: all projects rendered in the hero list (none truncated or hidden) + balanced mobile hero proportions. Found: <=560px showed only 4 of 6 rows (BENEATH_VISIBLE + .signal-index-beneath-row-extra display:none) behind an 'all 06' compensating link; TSX also silently skipped untagged projects; the desktop grid row-stretch leaked into the stacked mobile layout stranding copy and list ~185px apart at 390x844; copy collided with the absolute 64px header at 320x568. Changed: LandingPage.tsx renders beneath rows unconditionally (untagged would show bare NN), BENEATH_VISIBLE / row-extra / beneath-more machinery deleted; styles.css: <=900px hero align-content:center (stacked copy+index pack and centre as one group), <=560px padding-top calc(64px + 1.25rem) so centred content clears the header, mobile rows align-items:center + min-height:44px touch targets (mh-row draft geometry); desktop untouched. Verified: typecheck + build green; headless Chrome at 390x844 dpr3, 320x568 dpr2, 1440x900 dpr1, 390 reduced - 6/6 rows visible everywhere, copy top 84 vs header 64 on 320 (was 33, colliding), copy->list gap 28px on 390 (was 185), desktop geometry identical, zero console errors, zero horizontal overflow; ledger entry Pass 28 in docs/portfolio-redesign-handoff.md; screenshots in session temp (mobile-hero-probe)."}
{"id":"n64","kind":"iteration","actor":"design-iteration","title":"Pass 29 - Landing polish: 8-bit fox mark, edge-fading card hover, hugging mobile hero, louder entrance","ts":"2026-08-28","artifacts":["file=portfolio/shell/src/shell/ProjectArtwork.tsx","file=portfolio/shell/src/styles.css","doc-anchor=docs/portfolio-redesign-handoff.md#pass-29"],"summary":"Owner four-item ask. Changed: FoxCenterMark rebuilt as 15x12 grid of 8px crispEdges rects (seated amber fox, ink/cream details, dusk pines, fireflies) matching the project's real Bayer-dithered render; card hover replaced flat --ink-panel wash with @property --card-wash driven edge-fading gradient (transparent 0%/100%, 0.055 plateau 20-80%), hover/focus set --card-wash:1 so the 200ms fade survives as a registered custom-property transition; <=560px hero min-height:auto + align-content:start (kills the 100svh centered-pack voids), gap 1.6rem, padding-bottom 2.4rem, header clearance kept (copyTop 84 at 320x568); entrance: rise 16->30px + 40px H1 keyframe, 0.72->0.86s (H1 0.94s), beats .05/.12/.30/.42, rows .50-.80 at 0.06s, glyph canvas 1.25s staged opacity boot-up (no scale/blur), card reveals 28px/18px. Verified: typecheck+build green; headless Chrome 27/27 (errors/overflow zero at 1440/390/320, hero 900px desktop vs 596px mobile hugging content, copyTop 84, rows 44px x6, CSSTransition on --card-wash mid 0.41, 6/6 reveals, reduced-motion static, screenshots reviewed). No deps, no TSX logic changes, --hero-exit/reveal/bloom untouched. Ledger: Pass 29 in docs/portfolio-redesign-handoff.md."}
{"id":"n66","kind":"iteration","actor":"code-iteration","title":"Pass 30 - delegated: plotter draw-in card reveal + curl-noise flow hero","ts":"2026-08-28","artifacts":["file=portfolio/shell/src/shell/HeroField.tsx","file=portfolio/shell/src/shell/LandingPage.tsx","file=portfolio/shell/src/styles.css"],"summary":"Chat-model brief applied (both items). Item 1: card reveal redesigned as a left-to-right clip-path wipe ('plotter draw-in') with topline slide / copy rise / artwork rise stagger (delays +40/+140/+240ms); observer retuned threshold 0 + rootMargin -12%; hidden cards are visibility:hidden inside prefers-reduced-motion:no-preference so reduce and no-JS never hide content and tab order is clean; on-mount #project-<id> hash check reveals deep-linked card up front; grid hairline 700->760ms. Item 2: HeroGlyphField.tsx deleted, new HeroField.tsx - curl-noise flow field (curl of domain-warped fBm potential on coarse grid) drawn as crisp streamline 'filings', batched into 6 Path2D strokes, fully cleared each frame (no ghosting/fog); self-driven morph via noise time-axis so mobile is never static; pointer vortex + permissionless Android tilt as bonus layers; 4-rung EMA quality ladder, 30fps throttle, IO+visibility pause, static frame under reduce; caption updated to 'live field В· curl-noise flow в†’ streamline filings В· canvas2d В· no webgl'. Verified: tsc + vite build green; headless Chromium probe 24/24 (desktop 1440x900: no-input liveness, all hidden below fold -> revealed on scroll, beneath-row scroll lands revealed target at 24px, hairline draw-in; mobile 390x844 dpr3: liveness without input, in-view card revealed at load, fold cards hidden; reduced: everything visible, static hero frame; zero errors/overflow everywhere). Screenshots: %TEMP%\\opencode\\iso-shots\\reveal-desktop-bottom.png, reveal-mobile-load.png. Uncommitted per policy."}
{"id":"n68","kind":"decision","actor":"design-iteration","title":"Hero direction settled via grill: type-led zeitgeist, Unbounded, canvas demoted to texture; mobile reveal bug root-caused","ts":"2026-08-28","artifacts":[],"summary":"Two-round grilling after owner rejected Pass 30 curl-noise filings ('conceptually not it, no design taste, not zeitgeist'). Settled: (1) direction type-led - giant display typography is the hero object (kinetic entrance/breathing, subtle cursor/scroll response), Canvas2D demoted to thin texture layer, must stay self-driven/alive on mobile; taste words: 'bold feeling but minimalistic', bar = 'senior frontend designer page'; only reference allowed: helloclaude.ru (typography-first, giant lowercase wordmark, editorial boldness, minimal decor) - muse-spark dropped by owner (no VPN). (2) Font: Unbounded (Google Fonts, var 200-900) for hero only, page stays IBM Plex; palette may extend 1-2 colors but must fit ink-catalogue. (3) Keep texts meaning + beneath catalog; CTA may be cut; composition freedom to chat model. (4) ONE final hero from chat model, no drafts route. (5) Curl-noise filings deleted outright. (6) Mobile scroll-reveal bug (same brief, separate item): root cause reproduced - Chromium IO delivers NO entry for a card straddling the viewport top after an instant scroll jump (card sat at top=-2, ~380px visible, >1s, never revealed; probe logged obs entries: explosion only); card can end up permanently visibility:hidden. Fix requirement: rAF scroll-math as source of truth, IO not sole mechanism. Constraints unchanged: Canvas2D only, zero deps, no blur/fog, reduce/no-JS gates, hover systems intact. Deliverables: brief at C:\\Users\\v.argunov\\Desktop\\agent2\\chat-model-brief.md (outside repo, overwrites previous), handoff at docs/hero-zeitgeist-handoff.md."}
{"id":"n69","kind":"iteration","actor":"code-iteration","title":"Pass 31: type-led Unbounded hero + scroll-math card reveal integrated and verified","ts":"2026-08-28","artifacts":["file=portfolio/shell/src/shell/HeroTexture.tsx"],"summary":"Chat-model response to chat-model-brief.md applied: (1) new HeroTexture.tsx - sparse self-driven breathing dot-field canvas behind the type (30fps cadence + stride degradation, IO/hidden pause, static frame under reduce, DPR cap 3, StrictMode-safe); (2) hero JSX now type-led: three mask-rising Unbounded lines with ochre breathing amp (steps, no variation tween), kicker/retuned note, CTA removed, sea-warp SVG removed, HeroField.tsx deleted, class rename atmosphere/glyphs -> type/texture; (3) card reveal source of truth = rAF-throttled scroll/resize rect sweep vs band (0.94vh coarse / 0.88 fine, stagger 70/90), IO demoted to trigger, mount+120ms+700ms sweeps, hash deep-link reveal; (4) Unbounded variable font added to Google Fonts link; (5) styles.css: tokens --unbounded/--ink-accent-deep, new hero block, old beneath-panel + entrance choreography blocks removed (chat model could not see them), texture mask + reduce stops. Integrator adjustments beyond the response: headline size fitted to measured Unbounded advance (~6.9em for 'prototypes'): clamp(calc((100vw-72px)/7.4), 11.5rem) desktop, calc((100vw-32px)/7.1) mobile - chat model's 12.4vw/15.5vw numbers overflowed the shell (measured: 418px min-content in a 318px shell at 390); grid-template-columns minmax(0,1fr) so tracks cannot grow; beneath rail minmax 320px (3x2, no row wraps); grid-row 2/3 placement (header+canvas are absolute). Verified: tsc+vite build clean; headless Edge probes desktop 1440 / mobile 390 dpr3 / 320 / reduced-motion: zero console errors, zero overflow, no clipped headline, hero alive with zero input on desktop+mobile (canvas frame diff), --hero-exit fades, fast-flick + straddler at top:-2px + second flick all revealed, beneath-row click and #project-explosion deep link revealed on arrival, reduced = static hero + all cards visible. Screenshots eyeballed. Not committed - awaiting explicit owner delivery ask."}
{"id":"n71","kind":"decision","actor":"user","title":"Owner verdict on Pass 31 hero: 'not it' — next attempt is the drafts route (3 concepts)","ts":"2026-08-28","artifacts":[],"summary":"Owner reviewed the shipped Pass 31 type-led hero (Unbounded giant headline + breathing amp + dot-field texture, commit ec6991d) and rejected it: 'this is not it'. Second consecutive hero rejection (Pass 30 curl-noise, Pass 31 type-led). Per the n68 escalation, the next hero attempt is the drafts route: 3 distinct concepts behind the design-directions route, owner picks, winner becomes the landing hero. Survives regardless: scroll-math card reveal fix, --hero-exit contract, card hover systems, probe toolbox. Open for drafts: keep or drop Unbounded font + --ink-accent-deep. Full handoff: docs/hero-drafts-handoff.md."}
{"id":"n76","kind":"decision","actor":"user","title":"Owner verdict: dither-field card art rejected ('looks dirty, this is not it'); next pass = github.com logged-out landing manner","ts":"2026-08-31","status":"active","meta":{"stash":"stash@","verified":"tsc+vite green; headless probe 16/16"},"artifacts":["doc-anchor=docs/card-artwork-verdict-handoff.md","url=https://github.com/"],"summary":"Delegated six-engine Bayer-dither card attempt (runner/cosmos/forest/layout/fracture/route) was built against a stale pre-raft tree (7034ab1), which is why the removed Code Layout card reappeared. Concept accepted, execution rejected: full-bleed dither fields read as dirt. Not committed; implementation preserved in git stash for lifecycle-shell salvage (IO pause, DPR cap, degradation, reduced-motion static frame). Next iteration: redo cards in the github.com logged-out landing manner - clean, airy, crisp hairlines, calm surfaces, per-project identity kept, wall-to-wall texture dropped. See docs/card-artwork-verdict-handoff.md."}
{"id":"n78","kind":"iteration","actor":"user","title":"Tiger-bright cards / Ember Spine: per-card hue spines + hue hover choreography","ts":"2026-08-31","artifacts":["file=portfolio/shell/src/styles.css","doc-anchor=docs/portfolio-redesign-handoff.md"],"summary":"Owner verdict: cards lean too heavily on minimalism, lack visual impact; mandate 'tiger bright' while keeping minimalism. Delegated brief 5 returned the Ember Spine design: 3px per-card hue spine at 45% alpha (CSS-only --card-accent via #project-<id>), hue tag/numeral, h3 clamp(1.9-2.5rem), hue-tinted artwork hairline; hover/focus ignites spine to full hue + hue hairline sweep + 8% hue wash (registered --card-wash) + hue bloom + hue h3/link + artwork frame border + tight dark shadow lift, all <=340ms; Quiet-index card overrides deleted, media queries/shared CTA/artwork drawing untouched, TSX unchanged. Verified: typecheck+build green; headless Edge 49/49 (hues exact, rest+hover+focus parity, reveal 6/6 with 0 hidden-in-viewport, reduced-motion static+clamped, canvas liveness, zero console/overflow at 1440/390/320). Working-tree change (no commit); screenshots in agent2/card-shots; brief in agent2/chat-model-brief-5-cards-bold.md."}