# BRIEF — Compaction round 6: project-graph node summaries, second checkpoint pass (policy locked)

Inputs (25 post-first-checkpoint nodes verbatim) are NOT duplicated here — retrievable at git `4d90bff`. Policy and coverage only.

## Policy (as delivered to the chat model)

Same policy as round 5 (second checkpoint; KEEP/DROP checklist; auto-records become title stubs; shown deliberation then `---OUTPUT---` + one JSONL line per node echoing its `graph` tag).

Graph-specific rules:
- kitty-run: R1–R6 shipped as one commit (kitty-run n87) → per-node "UNCOMMITTED pending owner review" qualifiers dropped as superseded; explicitly owner-dropped open threads (deferred picks D/E/F: "forget them for now") removed.
- practice-map: n12 is a duplicate of n13 (n14 records the correction); n13 is the kept record.
- raft-cluster: n9 qualitative pass owner-rejected and reverted (n10/n11); n11 keeps both commit shas (b36600a, cd660cd) and the binding restraint lesson (protocol-legibility features read as embellishment; deepen existing quality, don't layer new visual systems).
- raft/spine zoom fix: each graph keeps its own CSS constraint (placement after appearance/narrow-layout rules so desktop values cannot win).

## Coverage

- evening-forest n35–n37 (3)
- explosion n28–n31 (4)
- kitty-run n84–n87 (4)
- practice-map n12–n15 (4)
- raft-cluster n8–n13 (6)
- spine n11–n14 (4)

## Integration notes

- Verified per graph: 0 structural diffs, edge counts preserved (evening-forest 29, explosion 28, kitty-run 81, practice-map 16, raft-cluster 12, spine 13).
- Bytes: evening-forest 23,092; explosion 19,411; kitty-run 60,221; practice-map 11,167; raft-cluster 6,441; spine 7,144.
