# chat-model prompt 39 — about-page (R032, page instead of window)

F014 rejected both window concepts (leaf R030, slip R031) as alike and
off-register; the owner steered to a standalone page at /about. Same
compression level as the proven v4 arm (−30%, ~155 words): telegraphic
spec, taste clause, and the two rejected sheets named as the banned
family so the third draw cannot reconverge on them.

## Paste this

```text
comprehensive code - about-page for my portfolio site: standalone page at /about, NOT an overlay. two prior about windows (right-docked sheets with gutter marks) were rejected as alike and off-register - banned: modal, overlay, scrim, drawer, docked sheet, gutter index-mark patterns. the site is a dark ink catalogue of my 8 projects; house register: mono microcopy, hairlines, one ochre accent, all lowercase, typography does the work. concept yours - the page a senior developer would ship. copy fixed, seated at integration: "# vasyapym" heading, 7 paragraphs, 9 links (8 project + 1 home), 1 em, email last line; project links go through my router, integrator wires them; return-to-index control yours, mono. react 18 + plain css, no deps. fits 390px+1440px; prefers-reduced-motion: fade max. challenge assumptions + failure cases first; rationale = why lines. reply: exactly 3 blocks - concept name + ≤80-word why; AboutPage.tsx ≤160 ln (props {onNavigate(id)}); about-page.css ≤160 ln - nothing else.
```
