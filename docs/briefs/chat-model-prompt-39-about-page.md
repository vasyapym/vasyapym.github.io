# chat-model prompt 39 — about-page (R032, v3 spec framing)

v2 (honest-intent freedom, ~135w) withdrawn pre-relay: owner steered the
register axis back to spec framing. v3: same facts and same design
freedom ("yours" clauses kept), telegraphic key=value register, spec
rails back in (viewports, reduced-motion). One signal vs v2: register.
~115 words.

## Paste this

```text
comprehensive code - about-page, route /about, standalone page (no overlay). site: dark ink catalogue, 8 projects; house register: mono microcopy, hairlines, one ochre accent, lowercase; typography does the work. concept yours - the page a senior developer would ship; layout, type scale, density, return control = yours. copy fixed, seated at integration: "# vasyapym" heading, 7 paragraphs, 9 links (8 project + 1 home), 1 em, email last line; links via my router, integrator wires. react 18 + plain css, no deps. fits 390px+1440px; prefers-reduced-motion: fade max. challenge assumptions + failure cases first; rationale = why lines. reply: exactly 3 blocks - concept name + ≤80-word why; AboutPage.tsx; about-page.css - nothing else.
```
