# BRIEF — Compaction round 2b: main-page graph node summaries, part 2 of 2 (policy locked)

You cannot see the repository. This brief is self-contained. You are rewriting verbose event-log summaries into compact, high-value ones as part of a "Compaction Checkpoint" milestone. Do NOT write code. Output format is specified at the end.

## Context

The repo drives a multi-agent workflow. The main page keeps an append-only JSONL event log (the "portfolio graph"): nodes (kind: snapshot/iteration/decision/milestone; fields: id, actor, title, summary, status, artifacts, meta) and typed edges. Agents orient by folding this log; its prose cost is the problem. The milestone consolidates prior state into compact form; the workflow continues identically. Compaction rules: summarize similar/redundant entries; compact (never remove) important ones — preserve decisions, rationale, open threads, dependencies; maximize value-per-token.

This is **part 2 of 2** for the main-page portfolio graph (the landing shell under portfolio/shell). Part 1 covered n20-n78 (ink catalogue, Cat Runner polish, hero saga passes 21-31, dither rejection, Ember Spine). This part covers the card-shape redesign, the Raft Cluster card-art saga (rounds 1-7: reconcept → refinement → rethink 1-4 → ten concepts → vault quorum ADOPTED → cybernetic round, candidate H ADOPTED into production), press-proof/gems work, card artwork iterations, the README revamp, and the final hero/landing passes. Node ids are permanent; part-1 node ids may be referenced (they are stable) — later nodes should shrink to their delta with cross-node context already carried by the earliest node.

Known end-state (for verdict trails): Raft saga CLOSED — vault 'seven-key majority' adopted as the Raft landing mark (n124), then round-7 cybernetic candidate H adopted into the production slot (n129). Do not restate those adopted verdicts in every node; nodes before them keep their pending state, the adoption lives in the adopting node.

## Checklist — what a kept summary must contain

KEEP (the durable spine):
- Final verdict/decision state — adopted / rejected / pending — including pending→adopted transitions (both states if the trail matters).
- Quality-gate result (tsc/build/probe pass|fail) — usually already in meta; don't duplicate.
- Lesson or hard constraint born in this node (e.g. accumulated ban lists, a family technique, an anchor convention).
- Open thread / explicit next-step, only if unresolved as of the checkpoint.
- Durable pointers (brief filename, doc-anchor, commit sha) are already in `artifacts` — do not restate them in prose.

DROP (reconstructable or duplicated):
- Step-by-step narration of implementation passes (what file changed in what order).
- Owner mood/color commentary — unless the quote itself is the decision (then keep it in one clause).
- Rationale duplicated in a linked doc or in meta.
- Enumerations restatable from the referenced brief.
- "Nothing committed / keep-delivery-separate" boilerplate — collapse to a `verdict: pending` fact.

## Input

JSONL, one object per node: `id, kind, actor, title, ts, status, meta, artifacts, summary`. The `summary` field is your target; `title` may also be tightened. `status`/`meta`/`artifacts` are context only — you output neither.

## Output contract

- One JSONL line per input node, same order: `{"id":"n81","newSummary":"..."}` (optionally add `"newTitle":"..."`).
- `newSummary`: ≤2 sentences, ≤450 characters. If `meta.verdict` / `meta.gate` already carry the outcome, one short sentence is ideal.
- Empty string `""` = drop the summary entirely (title + meta suffice) — prefer this when nothing else is lost.
- `newTitle`: only if the original is bloated or narrates steps; otherwise omit the key.
- Never invent facts; never lose a verdict, a lesson, or an open thread. Cross-node redundancy should land in the EARLIEST node that carries it; later nodes shrink to their delta.
- If you approach output limits, end with the line `TRUNCATED — reply CONTINUE` and finish the remaining ids in the same order after I reply.

## Input nodes

{"id":"n81","kind":"iteration","actor":"design-iteration","title":"Redesign project cards as rounded flat-scrim panels: art-first layout, larger plates, ink-toned quiet hover","ts":"2026-08-31","meta":{"gate":"tsc+vite+29/29 headless","viewports":"1440/390/320","brief":"agent2/chat-model-brief-7-card-shape.md"},"artifacts":["file=portfolio/shell/src/styles.css","file=portfolio/shell/src/shell/LandingPage.tsx"],"summary":"Delegated brief 7 (chat-model-brief-7-card-shape.md) spliced by the integrating agent. Edge-to-edge rows became discrete rounded panels (radius 10px, 1px ink hairline, var(--ink-panel) surface, 2px per-card accent left edge @40%) with gap clamp(1.1rem,2.2vw,1.75rem); inside each panel order is topline -> full-width art plate -> copy. Art plates grew to full card width, min-height clamp(260px,27vw,400px) desktop / clamp(220px,30vw,320px) <=900 / 220 <=700 / clamp(190px,56vw,220px) <=560 (integrator reconciled SPEC's 390->~220 with the 320 floor). Colored hover choreography removed (wash @property+gradient, ::before accent sweep, ::after cursor bloom, hue h3, hue link, 40-60% hue artwork frame); replaced by ink-toned hover: surface 0.045->0.07, hairline soft->line, dark lift shadow 0 14px 30px -18px rgba(0,0,0,0.6), artwork ink border + pre-accepted dark shadow; surviving accent gestures: left edge 60%, inner art frame 22%, open -> ochre --ink-accent-bright. Six --card-accent hexes unchanged and still synced with ProjectArtwork. LandingPage pointermove --mx/--my effect deleted (was bloom-only). Grid top hairline kept; reveal wipe kept with end-state inset(-40px -40px -40px 0) so the lift shadow is not clipped; stagger re-ordered topline+40ms/artwork+140ms/copy+240ms. Mobile keeps art-first at every width (old <=700 art-below-copy intentionally dropped per brief 7; flip back with .project-artwork{order:2} if the owner objects). Verified: tsc --noEmit + vite build green; headless Chrome 29/29 at 1440/390/320 (6 cards, reveal all-visible, geometry/hover computed styles, canvas liveness, zero console errors, zero overflow); screenshots in agent2/card-shots/cards-v7-*.png. Working-tree change, not committed."}
{"id":"n82","kind":"iteration","actor":"design-iteration","title":"Card pass v8 (owner rejected v7): DOM art-first panels, 12px radius, de-hued tag, larger plates","ts":"2026-08-31","meta":{"gate":"tsc+vite+33/33 headless","supersedes-try":"v7 n81","viewports":"1440/390/320"},"artifacts":["file=portfolio/shell/src/styles.css","file=portfolio/shell/src/shell/LandingPage.tsx"],"summary":"Owner verdict on v7: 'illustrations not at all prominent, feels like old design.' Second chat-model output (same brief 7) integrated instead. Diffs vs v7: 12px radius (reveal clip-path inset round 12px, no card box-shadow at all - lift shadow lives on .project-artwork inside the clip); panel gap clamp(1.5rem,3vw,2.25rem); artwork moved above copy in the DOM (TSX edit, not order:-1); plate min-height clamp(260px,26vw,360px) with integrator reconciliation (chat CSS floor 240 vs SPEC 900->260 -> floor 260; <=560 clamp(200px,56vw,220px) to honor SPEC 390->~220 with 320->200 floor); body gap clamp(1.25rem,2.5vw,1.75rem); tag numeral de-hued to --ink-faint (hue now only in 2px spine @42->58% hover + faint 12% art inner frame); topline back to sans 0.76rem; hover = surface 0.045->0.075 + hairline soft->line + art ink border/dark shadow, no other accent; description max-width 560px @0.86rem. Verified: tsc --noEmit + vite build green; headless Chrome 33/33 at 1440/390/320; screenshots agent2/card-shots/cards-v8-*.png. Working-tree change, not committed."}
{"id":"n87","kind":"iteration","actor":"user","title":"Card round 2 verdict: lighthouse subject kept, indigo accent kept; Raft must read as system design, Raft+Explosion need the geometric backdrop family, Raft panel ground colour rejected","ts":"2026-08-30","artifacts":["file=../BRIEF-card-artwork-iteration-2.md"],"summary":"User loves the Explosion moon card and likes the Raft lighthouse mark with its periwinkle accent #8f8cf3, but: (1) the Raft illustration must be readable as system design for a senior-dev portfolio; (2) four of six cards carry thin-bordered geometric figures behind/around the subject (planck cosmic-field, practice-map terrain, kitty/forest contours+compass) and Raft+Explosion must join that family; (3) the indigo-night Raft panel ground is rejected and must be re-coloured. Implementation delegated to the chat model; brief lands at repo root as BRIEF-card-artwork-iteration-2.md."}
{"id":"n90","kind":"decision","actor":"user","title":"Round-3 card verdict: latest card art disregarded (n76-n80), cards feel oversized — full card-system redesign with creative freedom; concept round delegated to chat model","ts":"2026-08-31","artifacts":[],"summary":"Owner rejected the recent card-art churn (log-spine redraw, mid-blast moon rework, azure constellation recolor — commits 6450685/d5fd807/76459f3 stay in history but are disregarded) and finds the cards oversized. New direction: simple, bright, bold animated illustrations; reference = GitHub homepage mascot blobs (reference-images/github image.png); all six cards restyled uniformly (nothing survives, including the Explosion moon); hero-consistency demoted to a light guideline; Raft must still read as system design. Two-step delegation to the chat model: concept document first (BRIEF-card-concept-redesign.md), implementation only after the concept is received."}
{"id":"n91","kind":"iteration","actor":"design-iteration","title":"Gemstones concept received and advanced to implementation; implementation brief prepared for chat model","ts":"2026-08-31","artifacts":[],"summary":"Chat-model concept 'Gemstones' accepted into the pipeline: two-up grid (~624x360 desktop / single column mobile), art stage (200px/150px) over copy block (topline index+tag, title, 2-line description, footer with demoted tech list + open link), shared lifted ground #0f1b20, six chunky glossy SVG gem subjects with layered-halo glow (coral Raft crowned-leader topology, pink leaping cat, teal dusk trees, amber split sphere, violet orb+arc, blue map-pins on tile grid), idle breathe + halo pulse, 6-deg parallax kept, rAF reveal kept with fade/rise entrance, reduced-motion + no-JS gates intact. Concept archived verbatim at CONCEPT-card-gemstones.md; implementation brief at BRIEF-gemstones-implementation.md (deliverables: new ProjectArtwork.tsx with six gem marks, projects-section JSX replacement, self-sufficient appended CSS chunk; integrator deletes all legacy card/artwork CSS and empties parts arrays). Awaiting chat-model code round; integration, typecheck and visual probes happen after paste-back. Uncommitted per policy."}
{"id":"n92","kind":"iteration","actor":"design-iteration","title":"Gemstones implemented (chat-model code integrated), replacing the committed v8/pointer-reactive plates by owner decision; verified 19/19 headless","ts":"2026-08-31","artifacts":[],"summary":"Owner picked full Gemstones over the parallel sessions' committed v8 flat-scrim plates + 5837c06 pointer-reactive canvas engine (that engine stays recoverable in git history). Integrated the chat model's three deliverables with integration fixes: (1) ProjectArtwork.tsx replaced by the six gem SVG marks (raft/kitty/fox/blast/spiral/trail keys kept; canvas engine, parts machinery and art captions dropped); (2) LandingPage card interior restructured gem-style (stage first, then topline-index+tag / title / 2-line desc / footer tech+open; all JS contracts preserved — signal-index-grid, signal-index-card, is-revealed, --reveal-delay, data-project-reveal); (3) styles.css: legacy card/artwork CSS deleted (371-392, 401-686, media rules), Gemstones chunk appended — 2-col grid 24px gap desktop / 1-col 16px mobile at 899px, #0f1b20 panels r20, 200px/150px stages, hover lift -6px + ground brighten + halo boost, focus-visible ochre outline. Integrator corrections to the chat-model code: halo-pulse selectors presentation-raft/planck -> real classNames presentation-raft-cluster/planck-to-now; reveal reworked from transition to keyframe animation with backwards fill (transition-delay: var(--reveal-delay) would have lagged every hover lift by the reveal stagger); removed duplicate section width/margin (shell already constrains to 1280px); added aspect-ratio 260/160 to the center span; scroll-margin-top kept at 1.5rem (no sticky header); index.html Google Fonts +wght 700; parts arrays emptied in all six project.ts. Verified: tsc --noEmit clean for shell (repo-wide build blocked ONLY by the parallel session's uncommitted kitty-run WIP: moonTexture missing in Parallax.tsx — not this pass's files); vite build green; headless chrome-headless-shell 19/19 at 1440/390/320 — 6 cards, svg breathe active, halos, reveal 6/6 on desktop and user-like scroll on mobile, hover lift computed, 2-col/1-col grids, 200/150px stages, zero console errors, zero horizontal overflow, reduced-motion = no animation + all visible, no-JS = CSR page with gate off. Screenshots eyeballed: desktop-cards/mobile-cards match the Gemstones concept. Working-tree change, not committed per policy; dist/ rebuilt as a side effect of verification."}
{"id":"n94","kind":"iteration","actor":"design-iteration","title":"Card round 4 'polish elevation': six gem marks redrawn for project-relevance + flawless execution; judge accepted 4/6, Raft + Cat Runner pending repair","ts":"2026-08-31","meta":{"quality-gate":"judge fail: 3 defects (2 marks)","accepted":"fox, blast, spiral, trail marks; structure; hero; hover; reduced-motion","next":"repair round via BRIEF-card-artwork-polish-repair.md, then re-judge fixed evidence"},"artifacts":["file=BRIEF-card-artwork-polish-elevation.md","file=BRIEF-card-artwork-polish-repair.md","file=portfolio/shell/tests/landing-shots.mjs"],"summary":"Owner verdict: loves the round-3 Gemstones cards incl. illustrations, but they read too playful/informal for a senior portfolio; direction fixed to EVOLVE not replace — same two-up structure, hues, glossy-gradient family and halo technique, with project-true subjects (raft consensus ring + teal vote dot, cat dashing over hill with best-run ghost echo, layered dusk forest, rib-seamed lantern mid-detonation, epoch arc timeline, route map) and a polish system (single top-left light vector, faint gradient-faded halos replacing blot ellipses, optical consistency, palette discipline). Chat-model deliverable integrated into ProjectArtwork.tsx; gem-halo-pulse lowered to 0.08/0.16 with additive --halo-opacity hover; tsc --noEmit clean. New headless evidence probe portfolio/shell/tests/landing-shots.mjs (desktop/mobile/hover/reduced-motion, chrome-headless-shell). Judge pass: fox/blast/spiral/trail marks, structure, hero, hover, reduced-motion. Judge fail: 3 defects — Cat Runner ochre heart ornament; Cat Runner ghost reads as shadow blob (tail + left leg also detached from body); Raft inner glow reads as muddy blot in empty pentagon interior. Repair brief issued to chat model."}
{"id":"n96","kind":"iteration","actor":"design-iteration","title":"Card round 5 'press-proof gems': six gem marks re-skinned into the hero's printed-halftone language; owner verdict pending","ts":"2026-08-31","meta":{"quality-gate":"judge pass 6/6 + tsc + headless probe","verdict":"pending owner","next":"owner review of evidence shots","subsumes":"BRIEF-card-artwork-polish-repair.md"},"artifacts":["file=BRIEF-press-proof-gems.md","file=portfolio/shell/src/shell/ProjectArtwork.tsx","file=portfolio/backups/ProjectArtwork.gem-v4.tsx.bak"],"summary":"Goal: visual consistency between project cards (especially illustrations) and the hero (owner prefers the hero slightly). Direction chosen by owner: align cards to hero, do not strip what makes the gem illustrations appealing; original v4 artwork must stay revertable. Protected history: physical backups portfolio/backups/ProjectArtwork.gem-v4.tsx.bak + styles.css.gem-v4.css.bak, plus git 718b13e/4506a40. Technique: kept subjects, per-card hues (as ramp tips), chunky shapes, top-left light vector, gem-halo/haloVar hover hook (base <= 0.16), breathe + halo-pulse CSS; replaced smooth glossy radial gradients with discrete stepped tone fills (deep base + clipped mid/light caps), SVG <pattern> dot screens (dense 7u/sparse 11u) for halos + heat/texture, square dither glints replacing soft specular ellipses, ink-ramp neutrals (#26333b/#465059/#7d7669/#b6ac95) for context masses so accents concentrate on focal subjects. Stage unified with hero panels: plate #0f1b20 -> #0b1317, inset box-shadow -> 1px var(--ink-line-soft) border, artwork radial sheen removed, hover lift #0f171c. Subsumes the pending repair brief (BRIEF-card-artwork-polish-repair.md): Raft empty-interior stain removed (bloom tucked behind leader; vote dot moved to (160,51) clear of r=20 leader ring), Kitty heart deleted, ghost rebuilt as true 0.16-opacity detached second-cat echo up-left, tail+both legs re-rooted. Chat-model deliverable integrated into ProjectArtwork.tsx (6 functions) with two integration fixes (trail pin holes #0f1b20 -> #0b1317 to match new stage). Guardrail honored (n76): no wall-to-wall dither fields, plates stay clean. Verified: tsc --noEmit clean; headless probe (chrome-headless-shell) desktop hero/cards/hover + mobile + reduced-motion all pass. Judge pass 6/6 marks. Owner verdict pending. Next-review notes: fox tree glints read slightly scattered; card titles still Title Case vs hero lowercase (microtype alignment candidate); trail grid kept bluish slate #274a5c rather than ink neutral."}
{"id":"n99","kind":"decision","actor":"user","title":"Mid-round verdict on Cat Runner Hello-Kitty redraw: 'too girly' — direction narrowed to neutral, drafts route for this mark","ts":"2026-09-01","meta":{"verdict":"partial-reject","rejected":"bow,pink-body,girly-framing","carried":"recognizable-cat,runner-energy,ghost-echo,halftone-family"},"artifacts":[],"summary":"Owner reviewed the three-mark round (Raft system-topology redraw and Planck galaxy redraw integrated, verified, evidence rendered) and narrowed complaints to the kitty alone: 'hello kitty is too girly. redraw'. Rejected: bow, pink-dominant subject, saccharine Hello-Kitty framing. Carried forward: clean recognizable cat, rounded simple features, runner pose, best-run ghost, halftone language, pink #ff8fbf as card hue. Per the n71 precedent the redraw presented four independent variants behind an owner pick instead of a single attempt."}
{"id":"n100","kind":"iteration","actor":"design-iteration","title":"Card round 6 'three marks': Raft reads as consensus topology, Planck leans cosmos, Cat Runner redrawn neutral (V3 ink-detail) after girly verdict","ts":"2026-09-01","status":"active","artifacts":["file=portfolio/shell/src/shell/ProjectArtwork.tsx","file=BRIEF-card-artwork-three-marks.md","file=BRIEF-kitty-mark-redraw-neutral.md"],"summary":"Per-mark improvement round promised by n98, delegated via BRIEF-card-artwork-three-marks.md + BRIEF-kitty-mark-redraw-neutral.md. CHANGED (Raft): crowned leader left-of-centre fans thick neutral links to four uneven followers + follower mesh; ochre log-entry squares ride the leader-follower links (log-replication read); banned teal vote dot removed; coral hue kept. CHANGED (Planck): tilted three-arm spiral galaxy with stepped violet core is the hero; uneven star field + two four-point stars + ink ringed planet; epoch timeline demoted to thin dashed arc with ochre scrubber ON the arc at (207,66). CHANGED (Kitty): Hello-Kitty draft rejected mid-round (n99); four variants rendered for owner pick; winner V3 'ink-detail' — cream #fff4ec subject, ink inner-ear liners + forepaws, ochre nose, no mouth/bow, pink confined to halo/dust-kicks/ghost/rim. LIKED (owner): Raft + Planck as rendered, V3 pick. REJECTED: girly kitty framing (n99). Integrator fixes: kitty chin/body seam closed (body cy 116 to 112), speed streaks moved to dust-kick zone clear of ghost, Planck scrubber detached from arc re-seated, kitty ears rebuilt with buried bases (floating-ear defect), ghost synced. QUALITY GATE: tsc --noEmit clean, vite build green; no browser on this machine so evidence = QuickLook renders of extracted SVGs on the #0b1317 plate (raft/spiral/kitty variants + final kitty from source, all eyeballed, geometry checked numerically); landing-shots.mjs skipped (no Chrome). NEXT REVIEW: card titles Title Case vs hero lowercase (n98 stand); fox tree glints (n98 stand); scarf variant V1 noted as alternate if V3 tires."}
{"id":"n102","kind":"iteration","actor":"design-iteration","title":"Hero round 'card-corner language': hero copy panel + beneath rail adopt the cards' 20px radius + hairline frame via shared --panel-radius token; owner verdict pending","ts":"2026-09-01","meta":{"revert":"portfolio/backups/*.hero-v1.*.bak+git 86b0c62","probe":"24/24 pass desktop+mobile+reduced"},"artifacts":[],"summary":"Goal: visual consistency between hero rectangles (sharp 90-degree corners) and project cards (20px rounded panels, owner prefers cards slightly). Direction chosen by owner: align hero shape language to cards, keep everything that makes the hero appealing; original hero must stay revertable. Protected history: physical backups portfolio/backups/styles.css.hero-v1.css.bak + LandingPage.hero-v1.tsx.bak + HeroFluid.hero-v1.tsx.bak, plus git 86b0c62 (hero files clean at HEAD). Technique: CSS-only, one coherent concern (corner geometry). Added --panel-radius: 20px token on .signal-index scope; hero copy panel gets border-radius + keeps scrim rgba(9,15,18,0.62), 1px --ink-line-soft border and 2px --ink-accent-deep left edge; beneath rail upgraded from border-top strip to full 1px hairline floating panel + radius (scrim 0.78 kept); .signal-index-card refactored onto the same token so the value is load-bearing both directions. Palette untouched (n96/n98 already unified it); no new animations, shadows, or JS; type/canvas/entrance/scroll-fade untouched. Note: one prior chat-model FIND was ambiguous (3 matches for --focus-ring line); disambiguated with context during integration. Verified: npm run build clean (tsc + vite); headless probe (chrome-headless-shell 152, puppeteer-core) 24/24 PASS across desktop 1440x900 / mobile 390x844 / reduced-motion: copy + beneath radii 20px, accent edge 2px, scrims 0.62/0.78 intact, beneath full border, card radius 20px via token, canvas present, zero page/console errors; screenshots reviewed at all three viewports. Owner verdict pending."}
{"id":"n104","kind":"iteration","actor":"design-iteration","title":"Card artwork consistency round: three variant languages (A Two-Ink Plate / B Spot-Colour Overprint / C Blueprint on Ink) implemented on Raft+Kitty+Fox and rendered behind an owner pick; verdict pending","ts":"2026-09-01","status":"active","meta":{"quality-gate":"tsc+vite build clean; probe 0 failures; screenshots reviewed","verdict":"pending owner pick (A/B/C)","next":"complete chosen language across Blast+Spiral+Trail, integrate into ProjectArtwork.tsx, retire draft route"},"artifacts":["file=BRIEF-card-art-consistency-directions.md","file=BRIEF-card-art-variant-a-two-ink-plate.md","file=BRIEF-card-art-variant-b-spot-colour.md","file=BRIEF-card-art-variant-c-blueprint.md","file=portfolio/shell/src/design-directions/art-directions/variant","file=portfolio/shell/src/design-directions/art-directions/variant","file=portfolio/shell/src/design-directions/art-directions/variant","file=portfolio/shell/src/design-directions/art-directions/ArtDire","file=portfolio/shell/tests/art-directions-shots.mjs"],"summary":"Goal: card illustrations feel inconsistent with the hero's two-ink aesthetic + senior tone; refine, don't strip; owner picks from distinct variants. Direction brainstorm delegated (BRIEF-card-art-consistency-directions.md): three accepted specs. Each variant implemented via its own brief for the representative set Raft (abstract system), Kitty (character), Fox (scene): variantA two-ink duotone (identity hues 0%, one ochre signal/card), variantB spot-colour overprint (identity hue confined to <=3 groups, <=~15%, halo <=0.12), variantC drafting line-grammar (single ochre accent, construction-circle halo). Incumbent Blast/Spiral/Trail shown unchanged as reference; subjects and locked geometry preserved everywhere (buried cat ears, raft node coords, forest paths). Integrator fixes: B raft stepped-disc caps restored to clipPath geometry (stacked circles overhung base discs ~1px); C ochre log square kept fill-only (0.8px stroke under the 1.2px floor). New comparison page /art-directions (design-directions/art-directions/: ArtDirections.tsx + art-directions.css + variant modules; route in App.tsx; INCUMBENT_MARKS exported additively from ProjectArtwork.tsx) reusing landing card classes so hover lift/tilt/halo behave natively. Evidence probe tests/art-directions-shots.mjs (chrome-headless-shell 152, puppeteer-core): PASS 3 sections, 0 console/page errors, 0 overflow at 390; screenshots reviewed (desktop per-variant, hover, mobile, reduced-motion). Landing ProjectArtwork.tsx marks UNTOUCHED - pick pending; final pass converts remaining three marks in the chosen language, then retires the draft route."}
{"id":"n105","kind":"decision","actor":"user","title":"Owner picks Variant B 'Spot-Colour Overprint' for the card family; notes kitty reads unredrawn; all six cards to convert","ts":"2026-09-01","meta":{"verdict":"accepted-variant-b","rejected":"variant-a two-ink duotone, variant-c blueprint on ink","next":"brief 5: Blast+Spiral+Trail in language B + kitty spot-colour sharpen; then integrate all six into ProjectArtwork.tsx"},"artifacts":["file=portfolio/shell/src/design-directions/art-directions/variant"],"summary":"Owner reviewed /art-directions evidence (three variant languages on Raft+Kitty+Fox vs incumbent Blast/Spiral/Trail) and chose B: identity hue kept per card as a confined second ink (halftone overprint + focal fill, <=3 groups, <=~15% coverage, neutrals dominate, halo <=0.12 hue-tinted or neutral). Owner perception: 'only raft cluster and evening forest illustrations are redrawn' - the B kitty reads too close to the incumbent neutral-cream cat (n100 V3 was already cream; B only demoted pink to nose+halftone ghost/dust). Final pass therefore converts the remaining three marks (Explosion amber, Planck violet, Practice Map sky) AND sharpens the kitty's spot-colour read within the no-bow/no-mouth/pink-not-dominant guardrails. A and C remain discoverable as graph nodes (n104 artifacts); draft route /art-directions stays for reference."}
{"id":"n106","kind":"iteration","actor":"design-iteration","title":"Card family re-skinned to Variant B 'Spot-Colour Overprint': all six marks integrated into ProjectArtwork.tsx; owner verdict pending","ts":"2026-09-01","status":"active","meta":{"quality-gate":"tsc+vite build clean; landing probe no failures; desktop cards screenshot reviewed","verdict":"pending owner","next":"owner review; on approval round closes, draft route retire is optional cleanup"},"artifacts":["file=BRIEF-card-art-variant-b-complete-family.md","file=portfolio/shell/src/shell/ProjectArtwork.tsx","file=portfolio/shell/tests/landing-shots.mjs"],"summary":"Per n105 pick, Brief 5 (BRIEF-card-art-variant-b-complete-family.md) completed the family: Explosion (amber halftone core cap + seam lines over neutral blast), Planck (violet stepped core + dashed now-sub-segment over neutral cosmos), Practice Map (sky pin caps + you-are-here over neutral survey, grid off slate onto #465059), and the Cat sharpened with levers (a) pink halftone inner-ear caps + (b) pink halftone ground-trail patch (halo stays neutral; body stays cream; no bow/mouth). Integrator fixes: Planck now-segment re-seated onto the dashed arc (start (210,70)->(212,66), control (224,71)->(224,70) per bezier evaluation - old start floated ~5px below the arc); all draft id prefixes (gvb-*, gvb2-*) renamed back to the gem-* convention inside ProjectArtwork.tsx; INCUMBENT_MARKS export now serves the integrated B marks to the kept-as-record /art-directions route (variant modules A/B/C untouched as round history; variantB.tsx kitty remains the pre-sharpen draft). Subjects, compositions and locked geometry preserved; hue discipline <=3 groups/<=~15%; halos base 0.12 (raft+planck CSS pulse unchanged). Verified: tsc --noEmit + vite build clean; landing-shots.mjs probe ran via chrome-headless-shell 152 (desktop hero/cards/hover + mobile + reduced-motion, no failures); desktop cards screenshot reviewed - six marks read as one print system, spot hues sit as accents. Owner verdict pending; revert anchor = git 030d3ad ProjectArtwork.tsx (no backups needed). n96 standing notes (fox glints, Title Case card titles) remain for future micro rounds."}
{"id":"n108","kind":"decision","actor":"design-iteration","title":"Owner rejects Hello-Kitty-iconic kitty mark (n108 draft): reads as animated cartoon mascot, not senior-developer work","ts":"2026-09-01","status":"superseded","artifacts":["file=BRIEF-card-art-kitty-hello-iconic.md"],"summary":"Attempt 2 for the Cat Runner card: iconic Sanrio geometry - wide rx50 head, wide-set small eyes, tiny pink nose, straight whiskers, deep-pink bow, simplified grounded ghost, ordered neutral dashes. Integrated and screenshot-verified, then rejected by owner: it seems animated cartoon, not senior developer stuff. Useful qualities carried forward: wide-set face geometry discipline, ordered horizontal speed dashes, simplified grounded halftone ghost, stray-dot hygiene. Rejection reason compressed here: ANY character-face treatment (chibi or iconic mascot) reads as childrens brand cartoon on this page - the subject must be treated as a design mark, not a character. Bow dies with this direction. Next: logotype-grade stretched running-cat silhouette (full stride, horizontal, no face detail beyond negative-space eye), pink confined to ghost echo + nose."}
{"id":"n109","kind":"iteration","actor":"design-iteration","title":"Card-artwork rework round n109-n114: Cat Runner HK head-mark, Explosion ordered radial shatter, Planck Big Bang epoch ripples, Raft machined topology","ts":"2026-09-01","status":"active","artifacts":["file=BRIEF-card-art-kitty-hk-silhouette.md","file=BRIEF-card-art-blast-spiral-rework.md","file=BRIEF-card-art-raft-refinement.md","file=portfolio/shell/src/shell/ProjectArtwork.tsx"],"summary":"Owner-approved page + Spot-Colour Overprint family held as constraints; four marks reworked per owner directions. LIKED: mark discipline of attempt 3 (flowing silhouette, ordered dashes, grounded halftone ghost) - carried into the final kitty; Raft concept (leader+followers+log packets) kept. REJECTED/COMPRESSED: n108 Hello-Kitty-iconic face (cartoon mascot read); realistic runner cat (owner: loved the language but game is Hello Kitty - brand mismatch); kitty body/torso (owner: ugly - head-only mark won). CHANGED in ProjectArtwork.tsx: KittyCenterMark = head-only HK silhouette (wide head rx40, buried ears, whisker spikes past the edge, negative-space wide-set eyes, pink bow + ghost echo + dash trail, centered cx136); BlastCenterMark = ordered radial shatter (disc + 8 shards on exact angles, amber core, halftone caps on 45/225 pair, radial seams, no glitch); SpiralCenterMark = Big Bang epoch ripples (violet singularity at 52,84, four nested right-opening ellipse bands stepping dark-to-light, violet now-frontier on band 4, stars between bands; spiral galaxy concept retired); RaftCenterMark = refinement (3px/2px machined links, axis-aligned log squares on midpoints, hairline follower bezels, dashed coral broadcast orbit r30, faint quorum boundary ellipse). QUALITY GATE: tsc --noEmit + vite build clean; landing-shots probe (chrome-headless-shell 152) desktop/hover/mobile/reduced-motion no failures; desktop-cards screenshots reviewed each pass. Briefs: BRIEF-card-art-kitty-hello-iconic.md, BRIEF-card-art-kitty-mark-silhouette.md, BRIEF-card-art-kitty-hk-silhouette.md, BRIEF-card-art-blast-spiral-rework.md, BRIEF-card-art-raft-refinement.md. Owner verdict pending on Explosion/Planck/Raft (Cat Runner head-only approved in-chat). n96 standing notes (fox glints, Title Case titles) remain."}
{"id":"n113","kind":"iteration","actor":"design-iteration","title":"Raft cybernetic round: candidate B 'control loop' adopted; Planck rebalanced; mobile card overflow fixed","ts":"2026-09-01","artifacts":[],"summary":"Owner verdict on the append-only columns mark: reads as a clay model — rejected. Two cybernetic candidates (broadcast mesh / control loop) briefed to the chat model (BRIEF-card-art-raft-cybernetic.md), integrated on /art-directions raft round, owner picked B: one dashed feedback ring, crowned leader, coral signal in flight at 2 o'clock, election-answer arc returning at 10, committed register at centre, missed-signal gap at the laggard. Carried forward: ringed-leader accent + coral in-flight protagonist from n111. LIKED: loop grammar reads as a self-regulating system; coral budget ~10%. Changed: RaftCenterMark replaced (ids gem-raft-*, family pulse intact); Planck group rescaled 0.78->0.66 and reseated translate(54 24.6) so the perceived epicentre moves from ~23% to ~34% of mark width (owner asked: smaller circles, shift right); mobile containment — .project-artwork-center capped max-width 240px under 899px so every 260/160 mark clears the 150px stage with breathe ±3px. Quality gate: tsc clean; landing-shots + art-directions-shots pass (4 sections, 0 console errors, 0 mobile overflow); desktop+mobile evidence reviewed. Next review: practice-map rethink round (brief pending), then confirm Planck balance on the landing grid."}
{"id":"n114","kind":"decision","actor":"design-iteration","title":"Stale-tree parallel round: The Core strata concept picked, abandoned on sync","ts":"2026-09-02","status":"abandoned","meta":{"base":5215868,"synced":"608c094"},"artifacts":[],"summary":"Ran against base 5215868 without syncing - 18 commits behind origin/main. Chat-model concept round (brief 9A): owner picked A3 The Core (replicated log as an accreting strata column) over A1 Ledger and A2 Escapement. The completed 9B implementation was discarded before integration once the tree synced to 608c094: raft had already moved through append-only columns (n111) to the adopted control loop (n113). Full code and verdicts archived outside the repo (agent2/chat-model-response-9b-raft-core-ARCHIVE.md). Carry-forward recorded in AGENTS.md: pull origin/main at session start, commit+push finished tasks at session end."}
{"id":"n117","kind":"decision","actor":"design-iteration","title":"Raft visual-interest rethink round: both scene candidates rejected — owner wants the system-design read kept","ts":"2026-09-02","status":"active","artifacts":["file=BRIEF-card-art-raft-rethink.md","file=portfolio/shell/src/design-directions/art-directions/raftRet"],"summary":"Owner verdict on the two rethink candidates (BRIEF-card-art-raft-rethink.md, integrated on /art-directions raft visual-interest round): 'these are not it. i want so that it would be easily understandable that this is system design.' Candidate A 'Succession' (crashed ex-leader, crowned successor, vote in flight) reads as abstract shapes with rings — the election narrative is too subtle; candidate B 'The log raft' (lashed logs, hoisted entry, wake) reads as a boat, not a cluster. Diagnosis: both traded the system-diagram vocabulary (nodes, links, signal flow, logs) for narrative scenes. Round 2 direction: keep the unmistakable architecture-diagram grammar and add interest through composition only — node hierarchy, asymmetric fan-out, live signal stream, aligned commit frontiers. The raftRethink.tsx integration file and round section stay as the documented rejection evidence."}
{"id":"n118","kind":"decision","actor":"design-iteration","title":"Raft round-2 verdict: write-frontier concept kept, execution too conservative — round 3 targets Practice-Map boldness","ts":"2026-09-02","status":"active","artifacts":["file=BRIEF-card-art-raft-rethink-2.md","file=portfolio/shell/src/design-directions/art-directions/raftRet"],"summary":"Owner verdict on round-2 candidates (BRIEF-card-art-raft-rethink-2.md): 'keep working on candidate b. it has something ok. but it is not it. i want i guess more elements. i want something more like practice map illustration boldness - i don't know how to tell it. what you give me feels quite conservative/safe.' Candidate A (replication stream) dropped; candidate B (write frontier: log bus + frontier + stubbed nodes) is the surviving concept but needs more elements and the Practice Map terraced-climb's boldness: full-card structure, many element types (contours/flag/tents/compass equivalent), halftone overprint used boldly, strong focal weight. Round-3 brief: two write-frontier evolutions — 'ledger span' (monumental full-width stepped beam, coral halftone committed-overprint band, beacon frontier, term dial) and 'two-tier consensus' (traffic plane above, log plane below, stubs tying them). Element budget raised to ~35-45 primitives; coral halftone committed band counts as one of the 3 coral groups."}
{"id":"n119","kind":"decision","actor":"design-iteration","title":"Raft round-3 verdict: both bold variants rejected as same-looking — round 4 must break the graphic grammar, not the arrangement","ts":"2026-09-02","status":"active","artifacts":["file=BRIEF-card-art-raft-rethink-3.md","file=portfolio/shell/src/design-directions/art-directions/raftRet"],"summary":"Owner verdict on round-3 candidates (BRIEF-card-art-raft-rethink-3.md, ledger span / two-tier consensus): 'you keep making the same. completely rethink but still consistent.' Diagnosis across rounds 1-3: every candidate reused the same micro-grammar — small stepped-cap disc nodes with hairline rims, thin strokes, tick rows, capsule signals — so all arrangements read as variations of one drawing. Round-4 direction: keep the family TECHNIQUE (plate #0b1317, neutral ramp + coral spot, halftone screens, halo contract, safe area, no text) but adopt a new graphic language with bigger masses/heavier ink. Two grammars briefed: (A) quorum overprint — three giant halftone-filled discs whose triple overlap IS the consensus zone in solid coral; (B) copper trace — the log as a bold PCB trace with via donuts, elbow-routed branch traces, pad-ring nodes, leader as the crowned write-head pad. Neither reuses stepped-cap discs, rims, or tick rows."}
{"id":"n120","kind":"decision","actor":"design-iteration","title":"Raft grammar-break round paused: round-4 candidates (quorum overprint / copper trace) integrated but verdict deferred","ts":"2026-09-02","status":"active","artifacts":["file=BRIEF-card-art-raft-rethink-4.md","file=portfolio/shell/src/design-directions/art-directions/raftRet"],"summary":"Owner stopped the raft rethink after seeing round-4 candidates: 'lets just stop here. these are not these. maybe let's try for later.' Round-4 candidates from BRIEF-card-art-raft-rethink-4.md (grammar break: no stepped-cap discs/hairline links/tick rows) were integrated verbatim into portfolio/shell/src/design-directions/art-directions/raftRethink4.tsx with two mechanical fixes (opaque plate rect removed from both; missing opacity={0.12} added to both gem-halo ellipses) and wired as the 'raft rethink · round 4 — the grammar break' section on /art-directions (probe updated to nine sections). Candidate A quorum overprint (three giant halftone discs, coral triple-overlap consensus zone), candidate B copper trace (log as heavy PCB bus, via donuts, crowned write-head pad). Typecheck + production build verified clean after the stop; screenshot probe for round 4 not run. NOTHING committed — all four rethink rounds (BRIEF files 1-4, raftRethink/2/3/4.tsx, ArtDirections sections, probe updates) sit uncommitted in the working tree alongside another agent's practice-map changes, per keep-delivery-separate. Resume points: render /art-directions round 4, get owner verdict, or revert raftRethink4 if the grammar-break direction is also dead."}
{"id":"n121","kind":"iteration","actor":"design-iteration","title":"Raft round 5 'ten concepts' integrated: ten fresh graphic languages rendered for owner pick; verdict pending","ts":"2026-09-03","status":"active","meta":{"quality-gate":"tsc clean; vite build green; probe 3/3 PASS (10 sections, 0 overflow, 0 console errors)","verdict":"pending owner pick (A-J)"},"artifacts":["doc-anchor=BRIEF-card-art-raft-rethink-5.md","file=portfolio/shell/src/design-directions/art-directions/raftRet","file=portfolio/shell/tests/art-directions-shots.mjs"],"summary":"Owner resumed the paused rethink (n120): the control loop is 'good but boring - needs more visual interest, a fresh concept, still serving its purpose' and asked for TEN variants. Ten concepts locked in BRIEF-card-art-raft-rethink-5.md (accumulated bans from n117-n120 baked in: no stepped-cap discs/hairline rims/tick rows, no quorum-overprint discs, no copper trace, no narrative scenes, no control-loop read; family technique kept - plate, neutral ramp + coral spot, halftone, one gem-halo, safe area, system-design read in under a second). Chat model delivered all ten in one response; integrated verbatim into raftRethink5.tsx (ids gem-raft5-*, no DOM collision with round 4) with three mechanical fixes from numeric geometry review: H suspender hairlines re-seated onto the main-cable Q-curve (delivered tops floated 4-12px below it - the floating-element defect class), D outer rotation chevron nudged onto orbit-2 radius (was 5.5px inside), A train decay dot removed from inside the laggard station disc. Wired as 'raft rethink . round 5 - ten concepts' on /art-directions (11 cards: incumbent + A metro line, B gear train, C punched tape, D orrery, E printing press, F dish array, G honeycomb, H suspension bridge, I vault quorum, J beacon chain); probe art-directions-shots.mjs updated to ten sections. QUALITY GATE: tsc clean, vite build green, probe 3/3 PASS (ten sections, 0 mobile overflow at 390, 0 console errors), desktop evidence reviewed - all ten render distinctly at card scale. NOTHING committed (verdict pending, keep-delivery-separate; rounds 1-4 evidence also still uncommitted in the working tree). Next: owner picks a candidate (or none); on adoption rename winner ids to gem-raft-*, replace RaftCenterMark, compress the rejected nine into graph nodes."}
{"id":"n122","kind":"decision","actor":"user","title":"Owner picks candidate I 'Vault quorum' from the ten-concepts round; next: 10 evolved vault variants with more nodes","ts":"2026-09-03","status":"active","meta":{"verdict":"accepted-candidate-I","rejected":"round-5 candidates A-H,J (kept as alternates in raftRethink5.tsx)","next":"round-6 brief: 10 vault evolutions, 5-7 nodes each"},"artifacts":[],"summary":"Owner verdict on round 5 (n121): candidate I wins - 'i like candidate i. let's keep working on it. add nodes (not only 3).' The vault identity is locked as the working base: giant circular vault door + keyholes-as-nodes + turned-keys-as-committed + crowned coral leader key mid-turn + empty dashed laggard + bolt/hub instrumentation. Rejected nine (metro line, gear train, punched tape, orrery, printing press, dish array, honeycomb, suspension bridge, beacon chain) stay discoverable in raftRethink5.tsx as round history. Round 6 direction: keep the vault read at a squint, raise node count to 5-7 keyholes with a readable majority (quorum arithmetic: turned > empty), add one new mechanism element per variant. Land target: replace RaftCenterMark after the owner picks from the round-6 board."}
{"id":"n123","kind":"iteration","actor":"design-iteration","title":"Raft vault round integrated: ten evolutions of the vault quorum (5-7 nodes each) rendered for owner pick; verdict pending","ts":"2026-09-03","status":"active","meta":{"quality-gate":"tsc clean; vite build green; probe 3/3 PASS (11 sections, 0 overflow, 0 console errors)","verdict":"pending owner pick (A-J)"},"artifacts":["doc-anchor=BRIEF-card-art-raft-rethink-6.md","file=portfolio/shell/src/design-directions/art-directions/raftRet","file=portfolio/shell/tests/art-directions-shots.mjs"],"summary":"Per n122 (owner picked candidate I 'vault quorum', asked for more nodes), brief BRIEF-card-art-raft-rethink-6.md locked ten evolutions of the vault identity, each 5-7 keyholes with a legible turned majority and ONE added mechanism: A seven-key majority (coral quorum span), B combination dial (notch ring as log), C time-lock sweep (sector overprint), D door ajar (committed log bars revealed), E safe-deposit wall (each node a vault), F turning wave (per-key phase arcs), G bolt-work ring (retracted vs protruding bolts), H tumbler stack (log as discs seeking alignment), I deposit slot (write port + committed grille), J quorum bell (rings on majority). Chat model delivered all ten; integrated verbatim into raftRethink6.tsx (ids gem-raft6-*) with a systemic fix the brief's own anchors caused - bottom-arc keyholes pulled inward in 8 variants so straight-down key shafts stay inside the door rim (delivered heads landed on/past the rim, worst case breaching the safe area at y147) - plus: A span re-threaded, D log bars re-seated inside the interior (~13px delivered outside the frame), G laggard detached head removed (bolt collision), H leader shaft dropped (head buried in disc 1) + gate seated on disc edge, I window re-seated inside the inner ring, J bell bracket closed onto the dome, F crest chevron seated. Wired as 'raft vault round' on /art-directions (11 cards: base = round-5 winner + A-J); probe updated to eleven sections. QUALITY GATE: tsc clean, vite build green, probe 3/3 PASS (11 sections, 0 mobile overflow, 0 console errors), desktop evidence reviewed - all ten render distinctly at card scale. NOTHING committed (verdict pending). Next: owner picks; on adoption rename winner ids to gem-raft-*, replace RaftCenterMark, retire/keep draft route per owner, compress rejects into graph nodes."}
{"id":"n124","kind":"decision","actor":"design-iteration","title":"Vault 'seven-key majority' adopted as the Raft Cluster landing mark; card-artwork rethink saga closed (rounds 1-6)","ts":"2026-09-03","status":"active","meta":{"verdict":"adopted-vault-seven-key-majority"},"artifacts":["file=portfolio/shell/src/shell/ProjectArtwork.tsx","file=portfolio/shell/src/design-directions/art-directions/ArtDire"],"summary":"Owner verdict on the vault round (n123): 'do candidate a. then commit and push.' Candidate A 'seven-key majority' adopted: RaftCenterMark in ProjectArtwork.tsx replaced by the vault (ids gem-raft-dense/sparse/halo keep the family prefix), replacing the control-loop mark adopted at n113. History accuracy: the six older /art-directions rounds' 'current' cards switched from INCUMBENT_MARKS.raft to RaftCandidateB (the control loop as it lives in raftCybernetic.tsx, its origin round) with toplines reworded 'then-current', so each round still compares against its actual incumbent. QUALITY GATE: tsc clean, vite build green, landing-shots probe rendered (desktop cards evidence reviewed - vault mark reads on the landing grid, other five cards untouched), art-directions probe 3/3 PASS (11 sections, 0 overflow, 0 console errors). The rejection chain compressed across n117-n123: control loop (good but boring) -> scene candidates (not system design) -> write frontier (too conservative) -> ledger span/two-tier (same-looking) -> quorum overprint/copper trace (not these) -> ten concepts -> vault quorum -> seven-key majority ADOPTED. Delivered in the same session per the standing git rule."}
{"id":"n126","kind":"decision","actor":"design-iteration","title":"Raft round-7 direction set: TEN cybernetic variants off the circuit-organism reference; brief written for the chat model","ts":"2026-09-04","status":"active","meta":{"verdict":"round-7-brief-ready; next=chat-model delivers A-J, then integrate + owner pick"},"artifacts":["doc-anchor=BRIEF-card-art-raft-rethink-7.md","file=reference-images/cybernetics.jpg"],"summary":"Owner verdict on the adopted vault 'seven-key majority' (n124): good but boring - needs more visual interest or a fresh concept while still serving its purpose. New direction: reference-images/cybernetics.jpg (black circuit-organism poster - central CPU package, white traces branching at 90/45-degree elbows, terminal vocabulary of solder dots, rings, pads, dashed ticks, resistor/IC packages; asymmetric, organic, white-on-black). Owner: thematically liked but not appropriate as-is for a card illustration - translate, give 10 variants. BRIEF-card-art-raft-rethink-7.md drafted for the chat model (brainstorm round: the model invents ten cybernetic concepts itself, unlike round 6 where mechanisms were pre-anchored). Guardrails baked in: family technique + halo contract + safe area unchanged; ids gem-raft7-*; components RaftCyberA-J; Raft system read in under a second (leader without text, committed vs future, coral protagonist, laggard deficiency, direction); round-4B copper-trace rejection explained - cybernetic vocabulary must carry the Raft read, not replace it; vault identity, control loop, round-5 alternates and round-6 mechanisms banned; max two central-hub radiating-spoke compositions. Two-part output protocol (A-E, F-J) with TRUNCATED boundary convention for chat-model timeout. Integration target on delivery: portfolio/shell/src/design-directions/art-directions/raftRethink7.tsx + new /art-directions section + probe section count 11->12."}
{"id":"n127","kind":"iteration","actor":"design-iteration","title":"Raft cybernetic round integrated: nine chat-model concepts (A-I) rendered for owner pick; verdict pending","ts":"2026-09-04","status":"active","meta":{"quality-gate":"tsc clean; vite build green; probe 3/3 PASS (12 sections, 0 overflow, 0 console errors); verdict=pending owner pick (A-I); source=chat-model round timed out after candidate I"},"artifacts":["file=portfolio/shell/src/design-directions/art-directions/raftRet","file=portfolio/shell/tests/art-directions-shots.mjs"],"summary":"Chat-model round 7 (brief n126): the model timed out after candidate I, owner said work with the nine delivered. Integrated into portfolio/shell/src/design-directions/art-directions/raftRethink7.tsx (ids gem-raft7-*, components RaftCyberA-I) with a systemic mechanical-fix pass: leader crown rings that grazed/sliced their chip silhouettes re-fitted everywhere (A r27->r31 past corners, B chip 28x28 so r24 crown clears + crown pulled inside safe area, C crown rebuilt as a coral pill outline around the wide writer bar, D chip 26x26, H chip 24x20, I crown r24->r25); F ring rebuilt on one r50 circle (delivered nodes wandered r50-62, arcs wobbly) with dots/chevrons/token re-seated and the dashed laggard arc split to stop at the dead node; C laggard ring un-translated and seated on its trace end; D laggard leg re-anchored to the trace vertex + protagonist seated; G traces trimmed to the register column, cells aligned to trace heights, chip shrunk for crown clearance, efferent command re-routed to exit the chip; E trunk seated on the plate edge, branch fork moved, laggard leg re-routed; H clock ticks straddled onto stage edges + invalid two-segment polyline points (M/L inside points) split into two polylines - caught by the probe as a console error; I wavefront arcs re-centred on the transmitter, ack stubs seated on the coral arc. Wired as 'raft rethink . round 7 - cybernetic directions' on /art-directions (12 cards: base = incumbent vault + A ganglion, B bus arbiter, C write frontier column, D repeater chain, E clock tree, F token ring, G sensor bus, H shift-register ripple, I transceiver broadcast); probe art-directions-shots.mjs updated to twelve sections. QUALITY GATE: tsc clean, vite build green, probe 3/3 PASS (12 sections, 0 mobile overflow at 390, 0 console errors), desktop evidence reviewed - all nine render distinctly at card scale. NOTHING redesigned: verdict pending on the board. Next: owner picks a candidate (or none); on adoption rename winner ids to gem-raft-*, replace RaftCenterMark, compress rejects into graph nodes."}
{"id":"n129","kind":"decision","actor":"design-iteration","title":"Owner picks candidate H (shift-register ripple) from the round-7 cybernetic board; promoted into the production Raft CenterMark slot","ts":"2026-09-04","status":"merged","meta":{"quality-gate":"tsc clean; vite build green; probe 3/3 PASS (12 sections, 0 overflow, 0 console errors)","verdict":"candidate H adopted"},"artifacts":["file=portfolio/shell/src/shell/ProjectArtwork.tsx","file=portfolio/shell/src/design-directions/art-directions/ArtDire"],"summary":"Verdict on the nine-concept cybernetic board (n127): candidate H wins - the cluster reads as a shift-register ripple: crowned leader flop (top-left) shifts the coral bit down a diagonal cascade of stages, two stages hold it (stage 3 overprinted coral dense fill), the next stage sits dashed and empty ahead of the frontier, the laggard stage stalls off the cascade with an X across it, clock-tick log lines straddle the committed stage edges, and the coral bit is mid-hop into the frontier. Production promotion: RaftCenterMark in portfolio/shell/src/shell/ProjectArtwork.tsx replaced (vault seven-key majority out), ids renamed to the family prefix gem-raft-*, exactly one gem-halo ellipse with haloVar(0.12) opacity 0.12; ArtDirections.tsx round-7 board re-labelled (candidate h marked adopted, base re-marked 'round-6 pick, then-incumbent' for the record). QUALITY GATE: tsc clean, vite build green, probe 3/3 PASS (12 sections, 0 mobile overflow at 390, 0 console errors). Next: portfolio-ready target reached for the raft card; any further raft work starts from the adopted ripple mark."}
{"id":"n140","kind":"iteration","actor":"user","title":"design-iteration (hero): list cap + More affordance, symmetric desktop rhythm, Explosion 3rd / Spine 4th","ts":"2026-09-06","artifacts":["file=portfolio/shell/src/catalog/discover-projects.ts","file=portfolio/shell/src/shell/LandingPage.tsx","file=portfolio/shell/src/styles.css"],"summary":"Liked: 6-project rail look (3 cols x 2 rows), smooth scroll-to-card rows. Rejected: unbounded beneath list (7th row broke the 2-row proportions); asymmetric hero padding (header->copy ~160px vs rail->bottom ~58px at 1440x900). Changed: discover-projects pinnedOrder -> raft-cluster, kitty-run, explosion, spine, evening-forest; beneath renders slice(0,6) + centered more-down anchor to #projects (mobile 560px cap: row 6 hidden, 5 + more); --hero-bottom-pad measured in a LandingPage effect so header->copy, copy->rail and rail->viewport-bottom gaps all read equal; nth-child(7) breathe delay. Quality gate: typecheck ok, vite build ok, probe gap delta 0.0/0.0/0.1px at 1440x900/1280x800/1600x1000, more-click lands #projects.top=0, mobile 5 visible rows + hidden row 6, no overflow-x at 390. Next review: after the next project lands, check the more-row wrapping at mid widths 900-1100px."}