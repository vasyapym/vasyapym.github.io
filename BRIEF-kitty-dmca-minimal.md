comprehensive code - react tsx: a portfolio game's white cat mascot reads as Hello Kitty (no mouth, ear bow, 3 whiskers/side, yellow oval nose, wide flat head) — redesign it to break that combo while keeping the big-head small-ear silhouette cute and familiar, invent your own differentiating features/accessory/colors; deliver two complete drop-in functions, no diffs: KittyCenterMark (svg viewBox="0 0 260 160", dark #0b1317 halftone card art with dot-screen <pattern> fills, keep ids gem-cat-dense/gem-cat-sparse/gem-cat-halo/gem-cat-head-clip, exactly one <ellipse className="gem-halo" style={haloVar(0.12)}/>, no imports/props/hooks) and KittyPortrait (svg viewBox="0 0 100 100", outline-poster style fill="none" stroke="#3a3142" strokeWidth={3}, hardcoded colors, head+body+arms bust); plus a short design-language spec listing each new feature with hex + a palette role (base coat, outline ink, red accent + deep step, pink clothing + deep step, yellow, cheek, eye ink) so a flat-shape 3D rig can reuse it.
