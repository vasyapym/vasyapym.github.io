import type { LessonSection } from "../curriculum";

export const sections: readonly LessonSection[] = [
  {
    heading: null,
    blocks: [
      {"kind": "p", "text": "Когда программа начинает передавать данные между функциями, обрабатывать ошибки и освобождать ресурсы, одного знания синтаксиса недостаточно: вы либо угадываете последствия изменений, либо умеете их предсказывать. Rust делает значительную часть этих последствий предметом явных правил и проверок. Этот текст рассчитан на вас, если вы уже пишете на любом распространённом языке; знания C, C++ или устройства процессорной памяти не потребуются. В первых частях мы пройдём от запуска проекта до владения ресурсами, не пытаясь одновременно охватить многопоточность и низкоуровневый код; все примеры ориентированы на **Rust 1.90.0 stable, edition 2024**."},
    ],
  },
  {
    heading: "Часть 0. Зачем вообще существует Rust",
    blocks: [
      {"kind": "p", "text": "Вы меняете обработку одной записи, а ошибка проявляется в другом месте: данные неожиданно изменились, ресурс уже освобождён или задержка выросла из-за скрытой работы среды исполнения. Чтобы понять решения Rust, полезно сначала увидеть требования, из которых они выросли."},
      {"kind": "p", "text": "**0.1. История как объяснение требований**"},
      {"kind": "p", "text": "Rust начался как проект Graydon Hoare, затем получил поддержку Mozilla. Работа над экспериментальным браузерным движком Servo сделала особенно существенным сочетание производительности, безопасности памяти и параллельного исполнения. Браузер обрабатывает недоверенные данные, долго работает и взаимодействует со множеством ресурсов: ошибки управления памятью здесь не просто мешают отладке, а становятся уязвимостями."},
      {"kind": "p", "text": "Версия 1.0 вышла в 2015 году. Важна не дата сама по себе, а переход к стабильным обещаниям языка и библиотеки: на инструмент стало возможно опираться в долгоживущих проектах. Rust искал не максимально короткую запись программы, а способ совместить контроль над ресурсами с проверяемыми ограничениями на их использование."},
      {"kind": "p", "text": "**0.2. Философия как реакция на реальные издержки**"},
      {"kind": "p", "text": "В Rust нет обязательного tracing GC — сборщика мусора, который ищет достижимые объекты, обходя связи между ними. Основной механизм освобождения строится на **владении**: у значения есть ответственный за его жизненный цикл. Это позволяет связывать освобождение с определёнными событиями программы, но не обещает отсутствие выделений памяти или фиксированную длительность каждой операции."},
      {"kind": "p", "text": "Компилятор отвергает некоторые программы, для которых его правила не позволяют установить безопасность, даже если автор знает, что конкретный запуск безопасен. Это сознательная граница статического анализа, а не доказательство ошибки каждого отвергнутого алгоритма. Правила приходится выражать через структуру данных и интерфейсы."},
      {"kind": "p", "text": "Глубокое копирование динамических данных также не происходит неявно при обычном присваивании: потенциально дорогую работу нужно обозначить. Ожидаемые ошибки обычно возвращают через `Result`, а **паника** (`panic`) представляет другой канал — например, нарушение внутреннего условия. Сам язык не запрещает вам использовать панику неуместно; различие поддерживается проектированием API."},
      {"kind": "p", "text": "Честная обратная сторона: динамические проверки, выделения памяти и утечки остаются возможными. Безопасность памяти не означает правильность бизнес-правил, отсутствие зависаний или защиту от любых атак."},
      {"kind": "p", "text": "**0.3. Четыре свойства с наблюдаемыми последствиями**"},
      {"kind": "p", "text": "**Владение и RAII** связывают ресурс с временем жизни управляющего значения. RAII — подход, при котором освобождение ресурса привязано к уничтожению этого значения. Вы сможете объяснить, почему буфер освобождается при выходе из области видимости; механизм разберём в части 4."},
      {"kind": "p", "text": "**Статическая проверка доступа** ограничивает несовместимые способы обращения к данным. Следствием становится отказ компиляции там, где другой язык мог бы оставить риск на время исполнения. После владения это приведёт нас к **заимствованию**."},
      {"kind": "p", "text": "**Алгебраические типы и сопоставление с образцом** позволяют описать данные как комбинации полей и альтернатив. Добавление нового варианта может обнаружить забытый обработчик ещё при сборке; это центральный результат части 3."},
      {"kind": "p", "text": "**Абстракции с контролируемой стоимостью и единая цепочка инструментов** помогают сопоставлять удобную запись с выполняемой работой и одинаково собирать проект. Это не обещание бесплатности любых абстракций: динамический вызов или выделение памяти никуда не исчезают. Начнём с инструментов в части 1."},
      {"kind": "p", "text": "**0.4. Ментальная модель №1: где находится доказательство и кто платит за проверку**"},
      {"kind": "p", "text": "Разделяйте три уровня. На первом компилятор устанавливает свойство заранее: например, соответствие типов аргументов. На втором программа проверяет условие во время исполнения: обычное индексирование среза проверяет границы, если оптимизатор не устранил проверку как ненужную. На третьем автор `unsafe`-кода обязан обосновать условия безопасности операций, которые компилятор полностью не проверяет."},
      {"kind": "p", "text": "Предсказание **Ментальной модели №1**: успешно собранная программа всё ещё может паниковать на неверном индексе — проверка просто находится на другом уровне. Плата выражается соответственно в ограничениях исходника, работе при исполнении или обязанности автора предоставить корректное обоснование."},
      {"kind": "p", "text": "Граница модели: она показывает место ответственности, но сама ничего не доказывает. Начнём с различия между проверкой исходника и наблюдением работающей программы."},
    ],
  },
  {
    heading: "Часть 1. От исходника до проверяемого запуска",
    blocks: [
      {"kind": "p", "text": "Вы создали проект анализатора записей, редактор перестал подчёркивать ошибки, но программа ещё ни разу не запускалась. Привычка считать отсутствие диагностики подтверждением работоспособности здесь особенно опасна: разные инструменты отвечают на разные вопросы."},
      {"kind": "p", "text": "**1.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Нам нужен воспроизводимый путь от текста до наблюдаемого результата. Важно понимать, какой исходник проверяется, с какими настройками, во что он собирается и запускался ли полученный файл. Иначе можно успешно проверить библиотеку, но не проверить нужный бинарный target, либо запустить старый артефакт вручную."},
      {"kind": "p", "text": "Для будущего **анализатора записей** сначала установим короткий рабочий цикл. Его результат пока скромный: программа действительно исполняет выбранный код и выдаёт ожидаемый ответ."},
      {"kind": "p", "text": "**1.2. Как это устроено внутри**"},
      {"kind": "p", "text": "`rustup` устанавливает и выбирает набор инструментов Rust. `rustc` — компилятор. **Cargo** читает описание проекта, разрешает зависимости и организует вызовы компилятора, тестов и исполняемых файлов."},
      {"kind": "p", "text": "**Package**, или пакет Cargo, описывается файлом `Cargo.toml`. Он может содержать библиотечный target и несколько бинарных targets. **Target** здесь означает цель сборки, например бинарную программу из `src/main.rs`; это не то же значение слова, что целевая платформа. **Crate** — единица компиляции Rust с корневым модулем. Наш минимальный пакет содержит один бинарный target, исходники которого образуют один crate."},
      {"kind": "p", "text": "`Cargo.toml` задаёт свойства пакета, зависимости и **features** — именованные возможности конфигурации. Features выбираются при сборке, а не переключаются как обычные переменные работающей программы. `Cargo.lock` фиксирует разрешённые версии зависимостей."},
      {"kind": "p", "text": "Стрелки обозначают разные этапы, а не одно действие. `cargo check` проверяет выбранную конфигурацию, но обычно не создаёт готовый исполняемый файл; `cargo build` создаёт его; `cargo run` при необходимости собирает и запускает. Инвариант цепочки: вывод каждого этапа относится только к выбранным исходникам, targets и настройкам. Успех предыдущего этапа не заменяет следующий."},
      {"kind": "p", "text": "**1.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Предполагается, что `rustup` установлен и доступен в терминале; первая установка toolchain может потребовать сеть. Создадим пакет, затем заменим `src/main.rs`. Входных данных нет; только запуск должен вывести `Анализатор: 2 записи`, не считая служебных сообщений Cargo."},
      {"kind": "p", "text": "Первый вызов проверяет корректность программы в рамках правил компилятора. Второй создаёт бинарный артефакт, обычно в `target/debug`. Третий исполняет `main`: именно здесь появляется строка анализатора. Если исходник не изменился, Cargo может использовать уже подготовленные результаты — повторная команда не обязательно означает повторную компиляцию."},
      {"kind": "p", "text": "**1.4. Ловушка №1: проверка прошла — значит, программа работает**"},
      {"kind": "p", "text": "Следующий полный пример компилируется, но при запуске намеренно паникует:"},
      {"kind": "p", "text": "Ошибочное предположение: проверка исходника исполняет достижимые ветви. Механически `panic!` допустим в теле функции, поэтому `cargo check` не обязан его отвергать. Симптом — успешная проверка и аварийное завершение при `cargo run`. Исправление работает потому, что оно меняет проверяемое утверждение с «исходник принимается компилятором» на «запуск даёт ожидаемый результат»: паника в теле допустима для проверки, но не для работы. Исправленный вариант — пример «Первый запуск анализатора» — нужно действительно запустить и сравнить результат с ожиданием; впоследствии такие ожидания станут тестами."},
      {"kind": "p", "text": "**1.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Быстрая проверка при правках — `cargo check`; Что она не доказывает: Правильность результата", "Получение бинарного файла — `cargo build`; Что она не доказывает: Успешность исполнения", "Проверка конкретного запуска — `cargo run`; Что она не доказывает: Корректность остальных входов"]},
      {"kind": "p", "text": "Для приложения сохраняйте `Cargo.lock` в системе контроля версий. Для библиотеки он также полезен при разработке, но не фиксирует версии зависимостей у её потребителей. Сам lockfile не закрепляет компилятор и внешнюю среду, поэтому полной воспроизводимости одного файла недостаточно."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Я разделяю «код принят», «артефакт построен» и «сценарий прошёл»: это разные свидетельства. Даже правильный запуск ещё не объясняет, почему выражение вычислило именно такое число; следующим шагом разберём значения и порядок вычислений."},
    ],
    examples: [
      {"title": "Путь от исходников до запуска", "code": "исходники + Cargo.toml + Cargo.lock + настройки\n                         |\n                         v\n              проверка / компиляция\n                         |\n                         v\n                 артефакт сборки\n                         |\n                         v\n                       запуск", "explanation": "Стрелки обозначают разные этапы; успех предыдущего не заменяет следующий."},
      {"title": "Создание пакета на Rust 1.90.0", "code": "rustup toolchain install 1.90.0\ncargo +1.90.0 new record_analyzer --bin --edition 2024\ncd record_analyzer\nrustup override set 1.90.0", "explanation": "Команды закрепляют toolchain и создают бинарный пакет с edition 2024."},
      {"title": "Первый запуск анализатора", "code": "fn main() {\n    let records = 2;\n    println!(\"Анализатор: {records} записи\");\n}\n// Ожидаемый вывод программы:\n// Анализатор: 2 записи", "explanation": "Только `cargo run` исполняет `main`: строка появляется именно здесь, а не при проверке."},
      {"title": "Три команды, три разных свидетельства", "code": "cargo check\ncargo build\ncargo run", "explanation": "`check` проверяет, `build` создаёт артефакт, `run` исполняет его."},
      {"title": "Ловушка: паника проходит проверку", "code": "fn main() {\n    panic!(\"Записи пока не обрабатываются\");\n}\n// При запуске: сообщение о панике и неуспешное завершение.\n// Точное оформление диагностики зависит от окружения.", "explanation": "Код компилируется успешно, но при запуске аварийно завершается."},
    ],
  },
  {
    heading: "Часть 2. Значения, выражения и управление потоком",
    blocks: [
      {"kind": "p", "text": "Вы считаете долю обработанных записей, переводите результат в дробный тип и всё равно получаете ноль. Знакомая математическая запись не гарантирует знакомой арифметики: сначала нужно выяснить типы операндов и порядок операций."},
      {"kind": "p", "text": "**2.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Нам нужно предсказывать значение выражения до запуска. Для этого недостаточно читать формулу слева направо: Rust связывает каждую операцию с типами, а блоки и ветвления тоже могут возвращать значения."},
      {"kind": "p", "text": "Особенно важно различать изменение существующей переменной и создание новой с тем же именем. В коротком примере разница незаметна, но при последовательной обработке данных она определяет, какие преобразования вообще допустимы."},
      {"kind": "p", "text": "**2.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Выражение** имеет тип и при обычном завершении даёт значение. Блок `{ ... }` может получить значение последнего выражения без точки с запятой. Если завершающего выражения нет, его результат — `()`, единичный тип с единственным значением. Поэтому `;` не просто оформление: в блоке `{ 7; }` результатом становится `()`, а не число."},
      {"kind": "p", "text": "`let` создаёт привязку имени к значению. Без `mut` присваивание нового значения этой привязке запрещено. **Shadowing**, затенение, — повторный `let` с тем же именем: новая привязка может иметь другой тип. При обычном присваивании тип существующей переменной не меняется."},
      {"kind": "p", "text": "Ветви `if`, используемого как значение, должны согласоваться по типу. `loop` может вернуть значение через `break значение`; `while` и `for` обычно используют ради действий, их результат — `()`. Диапазон `0..n` исключает `n`, а `0..=n` включает верхнюю границу."},
      {"kind": "p", "text": "Сначала компилятор определяет типы, затем проверяет допустимость операций и совместимость результатов. Инвариант: место, ожидающее значение определённого типа, должно получать совместимое значение по каждому нормально завершающемуся пути. Это подготовка к **Ментальной модели №4**: тип задаёт обязательства, а не только способ хранения."},
      {"kind": "p", "text": "Числовые типы задают диапазон и арифметику: `u32` — беззнаковое 32-битное целое, `f64` — число с плавающей точкой двойной точности. Литералы получают тип из контекста или суффикса. При обычной арифметике переполнение целых во время исполнения по умолчанию вызывает панику в профиле dev, а в release даёт оборачивание; настройки профилей могут это изменить. `checked_*` позволяет явно получить признак переполнения, `wrapping_*` — явно выбрать оборачивание независимо от этих настроек."},
      {"kind": "p", "text": "**2.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "В нашем пакете замените `src/main.rs` и выполните `cargo run`. Входы заданы в коде: одна готовая запись из четырёх. Ожидаются успешные проверки `assert_eq!` и строка `Готово: 25.0%`."},
      {"kind": "p", "text": "Присваивание меняет `total`, сохраняя тип `u32`. Функция проверяет ненулевой знаменатель во время исполнения, затем выполняет дробное деление. Блок передаёт наружу результат умножения. Добавьте `;` после `share * 100.0` — `percent` станет иметь тип `()`, и последующее использование как числа перестанет компилироваться. Равенства здесь точные: выбранные дроби представимы в `f64` без округления."},
      {"kind": "p", "text": "**2.4. Ловушка №2: приведение исправит уже выполненное вычисление**"},
      {"kind": "p", "text": "Этот самостоятельный пример показывает различие при ненулевом знаменателе:"},
      {"kind": "p", "text": "В первом случае целочисленное деление уже отбросило дробную часть. Преобразование нуля в `f64` не восстанавливает потерянную информацию. Во втором операнды преобразованы до деления; все значения `u32` представимы в `f64` точно, хотя результат произвольного деления может округляться."},
      {"kind": "p", "text": "**2.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Накопить счётчик — `let mut` и присваивания", "Обозначить новый этап преобразования — Shadowing или новое имя", "Сделать переполнение частью алгоритма — Явный `checked_*` или `wrapping_*`"]},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Я выбираю тип по допустимым значениям и требуемым операциям, а не по краткости записи. Но одно число ещё не выражает различие между «результат равен нулю» и «результата пока нет»: для этого потребуется описать состояния данных."},
    ],
    examples: [
      {"title": "Дробное деление и значение блока", "code": "fn ratio(ready: u32, total: u32) -> f64 {\n    assert!(total != 0, \"Общее число записей должно быть ненулевым\");\n    ready as f64 / total as f64\n}\n\nfn main() {\n    let mut total = 3_u32;\n    total += 1;\n    let ready = 1_u32;\n\n    let percent = {\n        let share = ratio(ready, total);\n        share * 100.0\n    };\n\n    assert_eq!(ratio(1, 4), 0.25);\n    assert_eq!(percent, 25.0);\n    println!(\"Готово: {percent:.1}%\");\n}\n// Ожидаемый вывод:\n// Готово: 25.0%", "explanation": "Операнды преобразованы до деления; блок возвращает значение последнего выражения."},
      {"title": "Позднее преобразование не возвращает дробную часть", "code": "fn main() {\n    let a = 1_u32;\n    let b = 2_u32;\n    assert!(b != 0);\n\n    let late = (a / b) as f64;\n    let early = a as f64 / b as f64;\n\n    assert_eq!(late, 0.0);\n    assert_eq!(early, 0.5);\n}\n// Вывода нет: обе проверки проходят.", "explanation": "Целочисленное деление уже отбросило остаток; `as f64` сохраняет усечённый результат."},
    ],
  },
  {
    heading: "Часть 3. Типы данных как ограничения на состояний",
    blocks: [
      {"kind": "p", "text": "Вы храните признак готовности записи отдельно от результата и обнаруживаете сочетание «готово, но значения нет». Проблема возникает раньше обработки: структура данных позволяет создать состояние, которому предметная область не придаёт смысла."},
      {"kind": "p", "text": "**3.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Нам нужно не только проверять корректность данных после создания, но и уменьшать множество некорректных состояний, которые можно выразить. Если результат существует только после завершения обработки, представление должно связывать эти факты."},
      {"kind": "p", "text": "Это не отменяет проверку внешнего ввода. Оно позволяет один раз перевести вход в осмысленный тип, а затем опираться на его структуру, вместо повторения одного условия в каждой функции."},
      {"kind": "p", "text": "**3.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Кортеж** объединяет несколько значений, возможно разных типов; позиции различаются номерами. **Массив** `[T; N]` содержит ровно `N` элементов одного типа, причём длина входит в тип. **Структура** (`struct`) объединяет именованные поля, существующие одновременно."},
      {"kind": "p", "text": "**Перечисление** (`enum`) описывает альтернативы: значение находится ровно в одном варианте и содержит данные этого варианта. При создании `Progress::Ready(12)` вы одновременно выбираете состояние и предоставляете обязательное для него число. У `Progress::Pending` такого числа нет."},
      {"kind": "p", "text": "`match` выполняет **сопоставление с образцом**: выбирает подходящую ветвь и может извлечь её поля. Компилятор проверяет исчерпывающий охват вариантов. Деструктуризация аналогично разбирает структуры и кортежи на составляющие. Блок `impl` связывает с типом методы и функции, но сам по себе не добавляет ограничений на значения полей."},
      {"kind": "p", "text": "Инвариант перечисления: данные конкретного варианта доступны только после установления этого варианта. Поэтому нельзя просто прочитать число из `Pending`, как будто оно там существует."},
      {"kind": "p", "text": "`Option<T>` и `Result<T, E>` пока достаточно понимать как обычные перечисления стандартной библиотеки: `Some(T)` либо `None`, `Ok(T)` либо `Err(E)`. Их полезность начинается не с особого синтаксиса обработки ошибок, а с необходимости учитывать альтернативу."},
      {"kind": "p", "text": "**Ментальная модель №4: тип как набор проверяемых обязательств.** Она предсказывает, что добавление варианта в `enum` обнаружит неисчерпывающие `match` без универсальной ветви. Граница модели: тип не доказывает предметную истинность. `Ready(999)` структурно корректен, даже если исходный файл содержит только десять записей."},
      {"kind": "p", "text": "Наконец, семантическая структура не равна гарантированному размещению в памяти. Из наличия варианта нельзя вывести отдельный байт-тег или порядок полей: стандартное представление допускает оптимизации. Для форматов файлов и внешних интерфейсов нужны отдельные правила."},
      {"kind": "p", "text": "**3.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Замените `src/main.rs` и выполните `cargo run`. Проверим два входа: ожидание и готовый результат `12`. Ожидаются успешные утверждения и строка `Запись 7: Some(12)`."},
      {"kind": "p", "text": "Метод получает значение, различает варианты и возвращает наличие либо отсутствие числа. Деструктуризация извлекает оба поля записи; кортеж сохраняет идентификатор рядом с ответом. Если удалить обработку `Pending` из `match`, код перестанет компилироваться: отсутствие результата нельзя проигнорировать молча. Здесь мы передаём значения целиком; последствия этой передачи станут темой части 4."},
      {"kind": "p", "text": "**3.4. Ловушка №3: несколько полей всегда проще одного перечисления**"},
      {"kind": "p", "text": "Фрагмент определения, не самостоятельная программа:"},
      {"kind": "p", "text": "Предположение «эти поля всегда согласованы» не выражено в типе. Он разрешает четыре комбинации категорий: `false/None`, `false/Some`, `true/None`, `true/Some`. Если предметная область допускает только первую и последнюю, половина категорий состояний требует дополнительного запрета."},
      {"kind": "p", "text": "Наблюдаемый симптом — разные участки программы доверяют разным полям. Один сообщает готовность, другой не находит значение. `Progress` устраняет само представление противоречия; это сильнее соглашения «не забывайте обновлять оба поля»."},
      {"kind": "p", "text": "**3.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Небольшая локальная группа значений — Кортеж", "Поля существуют одновременно и имеют смысловые имена — `struct`", "Состояния взаимоисключающие — `enum`"]},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Я стараюсь помещать обязательную связь внутрь типа, а не в комментарий рядом с ним. Однако корректная форма значения ещё не отвечает на вопрос, кто вправе использовать его после передачи функции и кто освободит связанные ресурсы."},
    ],
    examples: [
      {"title": "Состояния записи через enum", "code": "enum Progress {\n    Pending,\n    Ready(u32),\n}\n\nstruct Record {\n    id: u32,\n    progress: Progress,\n}\n\nimpl Progress {\n    fn completed(self) -> Option<u32> {\n        match self {\n            Self::Pending => None,\n            Self::Ready(count) => Some(count),\n        }\n    }\n}\n\nfn main() {\n    assert_eq!(Progress::Pending.completed(), None);\n\n    let record = Record {\n        id: 7,\n        progress: Progress::Ready(12),\n    };\n    let Record { id, progress } = record;\n    let result = (id, progress.completed());\n\n    assert_eq!(result, (7, Some(12)));\n    println!(\"Запись {}: {:?}\", result.0, result.1);\n}\n// Ожидаемый вывод:\n// Запись 7: Some(12)", "explanation": "Исчерпывающий `match` не даёт игнорировать альтернативу: без ветви `Pending` код не компилируется."},
      {"title": "Фрагмент с рассогласованными полями", "code": "struct FragileProgress {\n    ready: bool,\n    value: Option<u32>,\n}", "explanation": "Тип допускает четыре сочетания флагов там, где предметная область допускает два."},
    ],
  },
  {
    heading: "Часть 4. Владение: перемещение, копирование и освобождение",
    blocks: [
      {"kind": "p", "text": "Вы передали строку функции и ожидаете продолжить работу с прежней переменной, но компилятор запрещает обращение. В Rust присваивание и передача аргумента не означают автоматически ни создание независимого объекта, ни появление общей ссылки."},
      {"kind": "p", "text": "**4.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Динамический буфер должен существовать, пока используется, и освобождаться ответственным участником программы. Если несколько независимых участников считают ресурс своим, возникает риск повторного освобождения; если никто не отвечает за него — утечки."},
      {"kind": "p", "text": "Rust делает ответственность частью семантики значений. Сначала разберём передачу этой ответственности целиком, без временного доступа к чужому значению. Такой доступ — **заимствование** — потребует следующего слоя правил."},
      {"kind": "p", "text": "**4.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Владелец** отвечает за жизненный цикл значения; в простом случае это локальная переменная. Для значения без `Copy` присваивание другой переменной или передача параметру по значению выполняет **move** — перемещение ответственности. Старое место больше нельзя использовать как содержащее прежнее значение."},
      {"kind": "p", "text": "У `String` есть управляющее значение и принадлежащий ему буфер динамической памяти:"},
      {"kind": "p", "text": "Схема концептуальная: длина измеряется в байтах, ёмкость описывает доступное место в буфере. Она не задаёт порядок полей, не обещает размещение управляющего значения на стеке и не означает, что при каждом move процессор обязательно копирует эти поля. Move — семантическое событие; машинные действия определяет компиляция."},
      {"kind": "p", "text": "**Copy** разрешает неявное копирование значения: исходное остаётся доступным. Так ведёт себя `u32`, но не `String`. **Clone** предоставляет явный способ получить другое значение; для `String` это независимая копия содержимого. Не любой `clone` означает глубокое копирование: поведение зависит от типа."},
      {"kind": "p", "text": "При обычном выходе из области видимости компилятор организует уничтожение оставшихся значений. **Drop glue** — сгенерированный код очистки, который вызывает необходимые деструкторы и рекурсивно уничтожает поля. Так RAII связывает освобождение буфера с уничтожением управляющего значения. Перемещённое значение не уничтожается повторно в прежнем месте."},
      {"kind": "p", "text": "**Ментальная модель №2: граф владения и освобождения ресурсов.** Рёбра показывают, какое значение отвечает за какое вложенное значение или ресурс. Для нашего `String` инвариант — единственная цепочка ответственности за буфер. Предсказание: после move очистка относится к новому владельцу. Граница: это семантический граф, не физическая карта памяти; RAII не гарантирует очистку при `abort`, а намеренные утечки также допустимы."},
      {"kind": "p", "text": "**4.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "В том же пакете замените `src/main.rs` и выполните `cargo run`. Входы — строка `запись` и число `7`. Ожидаются две строки вывода и успешное утверждение; освобождение памяти само по себе здесь не печатается."},
      {"kind": "p", "text": "Первое присваивание переносит строку из `source` в `moved`, не запрашивая независимую копию буфера. `clone` создаёт такую копию явно. Вызов `consume` передаёт исходную строку параметру `text`, а `drop(duplicate)` принимает вторую строку по значению и уничтожает её до конца `main`. Число, напротив, остаётся доступным под обоими именами благодаря `Copy`."},
      {"kind": "p", "text": "**4.4. Ловушка №4: присваивание создаёт независимую копию ресурса**"},
      {"kind": "p", "text": "**Этот код не компилируется:**"},
      {"kind": "p", "text": "Ошибочное предположение: два имени автоматически означают два независимых ресурса. Но присваивание для `String` означает move; обращение к `first` пытается использовать уже перемещённое значение. Симптом — диагностика использования после move, а не скрытое копирование и не ошибка во время запуска."},
      {"kind": "p", "text": "Исправление зависит от намерения: продолжать работу через `second`, передать строку окончательно или явно создать `clone`, как в предыдущем примере. Механическое добавление копий лишь скрывает неясную ответственность и увеличивает расход памяти."},
      {"kind": "p", "text": "**4.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Передать ответственность — Параметр по значению; для `String` — move", "Сохранить независимое содержимое — `clone`; копирование байтов и память для копии", "Только прочитать существующие данные — Заимствование, которое разберём далее"]},
      {"kind": "p", "text": "Для `String` стоимость клонирования растёт с длиной содержимого в байтах; перемещение само по себе не требует копирования содержимого буфера. При панике с раскруткой стека уничтожаются соответствующие локальные значения, но при аварийном прекращении процесса такой гарантии нет."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Я сначала спрашиваю, кому должен принадлежать результат операции, и только потом выбираю передачу или копию. Следующая проблема возникает естественно: как позволить функции прочитать строку, не отдавая владение и не оплачивая независимый буфер?"},
    ],
    examples: [
      {"title": "String: дескриптор и буфер", "code": "переменная-владелец\n        |\n        v\nзначение String\n[указатель | длина | ёмкость]\n        |\n        v\nбуфер в куче: байты строки", "explanation": "Схема концептуальная: move не обещает копирования байтов буфера."},
      {"title": "Перемещение, клонирование и Copy", "code": "fn consume(text: String) {\n    println!(\"{text}\");\n} // Значение text уничтожается при выходе.\n\nfn main() {\n    let source = String::from(\"запись\");\n    let moved = source;\n    let duplicate = moved.clone();\n\n    consume(moved);\n    drop(duplicate);\n\n    let before = 7_u32;\n    let after = before;\n    assert_eq!(before, after);\n    println!(\"{before} {after}\");\n}\n// Ожидаемый вывод:\n// запись\n// 7 7", "explanation": "Строка передаёт ответственность, `u32` остаётся доступен под обоими именами."},
      {"title": "Использование после move", "code": "fn main() {\n    let first = String::from(\"запись\");\n    let second = first;\n\n    println!(\"{first}\");\n    println!(\"{second}\");\n}", "explanation": "Не компилируется: это диагностика перемещения, а не скрытая копия."},
    ],
  },
  {
    heading: "Часть 5. Заимствование: разрешения на доступ, а не запрет мутации",
    blocks: [
      {"kind": "p", "text": "**5.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "В анализаторе вы прочитали поле записи, а затем хотите дополнить буфер. Почему иногда это разрешено, а иногда borrow checker — **проверяющий заимствования механизм компилятора** — останавливает сборку? Вопрос не в том, объявлена ли переменная с `mut`, а в том, какие доступы должны оставаться действительными одновременно."},
      {"kind": "p", "text": "**5.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**`&T`** предоставляет совместный доступ; **`&mut T`** — эксклюзивный доступ, через который обычно можно изменять значение. Это не утверждение, что за `&T` ничего никогда не меняется: типы с внутренней изменяемостью предоставляют собственные безопасные операции. Но обычную запись в `T` совместная ссылка не разрешает."},
      {"kind": "p", "text": "Оператор **разыменования** `*` обращается к значению через ссылку. Из `&mut T` можно получить более короткое **повторное заимствование**, или reborrow: пока оно используется, исходный доступ ограничен его разрешениями. Это не две независимые эксклюзивные ссылки."},
      {"kind": "p", "text": "**NLL**, нелексические времена жизни заимствований, позволяют учитывать фактические использования ссылки, а не обязательно весь текстовый блок её переменной. Для следующего линейного случая полезна схема:"},
      {"kind": "p", "text": "Здесь рассматриваются независимые доступы к одной области памяти. Два чтения совместимы; перекрывающее их изменение — нет. Нижняя строка показывает последовательность, а не одновременность. Для раздельных областей вывод будет другим."},
      {"kind": "p", "text": "**Срез `&[T]`** представляет заимствованный диапазон элементов: адрес начала и длину, но не владение буфером. **`&str`** аналогично представляет диапазон байтов, для которого гарантирована корректность UTF-8. Анализатору часто нужен именно вид на часть записи, а не вся коллекция владельца."},
      {"kind": "p", "text": "**5.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Все запускаемые примеры этих частей заменяют `src/main.rs` пакета `record_analyzer`; используются Rust 1.90.0 stable и edition 2024. Проверка: `cargo run`."},
      {"kind": "p", "text": "`split_at_mut` проверяет позицию разделения и возвращает два непересекающихся изменяемых среза. Независимость здесь обеспечивает контракт библиотечной операции; клонировать элементы не требуется. Так можно готовить отдельные области записи к независимой обработке."},
      {"kind": "p", "text": "**5.4. Ловушка №5: если памяти достаточно, ссылку можно сохранить через push**"},
      {"kind": "p", "text": "**Этот код не компилируется:**"},
      {"kind": "p", "text": "Существенная часть диагностики:"},
      {"kind": "p", "text": "В общем случае рост `Vec` способен переместить буфер и сделать прежний адрес непригодным. Однако здесь свободная ёмкость действительно есть — и это всё равно не устраняет ошибку: `push` требует `&mut Vec<_>`, перекрывающий заимствование, которое понадобится последующему `println!`. Компилятор не заменяет правила доступа рассуждением о конкретной ёмкости."},
      {"kind": "p", "text": "Завершите использование ссылки до `push`, как в примере «Конец заимствования и непересекающиеся части», либо сохраните **индекс** и заново получите ссылку:"},
      {"kind": "p", "text": "Индекс не обещает сохранения логической идентичности элемента после произвольных удалений или перестановок."},
      {"kind": "p", "text": "**5.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["`&str` или `&String` — При чтении текста принимайте `&str`: он подходит и для строки, и для её части", "`&[T]` или `&Vec<T>` — При чтении последовательности принимайте `&[T]`, если свойства самого `Vec` не нужны", "Длительность `&mut` — Ограничивайте участком, действительно требующим эксклюзивного доступа"]},
      {"kind": "p", "text": "**Ментальная модель №3: разрешения на доступ во времени.** Она помогает предсказать конфликт, но не является полной формализацией правил наложения ссылок в `unsafe`."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Сначала выясняют, какие области действительно пересекаются, затем выбирают API, выражающий это разделение. Клонирование ради обхода непонятной ошибки обычно откладывают до выяснения причины."},
    ],
    examples: [
      {"title": "Совместимость доступов во времени", "code": "time ------------------------------------------------>\n\nread A:   [=======]\nread B:      [=======]       OK\n\nread A:   [=======]\nwrite B:     [=======]       conflict\n\nwrite A:  [=======]\nwrite B:     [=======]       conflict\n\nread A:   [====]\nwrite B:          [====]     OK", "explanation": "Перекрывающиеся чтение и изменение конфликтуют; последовательные — нет."},
      {"title": "Конец заимствования и непересекающиеся части", "code": "fn main() {\n    let mut values = vec![10, 20];\n\n    let first = &values[0];\n    println!(\"first={first}\");\n    // Последнее использование first уже прошло.\n    values.push(30);\n\n    {\n        let (left, right) = values.split_at_mut(1);\n        left[0] += 1;\n        right[0] += 2;\n    }\n    assert_eq!(values, [11, 22, 30]);\n\n    {\n        let access = &mut values[..];\n        {\n            let shorter = &mut *access; // reborrow\n            shorter[2] += 1;\n        }\n        access[2] += 1; // короткое заимствование закончено\n    }\n\n    assert_eq!(values, [11, 22, 32]);\n    println!(\"{values:?}\");\n}\n\n// Ожидаемый вывод:\n// first=10\n// [11, 22, 32]", "explanation": "После последнего использования ссылки `push` разрешён; `split_at_mut` разделяет срез без клонирования."},
      {"title": "Ссылка, переживший push", "code": "fn main() {\n    let mut values = Vec::with_capacity(16);\n    values.push(10);\n\n    let first = &values[0];\n    values.push(20);\n    println!(\"{first}\");\n}", "explanation": "Не компилируется: конфликт заимствований, даже при свободной ёмкости."},
      {"title": "Диагностика E0502", "code": "error[E0502]: cannot borrow `values` as mutable because it is also borrowed as immutable", "explanation": "Компилятор называет конфликтующие заимствования явно."},
      {"title": "Доступ по индексу", "code": "fn main() {\n    let mut values = vec![10];\n    let index = 0;\n    values.push(20);\n    assert_eq!(values[index], 10);\n}\n// Вывода нет; assert проходит.", "explanation": "Индекс вместо ссылки: правило не зависит от предположений о ёмкости."},
    ],
  },
  {
    heading: "Часть 6. Времена жизни: отношения, которые проверяет компилятор",
    blocks: [
      {"kind": "p", "text": "**6.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Анализатор может возвращать поле без копирования, но тогда результат зависит от чужого буфера. Если буфер уничтожен, сохранить ссылку недостаточно. Требуется договор: какие входные данные должны оставаться доступными, пока используется результат."},
      {"kind": "p", "text": "**6.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Lifetime**, или время жизни ссылки, здесь следует понимать как ограничение допустимого использования, а не таймер жизни объекта. Именованный параметр `'a` связывает позиции в сигнатуре. Компилятор проверяет, можно ли удовлетворить этим отношениям в конкретном вызове."},
      {"kind": "p", "text": "Большинство функций обходится без явных имён благодаря **elision**, правилам опускания аннотаций. В обычных сигнатурах каждый опущенный входной lifetime получает отдельный параметр; при единственном входном lifetime он назначается выходным ссылкам. Для метода с `&self` или `&mut self` выходные ссылки по умолчанию связываются с заимствованием получателя. Если источников несколько и эти правила не определяют связь, её задаёте вы."},
      {"kind": "p", "text": "У `longest` оба аргумента допускают заимствование на общий регион `'a`. Возвращаемая ссылка пригодна в пределах этого региона: сигнатура не сообщает вызывающему, какая ветка победила. Это развитие **М3**: важны не только пересечения доступов, но и зависимости между ними."},
      {"kind": "p", "text": "Структура со ссылкой тоже несёт такое ограничение. Отдельно различайте **`&'static T`** и **`T: 'static`**. Первое — тип ссылки, допускающей использование на протяжении всей программы; хранить саму ссылку так долго необязательно. Второе — ограничение типа: он не содержит заимствований, привязанных к более коротким регионам. Владеющий `String` удовлетворяет `T: 'static`, хотя конкретный `String` можно уничтожить через секунду."},
      {"kind": "p", "text": "**6.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Длина здесь измеряется в байтах; для демонстрации отношений ссылок этого достаточно. Во второй расстановке областей видимости исчезновение промежуточной переменной не уничтожает строку-источник."},
      {"kind": "p", "text": "А вот перенос использования за границу источника запрещён. **Этот код не компилируется:**"},
      {"kind": "p", "text": "Диагностика `E0597` сообщает, что `inner` живёт недостаточно долго: отмечает `&inner` как происхождение заимствования, конец блока как уничтожение источника и `println!` как последующее использование. Даже очевидный человеку выбор `outer` не меняет договор сигнатуры."},
      {"kind": "p", "text": "**6.4. Ловушка №6: аннотация lifetime продлевает жизнь локального значения**"},
      {"kind": "p", "text": "**Этот код не компилируется:**"},
      {"kind": "p", "text": "Ошибка `E0515` указывает на возврат ссылки на локальную переменную. Предположение неверно механически: после выхода локальный `String` уничтожается, а обещанный результат должен оставаться пригодным. Замена `'a` на `'static` не создаёт владельца и не отменяет уничтожение."},
      {"kind": "p", "text": "Исправление зависит от задачи: возвращайте `String`, если функция создаёт данные, либо заимствуйте данные вызывающего. Оба варианта одного разбора показаны в примере «Две расстановки областей и оба результата разбора»."},
      {"kind": "p", "text": "**6.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Заимствованный — Поле уже находится во входном буфере; Требование к вызывающему: Сохранять источник и соблюдать ограничения доступа", "Владеющий — Результат должен пережить вход; Требование к вызывающему: Принять владение; возможно выделение памяти", "Структура со ссылкой — Несколько представлений связаны с источником; Требование к вызывающему: Учитывать эту зависимость при хранении структуры"]},
      {"kind": "p", "text": "**Ментальная модель №4: тип как обязательство.** Сигнатура с `'a` — проверяемый договор; реализация обязана ему соответствовать, а вызывающий — предоставить подходящие заимствования."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Lifetime описывает отношения между значениями, а не управляет памятью. Если аннотации становятся сложнее, сначала проверяют, не нужен ли результату собственный владелец."},
    ],
    examples: [
      {"title": "Две расстановки областей и оба результата разбора", "code": "fn longest<'a>(a: &'a str, b: &'a str) -> &'a str {\n    if a.len() >= b.len() { a } else { b }\n}\n\nstruct RecordView<'a> {\n    field: &'a str,\n}\n\nfn parse_borrowed(line: &str) -> RecordView<'_> {\n    RecordView {\n        field: line.split_once(':').map_or(line, |(_, field)| field),\n    }\n}\n\nfn parse_owned(line: &str) -> String {\n    parse_borrowed(line).field.to_owned()\n}\n\nfn main() {\n    let outer = String::from(\"lengthy\");\n    {\n        let inner = String::from(\"tiny\");\n        let result = longest(&outer, &inner);\n        assert_eq!(result, \"lengthy\");\n        // Использование результата заканчивается внутри блока.\n    }\n\n    let other = String::from(\"brief\");\n    let result;\n    {\n        let temporary_binding = &other;\n        result = longest(&outer, temporary_binding);\n    }\n    // Исчезла переменная ссылки, но не источник other.\n    assert_eq!(result, \"lengthy\");\n\n    let owned;\n    {\n        let line = String::from(\"kind:warning\");\n        let view = parse_borrowed(&line);\n        assert_eq!(view.field, \"warning\");\n        owned = parse_owned(&line);\n    }\n    assert_eq!(owned, \"warning\");\n    println!(\"lifetime checks passed\");\n}\n\n// Ожидаемый вывод:\n// lifetime checks passed", "explanation": "Результат применим, пока живы оба потенциальных источника; владеющий вариант от заимствованного отличает договор."},
      {"title": "Использование за границей источника", "code": "fn longest<'a>(a: &'a str, b: &'a str) -> &'a str {\n    if a.len() >= b.len() { a } else { b }\n}\n\nfn main() {\n    let outer = String::from(\"lengthy\");\n    let result;\n    {\n        let inner = String::from(\"tiny\");\n        result = longest(&outer, &inner);\n    }\n    println!(\"{result}\");\n}", "explanation": "E0597: время жизни `inner` недостаточно, даже если человеку очевиден выбор `outer`."},
      {"title": "Ссылка на локальное значение", "code": "fn field<'a>() -> &'a str {\n    let local = String::from(\"warning\");\n    &local\n}\n\nfn main() {}", "explanation": "E0515: аннотация не создаёт владельца и не продлевает жизнь локального `String`."},
    ],
  },
  {
    heading: "Часть 7. Трейты, обобщения и цена полиморфизма",
    blocks: [
      {"kind": "p", "text": "**7.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Одни и те же сведения анализатора нужно выводить двумя способами. Копировать алгоритм под каждый формат неудобно, но скрытие реализации тоже имеет последствия: где выбирается метод, какой тип известен и сколько вариантов кода понадобится?"},
      {"kind": "p", "text": "**7.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Трейт** определяет доступные операции; блок `impl` реализует их для конкретного типа. **Bounds**, ограничения параметров типов, записываются рядом с параметром или в `where`: второй вариант удобен для развёрнутых требований. **Ассоциированный тип** задаётся реализацией трейта: например, форматировщик выбирает тип своего результата."},
      {"kind": "p", "text": "**`derive`** генерирует поддерживаемые реализации по структуре типа: `Debug` для диагностического представления, `Clone` для клонирования и другие. Это не автоматическое выполнение произвольного пользовательского договора."},
      {"kind": "p", "text": "**Coherence**, согласованность реализаций, запрещает неоднозначные перекрывающиеся `impl`. Основная практическая форма **orphan rule**, правила «сирот»: внешний трейт нельзя реализовать просто для внешнего типа. Реализация обычно должна находиться в крейте трейта или локального типа; для обобщённых `impl` существуют дополнительные ограничения покрытия параметров. Так независимые библиотеки не добавят конкурирующие реализации одной пары."},
      {"kind": "p", "text": "Верх схемы показывает **мономорфизацию**: компилятор создаёт специализированные варианты обобщённого кода для используемых типов. Низ показывает динамическую диспетчеризацию: концептуально ссылка несёт адрес данных и адрес **vtable**, таблицы служебных сведений и методов реализации."},
      {"kind": "p", "text": "Специализация открывает возможности встраивания вызовов, но может увеличивать размер машинного кода. Динамический вызов обычно добавляет косвенный переход и может затруднить встраивание; оптимизатор иногда устраняет его. Сам `&dyn Trait` не требует выделения памяти в куче."},
      {"kind": "p", "text": "**7.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Оба способа вызова здесь создают `String`: это стоимость реализации форматирования, а не обязательная стоимость полиморфизма."},
      {"kind": "p", "text": "**Замыкание** — значение с захваченным окружением, которое можно представлять как поля скрытой структуры. Семейство `Fn`/`FnMut`/`FnOnce` определяется использованием захватов: совместный доступ, возможность изменять окружение, возможность потребить его. `Fn` допускает также вызовы по договорам `FnMut` и `FnOnce`. В примере возврат `text` перемещает захваченный `String`, поэтому повторный вызов невозможен. Само слово `move` ещё не делает любое замыкание исключительно `FnOnce`."},
      {"kind": "p", "text": "**7.4. Ловушка №7: impl Trait позволяет возвращать любой реализующий трейт тип**"},
      {"kind": "p", "text": "**Этот код не компилируется** при добавлении к определениям из примера «Один контракт, два вызова»:"},
      {"kind": "p", "text": "Диагностика `E0308` сообщает о несовместимых типах веток. Возвращаемый **непрозрачный тип** `impl Trait` — один конкретный тип, выбранный при компиляции, а не контейнер для любых реализаций."},
      {"kind": "p", "text": "Рабочий `choose` возвращает всегда `Selected`; меняется вариант перечисления, не тип. Владеющую динамическую альтернативу отложим до части 11."},
      {"kind": "p", "text": "**7.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Именованный generic-параметр — Нужны статическая специализация и связи между несколькими позициями типа", "`impl Trait` — В аргументе — отдельный обобщённый параметр; в результате — один скрытый конкретный тип", "`dyn Trait` — Реализация должна варьироваться во время выполнения через общий интерфейс"]},
      {"kind": "p", "text": "Для **dyn-совместимости** нужны дополнительные условия. В частности, динамически вызываемые методы не могут иметь обобщённых параметров типов или возвращать `Self`. Такие методы можно исключить из динамического интерфейса условием `where Self: Sized`; существуют и другие ограничения. Обычные ассоциированные типы, как `Output`, в данном объектном типе фиксируются явно."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Выбирают требуемую вариативность, а не самую короткую запись. **М4** объясняет bounds как обязательства. **М1 — место доказательства** — помогает разделить этапы: специализация выполняется при компиляции, выбор метода через vtable — обычно во время исполнения; соответствие трейту проверяется заранее в обоих случаях."},
    ],
    examples: [
      {"title": "Статическая и динамическая диспетчеризация", "code": "generic call\n    -> specialized code for Plain\n    -> specialized code for Tagged\n\n&dyn Report<Output = String>\n    -> data pointer\n    -> vtable pointer -> render function", "explanation": "Мономорфизация создаёт специализированные варианты; vtable хранит адреса методов реализации."},
      {"title": "Один контракт, два вызова", "code": "trait Report {\n    type Output;\n    fn render(&self) -> Self::Output;\n}\n\n#[derive(Debug)]\nstruct Plain(usize);\n\n#[derive(Debug)]\nstruct Tagged(usize);\n\nimpl Report for Plain {\n    type Output = String;\n\n    fn render(&self) -> String {\n        format!(\"records={}\", self.0)\n    }\n}\n\nimpl Report for Tagged {\n    type Output = String;\n\n    fn render(&self) -> String {\n        format!(\"[records:{}]\", self.0)\n    }\n}\n\nfn static_render<R>(report: &R) -> String\nwhere\n    R: Report<Output = String>,\n{\n    report.render()\n}\n\nfn dynamic_render(report: &dyn Report<Output = String>) -> String {\n    report.render()\n}\n\nenum Selected {\n    Plain(Plain),\n    Tagged(Tagged),\n}\n\nimpl Report for Selected {\n    type Output = String;\n\n    fn render(&self) -> String {\n        match self {\n            Self::Plain(value) => value.render(),\n            Self::Tagged(value) => value.render(),\n        }\n    }\n}\n\nfn choose(tagged: bool) -> impl Report<Output = String> {\n    if tagged {\n        Selected::Tagged(Tagged(2))\n    } else {\n        Selected::Plain(Plain(2))\n    }\n}\n\nfn call_once<F>(action: F) -> String\nwhere\n    F: FnOnce() -> String,\n{\n    action()\n}\n\nfn main() {\n    let plain = Plain(2);\n    let tagged = Tagged(2);\n\n    assert_eq!(static_render(&plain), \"records=2\");\n    assert_eq!(static_render(&tagged), \"[records:2]\");\n\n    let reports: [&dyn Report<Output = String>; 2] = [&plain, &tagged];\n    assert_eq!(dynamic_render(reports[0]), \"records=2\");\n    assert_eq!(dynamic_render(reports[1]), \"[records:2]\");\n\n    assert_eq!(choose(false).render(), \"records=2\");\n    assert_eq!(choose(true).render(), \"[records:2]\");\n\n    let text = String::from(\"done\");\n    let consume = move || text;\n    assert_eq!(call_once(consume), \"done\");\n\n    println!(\"dispatch checks passed\");\n}\n\n// Ожидаемый вывод:\n// dispatch checks passed", "explanation": "Трейт с ассоциированным типом: статический и динамический вызовы плюс потребляющее замыкание."},
      {"title": "Две ветви одного impl Trait", "code": "fn bad_choose(tagged: bool) -> impl Report<Output = String> {\n    if tagged {\n        Tagged(2)\n    } else {\n        Plain(2)\n    }\n}", "explanation": "E0308: непрозрачный тип — один конкретный тип, а не контейнер реализаций."},
    ],
  },
  {
    heading: "Часть 8. Коллекции, строки и ленивые вычисления",
    blocks: [
      {"kind": "p", "text": "**8.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Анализатор уже читает записи; теперь нужны нормализация слов и частотный словарь. Здесь легко перепутать владение с обходом, символ с байтом, а построение вычисления — с его выполнением. Особенно неприятен код, который собирается, но ожидаемый счётчик так и остаётся нулевым."},
      {"kind": "p", "text": "**8.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**`Vec<T>`** владеет последовательным буфером: длина учитывает инициализированные элементы, ёмкость — сколько элементов помещается без роста. У **`String`** аналогичные показатели измеряются в байтах; его содержимое обязано быть UTF-8. Рост может потребовать нового размещения, но конкретный коэффициент роста не является договором API."},
      {"kind": "p", "text": "**`HashMap<K, V>`** владеет ключами и значениями в хеш-таблице. Его длина — число пар, ёмкость — число пар, которые можно вместить без перераспределения; это не обещание устройства внутреннего буфера. Рост способен перестроить таблицу. Порядок обхода не определён, поэтому для воспроизводимого отчёта результат сортируют."},
      {"kind": "p", "text": "Трейт **`Iterator`** имеет ассоциированный тип **`Item`** и метод `next`, возвращающий `Option<Self::Item>`. Связь с частью 7 практическая: типы адаптеров сохраняют конкретные типы предыдущего итератора и замыкания. `map` хранит их, а вызывает преобразование, когда у него запрашивают следующий элемент. Ленивость определяется этим протоколом, не самим наличием ассоциированного типа."},
      {"kind": "p", "text": "**Адаптеры** строят следующий итератор; **потребители** запрашивают элементы: `collect`, `for_each`, `fold`, а также цикл `for`. Такая композиция обычно не требует промежуточных коллекций и допускает специализацию, но работа замыкания, выделения при `collect` и прочие вычисления не становятся бесплатными."},
      {"kind": "p", "text": "**8.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Здесь сначала сохраняются номера записей для каждого слова: анализатору это пригодится для объяснения результатов. Затем группы превращаются в частоты и собираются через `collect` в новый `HashMap`."},
      {"kind": "p", "text": "Повторяющийся номер записи означает несколько вхождений, а не несколько документов. Если нужны только частоты, экономнее сразу увеличивать счётчик через `entry`, не сохраняя номера."},
      {"kind": "p", "text": "Нормализация здесь учебная: разделение по пробелам сохраняет пунктуацию, а `to_lowercase` не выполняет полную Unicode-нормализацию или универсальное сравнение без учёта регистра."},
      {"kind": "p", "text": "**8.4. Ловушка №8: построенный map уже выполнил действие**"},
      {"kind": "p", "text": "Этот код **компилируется с предупреждением**, но ожидание побочного эффекта ошибочно:"},
      {"kind": "p", "text": "Созданное состояние сразу отбрасывается; вызова `next` нет, поэтому тело замыкания не исполняется. Для действий явно потребляйте обход:"},
      {"kind": "p", "text": "Для результатов используйте `collect`, как в примере «Счётчик ленивости и частотный словарь». Учтите границу: `take(1)` или поиск с ранним выходом не обязаны запросить все исходные элементы."},
      {"kind": "p", "text": "**8.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["`vec.iter()` — Элементы `&T`; коллекция остаётся владельцем", "`vec.iter_mut()` — Элементы `&mut T`; эксклюзивное заимствование коллекции на время доступа", "`vec.into_iter()` — Элементы `T`; владеющий `Vec` потребляется", "`map.entry(key)` — Вставка или обновление через один поиск записи", "`text.get(range)` — Проверяемый байтовый диапазон; `None` при неверных границах", "`chars()` — Обход Unicode scalar values, не пользовательских символов"]},
      {"kind": "p", "text": "В UTF-8 различаются **байт**, **Unicode scalar value** — скалярное значение Unicode, представляемое Rust-типом `char`, — и **графемный кластер**, воспринимаемый пользователем как символ. В `\"e\\u{301}\"` три байта, два скалярных значения и один графемный кластер. `len()` считает байты; `get` проверяет границы кодирования, но не границы графем. Сегментация графем требует отдельного средства, отсутствующего в стандартной библиотеке."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Сначала называют владельца и тип `Item`, затем выбирают потребитель. **М2** объясняет перемещение элементов: у `(&vec).into_iter()` элементы заимствованы, в отличие от владеющего `vec.into_iter()`. **М4** напоминает, что договор итератора обещает способ получения элементов, а не немедленное выполнение всей цепочки."},
    ],
    examples: [
      {"title": "Счётчик ленивости и частотный словарь", "code": "use std::collections::HashMap;\n\nfn main() {\n    let items = [1, 2, 3];\n    let mut calls = 0;\n\n    {\n        let _pending = items.iter().map(|value| {\n            calls += 1;\n            value * 2\n        });\n        // Итератор создан, но ни один элемент не запрошен.\n    }\n    assert_eq!(calls, 0);\n\n    let doubled: Vec<_> = items\n        .iter()\n        .map(|value| {\n            calls += 1;\n            value * 2\n        })\n        .collect();\n\n    assert_eq!(calls, 3);\n    assert_eq!(doubled, [2, 4, 6]);\n\n    let records = [\"Rust rust\", \"Borrow rust\"];\n    let mut occurrences: HashMap<String, Vec<usize>> = HashMap::new();\n\n    for (record_id, line) in records.iter().enumerate() {\n        for word in line.split_whitespace().map(str::to_lowercase) {\n            occurrences.entry(word).or_default().push(record_id);\n        }\n    }\n\n    assert_eq!(occurrences[\"rust\"], [0, 0, 1]);\n\n    let frequencies: HashMap<String, usize> = occurrences\n        .into_iter()\n        .map(|(word, record_ids)| (word, record_ids.len()))\n        .collect();\n\n    assert_eq!(frequencies[\"rust\"], 3);\n    assert_eq!(frequencies[\"borrow\"], 1);\n\n    let mut ordered: Vec<_> = frequencies.iter().collect();\n    ordered.sort_unstable_by(|a, b| a.0.cmp(b.0));\n    for (word, count) in ordered {\n        println!(\"{word}: {count}\");\n    }\n\n    let text = \"e\\u{301}\"; // e и комбинируемое ударение\n    assert_eq!(text.len(), 3);\n    assert_eq!(text.chars().count(), 2);\n    assert_eq!(text.get(0..1), Some(\"e\"));\n    assert_eq!(text.get(0..2), None);\n}\n\n// Ожидаемый вывод:\n// borrow: 1\n// rust: 3", "explanation": "Непотреблённый `map` не исполняет тело; `collect` продвигает обход; байт, скаляр и графема считаются по-разному."},
      {"title": "Непотреблённый map", "code": "fn main() {\n    let items = [1, 2, 3];\n    items.iter().map(|item| println!(\"{item}\"));\n}\n\n// Ожидаемый вывод: отсутствует.\n// Компилятор предупреждает о неиспользованном ленивом Map.", "explanation": "Компилируется с предупреждением; побочных действий нет."},
      {"title": "Потребление обхода", "code": "fn main() {\n    let items = [1, 2, 3];\n    items.iter().for_each(|item| println!(\"{item}\"));\n}\n\n// Ожидаемый вывод:\n// 1\n// 2\n// 3", "explanation": "`for_each` явно запрашивает элементы и исполняет замыкание."},
    ],
  },
  {
    heading: "Часть 9. Ошибки как часть интерфейса",
    blocks: [
      {"kind": "p", "text": "**9.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Анализатор получает предел обработки из аргумента командной строки и читает запись из файла. Аргумент может отсутствовать, число — оказаться неправильным, файл — быть недоступным, а его содержимое — нарушать формат. Если все эти ситуации завершаются одинаковым аварийным сообщением, пользователю трудно исправить ввод, а вызывающему коду — выбрать дальнейшее действие."},
      {"kind": "p", "text": "**Ошибка входит в интерфейс**, когда от её разновидности зависит поведение потребителя. Здесь действует **Ментальная модель №4: тип как обязательство**: сигнатура обещает не только успешный результат, но и определённый способ сообщить о неуспехе. Это обещание не означает, что функция вообще не может паниковать: такие свойства приходится рассматривать отдельно."},
      {"kind": "p", "text": "**9.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**`Option<T>`** различает наличие значения и его отсутствие: `Some(T)` или `None`. **`Result<T, E>`** различает успех и отказ с причиной: `Ok(T)` или `Err(E)`. Отсутствие необязательного параметра нормально представлять через `Option`; отсутствие обязательного параметра на границе конфигурации можно преобразовать в конкретную ошибку."},
      {"kind": "p", "text": "Оператор **`match`** заставляет рассмотреть варианты перечисления. Оператор **`?`** сокращает распространённый путь: извлечь успешное значение либо досрочно выйти из функции. Для обычного `Result` ошибка при необходимости преобразуется в выходной тип через `From`. Это не журналирование и не восстановление: решение передаётся вызывающему."},
      {"kind": "p", "text": "Выделенное перечисление ошибок сохраняет различия между причинами. Трейт **`Display`** задаёт понятное представление для пользователя, а **`std::error::Error`** позволяет предоставить исходную ошибку через `source`. Так текст остаётся удобным для человека, а структура — для программы."},
      {"kind": "p", "text": "**Паника** относится к другой оси поведения. При **unwinding**, раскрутке стека, выполняются деструкторы покидаемых кадров; при настройке `panic = \"abort\"` процесс прекращается без такой раскрутки. Настройка не меняет смысл `Result` и не превращает панику в обычную ошибку. Ожидаемые отказы ввода лучше возвращать явно; паника уместнее там, где нарушено внутреннее предположение программы."},
      {"kind": "p", "text": "**9.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Как и раньше, замените `src/main.rs`; примеры рассчитаны на Rust 1.90.0 stable, edition 2024."},
      {"kind": "p", "text": "Команды ниже используют POSIX-совместимую оболочку:"},
      {"kind": "p", "text": "Для отсутствующего файла получится вариант `Io`; его текст зависит от ОС. Некорректный UTF-8 также поступит как ошибка `read_to_string`, а не как наш `Format`: классификация определяется границей выбранной операции. Пример проверяет получение конфигурации, но ещё не реализует обработку нескольких записей с указанным пределом."},
      {"kind": "p", "text": "**9.4. Ловушка №9: любой unwrap — краткая обработка ошибки**"},
      {"kind": "p", "text": "Следующий код компилируется, но **намеренно паникует на неправильном пользовательском вводе**:"},
      {"kind": "p", "text": "`unwrap` предполагает наличие `Ok`, но не объясняет, почему оно гарантировано. Отсутствующий аргумент здесь вообще не представлен, а неправильное число превращается в панику вместо отдельной причины отказа."},
      {"kind": "p", "text": "Хороший вариант — `parse_limit` выше: сначала различить отсутствие и присутствие, затем корректное число и ошибку разбора. Решение о завершении принимается в `main`; библиотечная функция ничего не печатает."},
      {"kind": "p", "text": "**9.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["`Option` — Отсутствие нормально и отдельная причина не требуется", "`Result` — Отказ ожидаем, причина полезна вызывающему", "`expect` / `unwrap` — Тест либо установленный инвариант, нарушение которого считается дефектом"]},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Сначала определяют, кто способен принять решение об ошибке. `expect` полезнее безымянного `unwrap`, если сообщение объясняет именно инвариант. Но сообщение не является доказательством. В терминах **М1 — места доказательства**, необоснованный `unwrap` заменяет проверенное рассуждение ставкой на благоприятное выполнение."},
    ],
    examples: [
      {"title": "Различимые ошибки на границе", "code": "use std::{\n    error::Error,\n    fmt,\n    fs,\n    io,\n    num::ParseIntError,\n};\n\nenum AppError {\n    MissingLimit,\n    Configuration(ParseIntError),\n    Io(io::Error),\n    Format(&'static str),\n}\n\nimpl fmt::Display for AppError {\n    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {\n        match self {\n            Self::MissingLimit => write!(f, \"не указан предел обработки\"),\n            Self::Configuration(error) => {\n                write!(f, \"неверный предел обработки: {error}\")\n            }\n            Self::Io(error) => write!(f, \"не удалось прочитать запись: {error}\"),\n            Self::Format(message) => write!(f, \"формат записи: {message}\"),\n        }\n    }\n}\n\n// main, возвращающий Result, выводит Err через Debug.\n// Для этой CLI намеренно используем то же представление, что и Display.\nimpl fmt::Debug for AppError {\n    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {\n        fmt::Display::fmt(self, f)\n    }\n}\n\nimpl Error for AppError {\n    fn source(&self) -> Option<&(dyn Error + 'static)> {\n        match self {\n            Self::Configuration(error) => Some(error),\n            Self::Io(error) => Some(error),\n            Self::MissingLimit | Self::Format(_) => None,\n        }\n    }\n}\n\nimpl From<ParseIntError> for AppError {\n    fn from(error: ParseIntError) -> Self {\n        Self::Configuration(error)\n    }\n}\n\nimpl From<io::Error> for AppError {\n    fn from(error: io::Error) -> Self {\n        Self::Io(error)\n    }\n}\n\nfn parse_limit(input: Option<&str>) -> Result<u16, AppError> {\n    let input = input.ok_or(AppError::MissingLimit)?;\n    Ok(input.parse::<u16>()?)\n}\n\nfn read_record(path: &str) -> Result<String, AppError> {\n    let contents = fs::read_to_string(path)?;\n    let record = contents.trim();\n    let (kind, text) = record\n        .split_once(':')\n        .ok_or(AppError::Format(\"ожидалось kind:text\"))?;\n\n    if kind.trim().is_empty() || text.trim().is_empty() {\n        return Err(AppError::Format(\"поля не должны быть пустыми\"));\n    }\n\n    Ok(record.to_owned())\n}\n\nfn main() -> Result<(), AppError> {\n    let mut args = std::env::args().skip(1);\n    let raw_limit = args.next();\n    let limit = parse_limit(raw_limit.as_deref())?;\n    let path = args.next().unwrap_or_else(|| \"record.txt\".to_owned());\n    let record = read_record(&path)?;\n\n    println!(\"limit={limit}; record={record}\");\n    Ok(())\n}", "explanation": "Перечисление сохраняет причины; `?` преобразует ошибки через `From`."},
      {"title": "Запуск с успешным и отказным входом", "code": "printf 'warning:disk\\n' > record.txt\ncargo run --quiet -- 10 record.txt\n# Ожидаемый stdout:\n# limit=10; record=warning:disk\n\nprintf 'broken\\n' > bad.txt\ncargo run --quiet -- 10 bad.txt\n# Ожидаемый stderr:\n# Error: формат записи: ожидалось kind:text\n# Код завершения ненулевой.", "explanation": "stdout и stderr показывают разные классы исхода; код завершения ненулевой."},
      {"title": "unwrap на пользовательском вводе", "code": "fn main() {\n    let input = \"many\";\n    let _limit = input.parse::<u16>().unwrap();\n}\n// Успешного результата нет: unwrap вызывает панику на Err.", "explanation": "Ожидаемый отказ превращается в панику без отдельной причины."},
    ],
  },
  {
    heading: "Часть 10. Модули, crates и границы публичного API",
    blocks: [
      {"kind": "p", "text": "**10.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Пока весь анализатор находится в `main.rs`, его разбор записи трудно использовать отдельно от запуска программы. Копирование исходника в тест решает проблему лишь внешне: тестирует ещё одну компиляцию файла, а не тот публичный интерфейс, который получит настоящий потребитель."},
      {"kind": "p", "text": "Нужна граница между механизмом разбора и CLI — интерфейсом командной строки. Вынос в библиотеку полезен не числом файлов, а возможностью проверить обещание: потребителю достаточно публичных имён, и он не зависит от внутреннего устройства. **М4** здесь приобретает организационный смысл: публичность создаёт обязательство перед чужим кодом."},
      {"kind": "p", "text": "**10.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Модуль** образует узел дерева имён внутри крейта. Объявление `mod parsing;` включает модуль из файла; оно не импортирует внешнюю библиотеку. **Путь** вроде `crate::normalize` обозначает элемент, а `use` вводит удобное имя в текущей области. Сам по себе `use` не меняет видимость."},
      {"kind": "p", "text": "**Крейт**, crate, — единица компиляции. **Пакет Cargo** может содержать библиотечную цель сборки, binary target — исполняемую цель — и тестовые цели. `src/lib.rs` и `src/main.rs` принадлежат одному пакету, но компилируются как разные крейты. Поэтому «рядом в каталоге» не означает «внутри той же границы доступа»."},
      {"kind": "p", "text": "По умолчанию элементы закрыты; доступ возможен из определяющего модуля и его потомков. **`pub(crate)`** расширяет видимость на текущий крейт, **`pub`** позволяет открыть элемент внешнему коду, если существует доступный путь. **Реэкспорт** `pub use` предоставляет публичный путь, даже когда модуль реализации остаётся закрытым."},
      {"kind": "p", "text": "Интеграционный тест из `tests/` — отдельный крейт. Это ограничение полезно: тест не сможет незаметно опереться на внутренний помощник. При этом модульные тесты внутри библиотеки подходят для проверки закрытых деталей."},
      {"kind": "p", "text": "**10.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "В пакете `record_analyzer` создайте следующие файлы. В `Cargo.toml` достаточно прежнего имени пакета и `edition = \"2024\"`; Cargo обнаружит стандартные цели автоматически. Прежний `main.rs` заменяется."},
      {"kind": "p", "text": "**`src/lib.rs`:**"},
      {"kind": "p", "text": "**`src/parsing.rs`:**"},
      {"kind": "p", "text": "**`src/main.rs`:**"},
      {"kind": "p", "text": "**`tests/record_analyzer.rs`:**"},
      {"kind": "p", "text": "Ни CLI, ни тест не объявляют `mod parsing` повторно. Они используют библиотечный крейт по имени `record_analyzer`. Библиотека возвращает заимствованный результат: вызывающий сохраняет исходную строку, как требует договор из части 6. `Box<dyn Error>` на границе CLI — владеющее объединение разных ошибок; его механизм разберём в следующей части."},
      {"kind": "p", "text": "**10.4. Ловушка №10: соседний target видит pub(crate)**"},
      {"kind": "p", "text": "Добавьте в интеграционный тест следующий импорт — **этот код не компилируется**:"},
      {"kind": "p", "text": "Существенная часть диагностики:"},
      {"kind": "p", "text": "Предположение ошибочно потому, что `crate` означает библиотечную единицу компиляции, а не весь пакет. Даже `src/main.rs` не получает этот доступ автоматически."},
      {"kind": "p", "text": "Не исправляйте ошибку механическим `pub` у каждого помощника. Для внешней проверки здесь достаточно `parse_record`; реализацию `normalize` при необходимости проверяют модульным тестом внутри библиотеки."},
      {"kind": "p", "text": "**10.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Закрытый элемент — Деталь реализации модуля", "`pub(crate)` — Общая внутренняя операция одного крейта", "`pub` / публичный реэкспорт — Поддерживаемый вход для внешнего потребителя"]},
      {"kind": "p", "text": "**Workspace**, рабочее пространство Cargo, объединяет пакеты и обычно общий lock-файл, но не отменяет границы крейтов. **SemVer**, семантическое версионирование, связывает изменение версии с совместимостью API; публичные поля, варианты перечислений и ограничения типов тоже участвуют в этом договоре."},
      {"kind": "p", "text": "**Cargo features**, включаемые возможности пакета, проектируют аддитивными: включение добавляет поведение или API, поскольку запросы зависимостей могут объединяться. Взаимоисключающие режимы требуют отдельного проектирования."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Открывают минимальный достаточный интерфейс, а не всё, что сегодня удобно тестировать. Модуль можно перестроить относительно свободно; публичное обещание уже принадлежит и вашим потребителям."},
    ],
    examples: [
      {"title": "Публичная граница библиотеки", "code": "mod parsing;\n\npub use parsing::{ParseError, Record, parse_record};\n\npub(crate) fn normalize(input: &str) -> &str {\n    input.trim()\n}", "explanation": "Реэкспорт открывает путь, `pub(crate)` остаётся внутренним."},
      {"title": "Разбор записи как модуль библиотеки", "code": "use std::{error::Error, fmt};\n\n#[derive(Debug, PartialEq, Eq)]\npub struct Record<'a> {\n    kind: &'a str,\n    text: &'a str,\n}\n\nimpl<'a> Record<'a> {\n    pub fn kind(&self) -> &'a str {\n        self.kind\n    }\n\n    pub fn text(&self) -> &'a str {\n        self.text\n    }\n}\n\n#[derive(Debug, PartialEq, Eq)]\npub enum ParseError {\n    MissingSeparator,\n    EmptyField,\n}\n\nimpl fmt::Display for ParseError {\n    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {\n        match self {\n            Self::MissingSeparator => write!(f, \"ожидалось kind:text\"),\n            Self::EmptyField => write!(f, \"поля не должны быть пустыми\"),\n        }\n    }\n}\n\nimpl Error for ParseError {}\n\npub fn parse_record(input: &str) -> Result<Record<'_>, ParseError> {\n    let (kind, text) = input\n        .split_once(':')\n        .ok_or(ParseError::MissingSeparator)?;\n\n    let kind = crate::normalize(kind);\n    let text = crate::normalize(text);\n\n    if kind.is_empty() || text.is_empty() {\n        return Err(ParseError::EmptyField);\n    }\n\n    Ok(Record { kind, text })\n}", "explanation": "Заимствованный результат требует живого входа — договор части 6."},
      {"title": "CLI через публичный API", "code": "use record_analyzer::parse_record;\n\nfn main() -> Result<(), Box<dyn std::error::Error>> {\n    let input = std::env::args()\n        .nth(1)\n        .unwrap_or_else(|| \"warning:disk\".to_owned());\n\n    let record = parse_record(&input)?;\n    println!(\"{} => {}\", record.kind(), record.text());\n    Ok(())\n}", "explanation": "Бинарный крейт видит только `pub` элементы библиотеки."},
      {"title": "Интеграционный тест как отдельный крейт", "code": "use record_analyzer::{ParseError, parse_record};\n\n#[test]\nfn parses_through_public_api() {\n    let record = parse_record(\" warning : disk \").expect(\"валидная запись\");\n    assert_eq!(record.kind(), \"warning\");\n    assert_eq!(record.text(), \"disk\");\n}\n\n#[test]\nfn distinguishes_format_errors() {\n    assert_eq!(\n        parse_record(\"broken\"),\n        Err(ParseError::MissingSeparator),\n    );\n    assert_eq!(\n        parse_record(\"warning: \"),\n        Err(ParseError::EmptyField),\n    );\n}", "explanation": "Тест не переобъявляет модули и использует публичные имена."},
      {"title": "Запуск и тесты библиотеки", "code": "cargo run --quiet -- 'info:ready'\n# Ожидаемый stdout:\n# info => ready\n\ncargo test\n# Ожидается успешное завершение.\n# Оба интеграционных теста проходят; порядок их отчёта не существенен.", "explanation": "Оба интеграционных теста проходят через публичную границу."},
      {"title": "Импорт pub(crate) извне", "code": "use record_analyzer::normalize;", "explanation": "Не компилируется: крейт — не весь пакет."},
      {"title": "Диагностика E0603", "code": "error[E0603]: function `normalize` is private", "explanation": "Приватность сформулирована в тексте ошибки."},
    ],
  },
  {
    heading: "Часть 11. Умные указатели и изменяемость через общий доступ",
    blocks: [
      {"kind": "p", "text": "**11.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "У анализатора появляются два компонента, которым нужно видеть одно состояние: накопитель статистики и представление отчёта. Копия состояния не подходит — изменения должны быть общими. Но две обычные независимые изменяемые ссылки нарушили бы разрешения из части 5."},
      {"kind": "p", "text": "Нужно разделить вопросы: где хранится значение, кто удерживает его живым и кто сейчас вправе его менять. **Умные указатели** отвечают на разные части этой задачи; универсального «разрешить всё» среди них нет. Общий доступ означает связь с одним состоянием, а не появление двух независимо освобождаемых объектов."},
      {"kind": "p", "text": "**11.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**`Box<T>`** владеет значением в выделенном размещении в куче; для типов нулевого размера фактическое выделение не требуется. У бокса один владелец, а при уничтожении выполняется уничтожение содержимого и освобождение размещения. Обещанная в части 7 альтернатива **`Box<dyn Trait>`** добавляет владение к динамическому интерфейсу: конкретный тип скрыт, вызовы доступны через таблицу методов."},
      {"kind": "p", "text": "**`Rc<T>`**, reference counting — подсчёт ссылок, предоставляет разделяемое владение внутри одного потока. Клонирование `Rc` увеличивает счётчик сильных ссылок, а не клонирует `T`. Последняя сильная ссылка уничтожает значение."},
      {"kind": "p", "text": "**`Weak<T>`** — слабая ссылка: она не удерживает значение живым. `upgrade` возвращает `Some(Rc<T>)`, пока сильный владелец существует, и `None` после уничтожения значения. Управляющее размещение сохраняется, пока нужны слабые ссылки. Цикл сильных `Rc` сам не освободится: для каждого узла останется сильный владелец внутри цикла. Невладеющее обратное ребро обычно выражают через `Weak`."},
      {"kind": "p", "text": "**`RefCell<T>`** разрешает запрашивать доступ через `&self`, но проверяет заимствования во время выполнения. Его состояние учитывает либо совместные чтения, либо одно эксклюзивное заимствование. Возвращаемые **guards**, объекты-стражи `Ref` и `RefMut`, удерживают разрешение до своего уничтожения."},
      {"kind": "p", "text": "**`Cell<T>`** решает более узкую задачу через замену значения и копирование для `T: Copy`; обычных ссылок внутрь через совместный доступ он не выдаёт и счётчика заимствований не использует. Основа внутренней изменяемости — **`UnsafeCell<T>`**: она снимает обычное предположение о неизменности содержимого за совместной ссылкой, но не отменяет остальные требования корректного доступа."},
      {"kind": "p", "text": "Трейт **`Deref`** определяет разыменование обёртки, а **`Drop`** — действие при её уничтожении. Удобный синтаксис не стирает механизм: получить изменяемый доступ к содержимому `Rc<RefCell<T>>` всё равно нужно через `borrow_mut`."},
      {"kind": "p", "text": "**11.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Библиотеку и тесты части 10 оставьте; замените только `src/main.rs`."},
      {"kind": "p", "text": "Временный `Rc`, созданный успешным `upgrade`, исчезает после проверки. Поэтому он не удерживает состояние до конца `main`. Объект отчёта хранит самостоятельное число, а не ссылку на `State`."},
      {"kind": "p", "text": "**11.4. Ловушка №11: RefCell отменяет правила заимствования**"},
      {"kind": "p", "text": "**Незапускаемая демонстрация паники:** фрагмент компилируется, но его не следует добавлять к успешному примеру."},
      {"kind": "p", "text": "Предположение «проверка перенесена — ограничение исчезло» неверно. `borrow_mut` видит активное совместное заимствование и вызывает панику. Для стража важен момент `Drop`, а не только последнее чтение через него."},
      {"kind": "p", "text": "Ограничьте область стража, как в примере «Наблюдаемое освобождение общего состояния», либо используйте `try_borrow_mut`, если конфликт — допустимый исход операции. Последний возвращает `Result`, позволяя отложить изменение или сообщить об отказе."},
      {"kind": "p", "text": "**11.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Единственный владелец, размещение в куче — `Box<T>`", "Несколько владельцев в одном потоке — `Rc<T>`", "Невладеющая связь, разрыв цикла — `Weak<T>`", "Замена небольшого значения через общий доступ — `Cell<T>`", "Временные ссылки для чтения и изменения — `RefCell<T>` с динамической проверкой"]},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "По **М2** сначала рисуют граф владения, затем по **М3** — длительность разрешений. В терминах **М1** переход от статической проверки к динамической переносит ответственность на выполнение; он не отменяет правила. Если области стражей становятся запутанными, часто полезнее изменить распределение состояния, чем добавлять ещё одну обёртку."},
    ],
    examples: [
      {"title": "Наблюдаемое освобождение общего состояния", "code": "use std::{\n    cell::RefCell,\n    rc::Rc,\n};\n\nstruct State {\n    records: u32,\n}\n\nimpl Drop for State {\n    fn drop(&mut self) {\n        println!(\"released: records={}\", self.records);\n    }\n}\n\ntrait Report {\n    fn render(&self) -> String;\n}\n\nstruct Summary(u32);\n\nimpl Report for Summary {\n    fn render(&self) -> String {\n        format!(\"records={}\", self.0)\n    }\n}\n\nfn make_report(records: u32) -> Box<dyn Report> {\n    Box::new(Summary(records))\n}\n\nfn main() {\n    let first = Rc::new(RefCell::new(State { records: 0 }));\n    let second = Rc::clone(&first);\n    let observer = Rc::downgrade(&first);\n\n    assert_eq!(Rc::strong_count(&first), 2);\n\n    {\n        let mut state = first.borrow_mut();\n        state.records += 1;\n    }\n\n    assert_eq!(second.borrow().records, 1);\n\n    let report = make_report(second.borrow().records);\n    assert_eq!(report.render(), \"records=1\");\n\n    drop(first);\n    assert_eq!(Rc::strong_count(&second), 1);\n    assert!(observer.upgrade().is_some());\n\n    println!(\"before last owner\");\n    drop(second); // Последний сильный владелец уничтожает State.\n\n    assert!(observer.upgrade().is_none());\n    println!(\"weak cannot upgrade\");\n}\n\n// Ожидаемый вывод:\n// before last owner\n// released: records=1\n// weak cannot upgrade", "explanation": "Счётчики, `Weak` и собственный `Drop` делают граф владения видимым."},
      {"title": "Конфликт заимствований в RefCell", "code": "let state = RefCell::new(0_u32);\nlet reading = state.borrow();\nlet mut writing = state.borrow_mut(); // Паника: чтение ещё активно.\n*writing += 1;\nprintln!(\"{}\", *reading);", "explanation": "Незапускаемая демонстрация: паника при живом совместном заимствовании."},
    ],
  },
  {
    heading: "Часть 12. Потоки: перенос доказательств между исполнителями",
    blocks: [
      {"kind": "p", "text": "**12.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Непересекающиеся части записей можно считать параллельно. Но прежнего рассуждения «эти два вызова идут один за другим» больше нет: исполнители работают независимо. Нужно доказать, что переданные данные пригодны для другого потока, заимствованные источники не исчезнут, а совместное изменение защищено."},
      {"kind": "p", "text": "Самый простой проект часто не делит изменяемое состояние вообще: каждый работник получает свою часть работы и передаёт результат. Общий счётчик тоже возможен, но требует другого договора доступа. Выбор влияет не только на синтаксис, но и на ожидания, удержание ресурсов и завершение программы."},
      {"kind": "p", "text": "**12.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**`thread::spawn`** принимает замыкание во владение и требует, в частности, `Send + 'static`. Ключевое слово `move` переносит захваченные значения в замыкание; перенос самой ссылки не продлевает жизнь её источника. **Scoped threads**, потоки с ограниченной областью через `thread::scope`, могут заимствовать локальные данные: выход из области ожидает завершения её потоков."},
      {"kind": "p", "text": "**`Send`** — маркерный трейт, разрешающий перенос значения между потоками. **`Sync`** выражает пригодность совместного доступа: `T: Sync` означает, что `&T` можно передать другому потоку. Компилятор выводит эти свойства по составу типов; ручные реализации требуют `unsafe` и отдельного доказательства, здесь они не нужны."},
      {"kind": "p", "text": "**`Arc<T>`** использует атомарный подсчёт ссылок и обеспечивает потокобезопасное разделяемое владение. **`Mutex<T>`**, взаимное исключение, предоставляет эксклюзивный доступ через страж блокировки. Уничтожение стража освобождает блокировку. Если поток паникует, удерживая её, mutex обычно становится **poisoned**, «отравленным»: следующее получение сообщает, что логический инвариант данных мог быть нарушен. Это предупредительный механизм, а не доказательство повреждения или полноценная гарантия его обнаружения."},
      {"kind": "p", "text": "**Канал `mpsc`** передаёт сообщения от нескольких отправителей одному получателю."},
      {"kind": "p", "text": "Вверху каждый работник формирует самостоятельный результат, а агрегатор один изменяет итог. Внизу владельцы удерживают общий ресурс, но право изменять его выдаётся по очереди блокировкой. Канал тоже имеет внутреннюю синхронизацию; различается прежде всего устройство пользовательских данных."},
      {"kind": "p", "text": "**12.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Замените `src/main.rs`:"},
      {"kind": "p", "text": "Инвариант: каждая запись принадлежит ровно одному фрагменту, каждый работник отправляет ровно одну сумму, поэтому сумма частичных результатов равна последовательному подсчёту. Для этого маленького набора переполнения `u32` нет; для произвольных объёмов нужны подходящий тип и политика переполнения. После уничтожения всех отправителей обход получателя заканчивается; область также гарантирует завершение работников."},
      {"kind": "p", "text": "Второй, самостоятельный вариант использует общий счётчик:"},
      {"kind": "p", "text": "Каждое увеличение выполняется эксклюзивно, а `join` дожидается всех работников. Поэтому порядок не влияет на итог: потерянных обновлений нет. `expect` здесь фиксирует предположения демонстрации, а не универсальную стратегию восстановления после паники."},
      {"kind": "p", "text": "**12.4. Ловушка №12: Arc автоматически делает содержимое потокобезопасным**"},
      {"kind": "p", "text": "**Этот код не компилируется:**"},
      {"kind": "p", "text": "Ошибка `E0277` объясняет, что `RefCell<u32>` нельзя безопасно разделять между потоками: он не реализует `Sync`. Его состояние заимствования не синхронизировано между исполнителями. Для передачи `Arc<T>` нужны соответствующие свойства содержимого; атомарный счётчик владельцев защищает не само `T`."},
      {"kind": "p", "text": "Исправление — `Arc<Mutex<u32>>`, как в примере «Общий счётчик под блокировкой», либо перенос владения и передача результатов без общего изменяемого объекта."},
      {"kind": "p", "text": "**12.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Владение работой, результат через `join` — Передача данных; ожидание завершения работника", "Канал сообщений — Синхронизация, очередь; у неограниченного канала возможен рост памяти", "Общий `Mutex` — Конкуренция за блокировку, риск взаимного ожидания", "Scoped borrowing — Источник остаётся доступным до завершения области; область ждёт потоки"]},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "**М2** показывает владельцев, **М3** — эксклюзивность доступа, **М4** — требования `Send` и `Sync` на границе потока. Но отсутствие **data race**, несинхронизированного конфликтующего доступа к памяти, не доказывает отсутствие логических гонок. **Safe Rust не спасает от deadlock**, взаимной блокировки: два потока могут безопасно ждать друг друга бесконечно. Порядок блокировок и протокол завершения каналов проектируют отдельно."},
    ],
    examples: [
      {"title": "Сообщения против общего защищённого ресурса", "code": "ownership + messages:\nworker A [partial A] ----\\\n                         -> channel -> main [aggregate]\nworker B [partial B] ----/\n\nshared protected state:\nworker A ----\\\n              -> Arc -> Mutex [counter]\nworker B ----/", "explanation": "Канал передаёт результаты; `Arc<Mutex>` выдаёт право изменения по очереди."},
      {"title": "Совместная работа потоков", "code": "use std::{\n    sync::mpsc,\n    thread,\n};\n\nfn main() {\n    let records = [\n        \"rust rust\",\n        \"borrow\",\n        \"safe rust\",\n        \"\",\n    ];\n\n    let expected: u32 = records\n        .iter()\n        .map(|line| line.split_whitespace().count() as u32)\n        .sum();\n\n    let (sender, receiver) = mpsc::channel::<u32>();\n\n    let total = thread::scope(|scope| {\n        for chunk in records.chunks(2) {\n            let sender = sender.clone();\n\n            scope.spawn(move || {\n                let partial: u32 = chunk\n                    .iter()\n                    .map(|line| line.split_whitespace().count() as u32)\n                    .sum();\n\n                sender.send(partial).expect(\"получатель ещё работает\");\n            });\n        }\n\n        // Иначе этот отправитель удерживал бы канал открытым.\n        drop(sender);\n\n        receiver.into_iter().sum::<u32>()\n    });\n\n    assert_eq!(total, expected);\n    assert_eq!(total, 5);\n    println!(\"total={total}\");\n}\n\n// Ожидаемый stdout:\n// total=5\n// Порядок получения частичных результатов не фиксирован:\n// допустимы 3, 2 и 2, 3.", "explanation": "Сумма частичных результатов равна последовательному подсчёту; порядок приёма не фиксирован."},
      {"title": "Общий счётчик под блокировкой", "code": "use std::{\n    sync::{Arc, Mutex},\n    thread,\n};\n\nfn main() {\n    let counter = Arc::new(Mutex::new(0_u32));\n    let mut workers = Vec::new();\n\n    for _ in 0..4 {\n        let counter = Arc::clone(&counter);\n\n        workers.push(thread::spawn(move || {\n            for _ in 0..1_000 {\n                let mut guard = counter.lock().expect(\"нет паники под блокировкой\");\n                *guard += 1;\n            }\n        }));\n    }\n\n    for worker in workers {\n        worker.join().expect(\"работник завершился без паники\");\n    }\n\n    let result = *counter.lock().expect(\"счётчик не отравлен\");\n    assert_eq!(result, 4_000);\n    println!(\"counter={result}\");\n}\n\n// Ожидаемый stdout:\n// counter=4000\n// Порядок отдельных увеличений не определён.", "explanation": "Итог 4000 гарантирован; порядок отдельных увеличений не определён."},
      {"title": "Arc над RefCell", "code": "use std::{cell::RefCell, sync::Arc, thread};\n\nfn main() {\n    let counter = Arc::new(RefCell::new(0_u32));\n    let shared = Arc::clone(&counter);\n\n    thread::spawn(move || {\n        *shared.borrow_mut() += 1;\n    });\n}", "explanation": "E0277: `RefCell` не `Sync` — счётчик владельцев не защищает содержимое."},
    ],
  },
  {
    heading: "Часть 13. Async: владение состоянием между точками приостановки",
    blocks: [
      {"kind": "p", "text": "**13.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "В потоке легко увидеть, где выполнение остановилось. В асинхронной функции остановка выглядит как `.await`, но переменные вокруг неё никуда не исчезают. Соединение, буфер или блокировка могут остаться удерживаемыми на всё время ожидания."},
      {"kind": "p", "text": "Вызов **`async fn`**, как и создание **`async`-блока**, производит **`Future`** — значение, представляющее ещё не завершённое вычисление. Тело не выполняется при одном лишь создании future: ему нужен **опрос**, `poll`. При этом аргументы функции вычисляются обычным образом, а захваты блока могут происходить сразу."},
      {"kind": "p", "text": "**13.2. Как это устроено внутри**"},
      {"kind": "p", "text": "`.await` — потенциальная точка приостановки: уже готовая операция может завершиться без остановки. Если ожидание необходимо, нужные для продолжения значения сохраняются внутри future. Опрос продвигает вычисление; уведомление о готовности, **wake-up**, позволяет исполнителю запланировать новый опрос. Устройство очередей runtime здесь вне маршрута."},
      {"kind": "p", "text": "Из сохранённых полей следуют ограничения. Future, удерживающий `!Send`-значение, нельзя произвольно переносить между потоками. `tokio::spawn` требует `Send + 'static` и от future, и от его результата. `'static` здесь означает отсутствие заимствований с недостаточным временем жизни, а не обязанность существовать вечно. Важны также первоначальные захваты, ещё до первого опроса."},
      {"kind": "p", "text": "**`Pin`** нужен потому, что состояние future может содержать ссылки на собственные части. Для таких `!Unpin`-значений обещание закрепления защищает адрес при опросе и дальнейшем существовании вплоть до уничтожения. Это не запрет перемещать любой future сразу после создания: например, перемещение `Pin<Box<_>>` не перемещает содержимое выделения. `Pin` не обеспечивает справедливость планирования и не устраняет логические гонки."},
      {"kind": "p", "text": "**13.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Первая внешняя зависимость:"},
      {"kind": "p", "text": "Фактическая разрешённая версия берётся из `Cargo.lock`. Возможность `fs` не добавлена: блокирующее файловое чтение передадим в `spawn_blocking`. Следующий самостоятельный пример можно сохранить как `examples/async_phases.rs`."},
      {"kind": "p", "text": "При успешных файловых операциях stdout фиксирован:"},
      {"kind": "p", "text": "Порядок завершения чтений не фиксирован. Печать выполняется только после обеих проверок, поэтому порядок не влияет на строку. `join!` конкурентно опрашивает ветви, но сам по себе не создаёт два потока."},
      {"kind": "p", "text": "**13.4. Ловушка №13: «guard можно незаметно пронести через await»**"},
      {"kind": "p", "text": "**Не компилируется:**"},
      {"kind": "p", "text": "Guard нужен после ожидания, поэтому входит в сохранённое состояние. `std::sync::MutexGuard` не реализует `Send`: исполнитель не может получить право переносить такое состояние между потоками."},
      {"kind": "p", "text": "Исправление — закончить критическую секцию отдельным блоком:"},
      {"kind": "p", "text": "Теперь печатается снимок, не обязательно актуальное состояние счётчика. Это уже вопрос логики."},
      {"kind": "p", "text": "**Отмена** через уничтожение future запускает деструкторы удерживаемых значений, но не откатывает выполненные внешние эффекты. Уничтожение `JoinHandle` Tokio само по себе не отменяет задачу; уже начавшуюся `spawn_blocking`-операцию нельзя остановить обычным `abort`."},
      {"kind": "p", "text": "**13.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Блокирующая операция — `spawn_blocking`; не блокировать рабочий поток async", "Async-операция — `.await`; готовность обеспечивает соответствующий механизм уведомлений", "CPU-задача — Ограничивать параллелизм; async не ускоряет вычисления автоматически", "Локальный future — Может заимствовать локальные данные и быть `!Send`, если контекст допускает", "`tokio::spawn` — Future и результат: `Send + 'static`"]},
      {"kind": "p", "text": "**Ментальная модель №5: приостановка как граница удерживаемого состояния.** Перечислите, что живёт через `.await`, — предскажете удержание ресурсов и многие причины `!Send`. Модель не предсказывает справедливость runtime или порядок выполнения. М2 и М3 продолжаются здесь вопросами о владельце состояния и моменте освобождения ресурсов."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Я сначала проверяю, что переживает ожидание, и только затем выбираю способ запуска задачи."},
    ],
    examples: [
      {"title": "Первая внешняя зависимость", "code": "cargo add tokio --features rt-multi-thread,macros,time", "explanation": "Features заданы явно; версию фиксирует `Cargo.lock`."},
      {"title": "Две конкурентные фазы чтения", "code": "use std::{\n    cell::Cell,\n    error::Error,\n    fs,\n    time::{SystemTime, UNIX_EPOCH},\n};\n\n#[tokio::main]\nasync fn main() -> Result<(), Box<dyn Error>> {\n    let counter = Cell::new(0);\n    let never_polled = async {\n        counter.set(counter.get() + 1);\n    };\n    drop(never_polled);\n    assert_eq!(counter.get(), 0);\n\n    let stamp = SystemTime::now().duration_since(UNIX_EPOCH)?.as_nanos();\n    let dir = std::env::temp_dir()\n        .join(format!(\"record-analyzer-{}-{stamp}\", std::process::id()));\n    fs::create_dir(&dir)?;\n\n    let first_path = dir.join(\"first.txt\");\n    let second_path = dir.join(\"second.txt\");\n    fs::write(&first_path, \"first\\n\")?;\n    fs::write(&second_path, \"second\\n\")?;\n\n    let first_phase = async move {\n        tokio::task::spawn_blocking(move || fs::read_to_string(first_path)).await\n    };\n    let second_phase = async move {\n        tokio::task::spawn_blocking(move || fs::read_to_string(second_path)).await\n    };\n\n    let (first, second) = tokio::join!(first_phase, second_phase);\n    assert_eq!(first??, \"first\\n\");\n    assert_eq!(second??, \"second\\n\");\n\n    fs::remove_dir_all(dir)?;\n    println!(\"Обе фазы проверены; счётчик = {}\", counter.get());\n    Ok(())\n}", "explanation": "Созданный и не опрошенный future не исполняется; `join!` конкурентно опрашивает ветви."},
      {"title": "Вывод двух фаз", "code": "Обе фазы проверены; счётчик = 0", "explanation": "stdout фиксирован, потому что печать идёт после обеих проверок; порядок чтений не фиксирован."},
      {"title": "Guard через await", "code": "use std::sync::{Arc, Mutex};\nuse std::time::Duration;\n\nfn launch(state: Arc<Mutex<u32>>) {\n    tokio::spawn(async move {\n        let guard = state.lock().unwrap();\n        tokio::time::sleep(Duration::from_millis(1)).await;\n        println!(\"{}\", *guard);\n    });\n}", "explanation": "Не компилируется: сохранённый guard делает future не-`Send`."},
      {"title": "Освобождение guard до ожидания", "code": "use std::sync::{Arc, Mutex};\nuse std::time::Duration;\n\nfn launch(state: Arc<Mutex<u32>>) -> tokio::task::JoinHandle<()> {\n    tokio::spawn(async move {\n        let value = {\n            let guard = state.lock().unwrap();\n            *guard\n        };\n        tokio::time::sleep(Duration::from_millis(1)).await;\n        println!(\"{value}\");\n    })\n}", "explanation": "Критическая секция закончена до приостановки; печатается снимок значения."},
    ],
  },
  {
    heading: "Часть 14. Unsafe и FFI: где заканчивается автоматическое доказательство",
    blocks: [
      {"kind": "p", "text": "**14.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Иногда вы знаете больше, чем может установить система типов: индекс проверен другим механизмом, память предоставлена внешней библиотекой, представление структуры согласовано с C. Возникает соблазн написать `unsafe` как разрешение «не мешать»."},
      {"kind": "p", "text": "Это неверная модель. **`unsafe`** не выключает проверку типов или заимствований. Он обозначает место, где часть доказательства безопасности должен предоставить человек. Если убедительного доказательства нет, ключевое слово лишь скрывает проблему от автоматической проверки."},
      {"kind": "p", "text": "Сначала стоит спросить, нужна ли такая граница вообще. Для обращения к элементу среза уже существует безопасный `get`, возвращающий `Option<&T>`."},
      {"kind": "p", "text": "**14.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Сырые указатели** `*const T` и `*mut T` не дают гарантий ссылок `&T` и `&mut T`. Создать сырой указатель часто можно безопасно; его разыменование требует `unsafe`. Проверить только ненулевой адрес недостаточно: нужны допустимое выделение, выравнивание, инициализация, подходящий срок жизни и соблюдение правил доступа."},
      {"kind": "p", "text": "К небезопасным операциям относятся также вызов `unsafe fn`, доступ к `static mut` и реализация `unsafe trait`. Обычное чтение безопасного неизменяемого `static` не требует `unsafe`. Отдельные обязательства существуют у внешних статических объектов и чтения полей `union`."},
      {"kind": "p", "text": "**`unsafe fn` — контракт для вызывающего кода.** Его документация должна объяснять условия безопасности. В edition 2024 небезопасные операции внутри такой функции следует всё равно выделять явными блоками: lint `unsafe_op_in_unsafe_fn` предупреждает об их отсутствии. Для `unsafe impl` доказательство относится к обещаниям всей реализации."},
      {"kind": "p", "text": "**Неопределённое поведение**, UB, означает нарушение требований, на которых язык основывает семантику выполнения. Это не разновидность `Result::Err` и не обещанная паника. Нарушение любого бизнес-правила не становится UB: речь именно об обязательствах безопасности соответствующих операций."},
      {"kind": "p", "text": "**14.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Самостоятельный компилируемый пример:"},
      {"kind": "p", "text": "Комментарий **`// SAFETY:`** объясняет конкретное доказательство, а не сообщает «здесь безопасно». Проверка устанавливает границу индекса; действительность среза обеспечивает входной тип. Для этой задачи предпочтительнее первая функция: она короче и не требует ручного доказательства. Автоматического выигрыша скорости здесь не показано, измерений нет."},
      {"kind": "p", "text": "**14.4. Ловушка №14: «безопасная сигнатура делает небезопасное тело безопасным»**"},
      {"kind": "p", "text": "**Дефектная обёртка; не запускать:**"},
      {"kind": "p", "text": "Фрагмент способен компилироваться, но безопасная публичная функция принимает любой `usize`. В её контракте нет обязанности вызывающего кода проверять индекс. Следовательно, обычный безопасный вызов с пустым срезом и индексом `0` уже может нарушить требования `get_unchecked`. Это **дыра в обоснованности безопасности**, soundness hole, а не просто неудобный API."},
      {"kind": "p", "text": "Исправление — `get` либо собственная проверка всех предусловий внутри обёртки."},
      {"kind": "p", "text": "На **FFI-границе**, интерфейсе с другим языком, добавляются соглашение вызова и представление данных. `#[repr(C)]` задаёт C-совместимые правила размещения соответствующего типа, но не делает произвольное содержимое действительным. В edition 2024 объявления внешних функций помещаются в `unsafe extern \"C\" { ... }`. Владение памятью, допустимость указателей и правила освобождения требуют отдельных договорённостей. Производственную FFI-обёртку и сборку C-библиотеки здесь не рассматриваем."},
      {"kind": "p", "text": "**14.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["`safe fn` — Автор не допускает UB при безопасном использовании API", "`unsafe fn` — Вызывающий устанавливает документированные условия безопасности", "`unsafe block` — Автор доказывает предусловия каждой небезопасной операции", "FFI — Стороны согласуют ABI, данные, владение и время жизни"]},
      {"kind": "p", "text": "М1 теперь означает перенос доказательства к автору обёртки; М3 напоминает о сроках жизни ресурсов, М4 — о честности публичного контракта."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Я предпочитаю доказательство, которое можно проверить рядом с операцией. Если оно зависит от «все вызывающие помнят», безопасная граница выбрана неправильно."},
    ],
    examples: [
      {"title": "Безопасное решение и обоснованный unsafe", "code": "pub fn item<T>(slice: &[T], index: usize) -> Option<&T> {\n    slice.get(index)\n}\n\nstruct CheckedSlice<'a, T> {\n    slice: &'a [T],\n}\n\nimpl<'a, T> CheckedSlice<'a, T> {\n    fn new(slice: &'a [T]) -> Self {\n        Self { slice }\n    }\n\n    fn item(&self, index: usize) -> Option<&T> {\n        if index >= self.slice.len() {\n            return None;\n        }\n\n        // SAFETY: ветка выше установила index < self.slice.len(),\n        // то есть предусловие get_unchecked. self.slice — действительный\n        // разделяемый срез; возвращаемая ссылка ограничена заимствованием self.\n        Some(unsafe { self.slice.get_unchecked(index) })\n    }\n}\n\nfn main() {\n    let values = [10, 20];\n    let checked = CheckedSlice::new(&values);\n\n    assert_eq!(item(&values, 1), Some(&20));\n    assert_eq!(checked.item(1), Some(&20));\n    assert_eq!(checked.item(2), None);\n    assert_eq!(CheckedSlice::<i32>::new(&[]).item(0), None);\n}", "explanation": "Проверка внутри обёртки обеспечивает предусловие; автоматического выигрыша не заявлено."},
      {"title": "Дыра обоснованности", "code": "pub fn broken<T>(slice: &[T], index: usize) -> &T {\n    unsafe { slice.get_unchecked(index) }\n}", "explanation": "Не запускать: безопасная сигнатура не устанавливает предусловия `get_unchecked`."},
    ],
  },
  {
    heading: "Часть 15. Проверка поведения и чтение диагностик",
    blocks: [
      {"kind": "p", "text": "**15.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Парсер может успешно возвращать не то имя, молча заменять число нулём или принимать пустое поле. Единственный `assert!(result.is_ok())` этого не обнаружит. Сначала назовите свойство: точные значения, отказ на недопустимом вводе, сохранение допустимых граничных случаев."},
      {"kind": "p", "text": "Фикстуры ниже проверяют контракт части 10: запись вида `kind:text`, публичные поля `Record { kind: &str, text: &str }` с доступами `kind()` и `text()`, вариант `ParseError::MissingSeparator` при отсутствии разделителя и `ParseError::EmptyField` при пустом поле после удаления внешних пробелов."},
      {"kind": "p", "text": "**15.2. Как это устроено внутри**"},
      {"kind": "p", "text": "**Unit-тесты** размещаются внутри библиотечного crate, обычно в модуле `#[cfg(test)]`, и могут проверять внутренние детали, включая доступные им непубличные элементы. **Интеграционный тест** в `tests/record_analyzer.rs` компилируется как отдельный crate и видит публичный API, а не `pub(crate) normalize`."},
      {"kind": "p", "text": "**Test harness**, исполнитель тестов, обнаруживает тестовые функции и сообщает результаты. Для обычных unit- и integration-тестов нет отдельного процесса на каждую функцию: несколько тестов одного бинарника обычно выполняются в одном процессе, потенциально параллельно. Отдельный тестовый бинарник — другая граница."},
      {"kind": "p", "text": "Поэтому глобальная переменная, общий файл или изменение текущего каталога могут связать якобы независимые проверки. Свежая строка в каждом случае дешевле, чем отладка зависимости от порядка запуска. Ограничение числа тестовых потоков полезно для диагностики, но не исправляет плохую изоляцию."},
      {"kind": "p", "text": "**15.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Добавление в `src/lib.rs`:"},
      {"kind": "p", "text": "Здесь отказ проверяется без конкретного варианта `ParseError`. Если классификация ошибки входит в контракт, добавьте сравнение именно с принятым вариантом: сам факт `Err` её не доказывает."},
      {"kind": "p", "text": "Добавление в `tests/record_analyzer.rs`:"},
      {"kind": "p", "text": "Следующие строки документации добавляются непосредственно перед существующим объявлением `parse_record`, без повторного определения функции:"},
      {"kind": "p", "text": "**Doctest** превращает пример документации в исполняемую проверку. Пометка **`compile_fail`** проверяет, что пример не компилируется, но не устанавливает причину отказа. Опечатка в имени функции тоже могла бы сделать его зелёным; поэтому диагностическое сообщение нужно прочитать."},
      {"kind": "p", "text": "**15.4. Ловушка №15: «один happy-path тест проверяет контракт»**"},
      {"kind": "p", "text": "В примере «Документация как тест» ищите три указателя диагностики: место требования изменяемого доступа — `input.clear()`; происхождение неизменяемого заимствования — `parse_record(&input)`; последующее использование — `record.kind()`. Компилятор запрещает изменение буфера, пока ссылка на его содержимое ещё нужна."},
      {"kind": "p", "text": "Объяснение кода ошибки помогает перейти от строки сообщения к инварианту. Не исправляйте диагностику случайным `clone`: сначала установите, действительно ли вам нужна независимая копия."},
      {"kind": "p", "text": "Противоядие от единственного happy path — точные поля, пустой ввод, неправильная форма, отсутствие разделителя и независимые фикстуры. Ни число тестов, ни процент покрытия сами по себе не показывают, проверен ли нужный контракт."},
      {"kind": "p", "text": "**15.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["Unit — Поведение выбранных внутренних функций на выбранных случаях", "Integration — Доступность и поведение публичного интерфейса извне crate", "Doc — Работоспособность опубликованных примеров", "`compile_fail` — Отказ компиляции данного фрагмента, не точную причину"]},
      {"kind": "p", "text": "Property testing генерирует случаи по свойствам, fuzzing исследует множество входов, Miri выявляет ряд нарушений при интерпретации Rust. Их назначение и требования к инструментам различаются: Miri обычно требует nightly, средства фаззинга — отдельной настройки; библиотеки property testing могут работать на stable. В маршрут они не входят."},
      {"kind": "p", "text": "М1 и М4 сходятся в границе утверждения: прошедшие тесты накапливают уверенность, но не доказывают отсутствие дефектов."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Я спрашиваю не «зелёный ли тест», а «какую правдоподобную ошибку он обязан заметить»."},
    ],
    examples: [
      {"title": "Таблица контракта разбора", "code": "#[cfg(test)]\nmod contract_tests {\n    use super::parse_record;\n\n    #[test]\n    fn record_cases() {\n        let cases = [\n            (\"warning:disk\", Some((\"warning\", \"disk\"))),\n            (\"warningdisk\", None),\n            (\":disk\", None),\n            (\"warning:\", None),\n            (\"\", None),\n            (\"  warning : disk \\n\", Some((\"warning\", \"disk\"))),\n            (\"warning:disk:extra\", Some((\"warning\", \"disk:extra\"))),\n            (\":\", None),\n        ];\n\n        for (input, expected) in cases {\n            match (parse_record(input), expected) {\n                (Ok(record), Some((kind, text))) => {\n                    assert_eq!(record.kind(), kind, \"input={input:?}\");\n                    assert_eq!(record.text(), text, \"input={input:?}\");\n                }\n                (Err(_), None) => {}\n                _ => panic!(\"нарушен контракт для {input:?}\"),\n            }\n        }\n    }\n}", "explanation": "Точные поля, границы и отказные случаи проверены в одном наборе."},
      {"title": "Публичный API в интеграционном тесте", "code": "#[test]\nfn public_api_returns_borrowed_kind_and_text() {\n    let input = String::from(\"beta:12\");\n    let record = record_analyzer::parse_record(&input).unwrap();\n\n    assert_eq!(record.kind(), \"beta\");\n    assert_eq!(record.text(), \"12\");\n}", "explanation": "Заимствованные поля доступны через публичные имена."},
      {"title": "Документация как тест", "code": "/// Разбирает запись вида `kind:text`.\n///\n/// ```\n/// let record = record_analyzer::parse_record(\"alpha:7\").unwrap();\n/// assert_eq!(record.kind(), \"alpha\");\n/// assert_eq!(record.text(), \"7\");\n/// ```\n///\n/// Нельзя изменять исходную строку, пока используется заимствованное имя:\n///\n/// ```compile_fail\n/// let mut input = String::from(\"alpha:7\");\n/// let record = record_analyzer::parse_record(&input).unwrap();\n/// input.clear();\n/// assert_eq!(record.kind(), \"alpha\");\n/// ```", "explanation": "Doctest исполняется; `compile_fail` проверяет сам факт отказа компиляции, не причину."},
      {"title": "От диагностики к инварианту", "code": "rustc --explain E0502", "explanation": "`rustc --explain` переводит код ошибки в правило доступа."},
    ],
  },
  {
    heading: "Часть 16. Рабочий цикл: инструменты, воспроизводимость и измерение",
    blocks: [
      {"kind": "p", "text": "**16.1. Какую проблему мы сейчас решаем**"},
      {"kind": "p", "text": "Один запуск показал «быстро», другой — «медленно». Между ними могли измениться профиль сборки, зависимости, размер входа, файловый кеш или фоновая нагрузка. Пока условия неизвестны, цифра описывает случай, а не производительность анализатора."},
      {"kind": "p", "text": "**Воспроизводимость** начинается раньше секундомера. Закрепите компилятор в `rust-toolchain.toml`:"},
      {"kind": "p", "text": "В существующем `[package]` сохраните `edition = \"2024\"`. Для проекта с CLI храните `Cargo.lock` в системе контроля версий. Он фиксирует разрешённые зависимости, включая Tokio; `--locked` запрещает команде молча обновить этот выбор. Это ещё не обещание побитово одинаковой сборки на разных ОС."},
      {"kind": "p", "text": "**16.2. Как это устроено внутри**"},
      {"kind": "p", "text": "По умолчанию **dev-профиль** использует `opt-level = 0` и включает debug assertions; **release-профиль** использует `opt-level = 3` и отключает их. Настройки можно изменить в `Cargo.toml`, поэтому проверяйте фактическую конфигурацию. `debug_assert!` не подходит для обязательной валидации пользовательского ввода."},
      {"kind": "p", "text": "**rustfmt** нормализует оформление, **Clippy** ищет подозрительные конструкции, **rust-analyzer** даёт навигацию и обратную связь редактора, **rustdoc** строит документацию. Ни lint, ни подсказка редактора не доказывают корректность. Политика `-D warnings` зависит от версии инструментов: обновление toolchain способно добавить предупреждение без изменения программы."},
      {"kind": "p", "text": "**CI** — место, где тот же цикл воспроизводится на чистом checkout. Закреплённые команды и toolchain делают расхождение расследуемым; образ ОС, переменные окружения и системные инструменты тоже остаются частью условий."},
      {"kind": "p", "text": "**16.3. Минимальный пример, который действительно что-то доказывает**"},
      {"kind": "p", "text": "Из корня пакета:"},
      {"kind": "p", "text": "Не утверждаем, что невидимый репозиторий уже прошёл эти команды: это критерий завершения цикла. При ошибке исправляется причина, затем проверки повторяются."},
      {"kind": "p", "text": "Следующий Bash-протокол предполагает CLI, читающий записи из указанного файла. Часть 10 принимает одну строку аргументом; для этого протокола расширьте CLI чтением файла построчно (это входит в проект 19.4) либо подавайте одиночную запись, сохранив одинаковый способ для обеих сборок. Грамматика — `kind:text` из части 10. Предполагаются стандартный каталог `target` и отсутствие переопределяющих профиль настроек."},
      {"kind": "p", "text": "Проверка `cmp` устанавливает одинаковый stdout на этих входах, не эквивалентность программ для всех данных. Успешные коды завершения контролирует оболочка."},
      {"kind": "p", "text": "**16.4. Ловушка №16: «время cargo run показывает производительность Rust»**"},
      {"kind": "p", "text": "Измерять один debug-запуск вместе с компиляцией — значит смешать работу Cargo, компилятора и программы. Даже `cargo run --release` может сначала пересобирать изменившиеся исходники. Здесь секундомер запускает уже готовый бинарник."},
      {"kind": "p", "text": "В протокол входят запуск процесса, чтение, разбор, вычисление результата и формирование stdout с записью в `/dev/null`. Исключены сборка, создание набора, предварительные сравнения и прогрев. Файловый кеш не очищается: это не измерение холодного диска. Укажите модель CPU, память, ОС, файловую систему, режим питания, ревизию исходников, переменные сборки и фоновую нагрузку."},
      {"kind": "p", "text": "Сохраните десять наблюдений, сообщите медиану и разброс как результаты **этого эксперимента**. Если длительность близка к разрешению таймера, увеличьте фиксированный набор и повторите весь протокол. Готовых чисел здесь нет: измерение не выполнялось. Никакой межмашинный выигрыш из этих команд не следует."},
      {"kind": "p", "text": "**16.5. Соглашения и опытное суждение**"},
      {"kind": "list", "items": ["`cargo fmt --check` — Проверить оформление без изменения файлов", "`cargo clippy -- -D warnings` — Применить выбранную строгую lint-политику", "`cargo test` — Выполнить включённые проверки", "`cargo doc --no-deps` — Построить документацию пакета", "`cargo build --release` — Создать оптимизированный артефакт", "`time готовый-бинарник` — Измерить запуск в зафиксированных условиях"]},
      {"kind": "p", "text": "М1 получает окончательную формулировку: **что именно доказывает наблюдение и при каких условиях?** Проверка, предупреждение и измерение отвечают на разные вопросы."},
      {"kind": "callout", "variant": "key", "title": "Как думают опытные", "text": "Сначала я добиваюсь воспроизводимости, затем объясняю цифры. Ускорение интересно лишь после того, как результат проверен, а измеряемая работа определена."},
    ],
    examples: [
      {"title": "Закрепление toolchain", "code": "[toolchain]\nchannel = \"1.90.0\"\nprofile = \"minimal\"\ncomponents = [\"rustfmt\", \"clippy\"]", "explanation": "Канал, профиль и компоненты фиксированы для всех запусков."},
      {"title": "Цикл проверок анализатора", "code": "cargo fmt --check\ncargo clippy --locked -- -D warnings\ncargo test --locked\ncargo doc --locked --no-deps", "explanation": "fmt, clippy, тесты и документация образуют критерий завершения работы."},
      {"title": "Протокол измерения", "code": "set -euo pipefail\n\nmkdir -p data\nprintf 'alpha:7\\nbeta:12\\n' > data/fixed.txt\n\ncargo build --locked\ncargo build --locked --release\n\ntarget/debug/record_analyzer data/fixed.txt > debug.out\ntarget/release/record_analyzer data/fixed.txt > release.out\ncmp debug.out release.out\n\n# Подготовка входа не входит в измерение.\nfor ((i = 0; i < 100000; i++)); do\n    printf 'alpha:7\\n'\ndone > data/bench.txt\n\n# Проверяем результат и на измеряемом наборе.\ntarget/debug/record_analyzer data/bench.txt > bench-debug.out\ntarget/release/record_analyzer data/bench.txt > bench-release.out\ncmp bench-debug.out bench-release.out\n\n{\n    rustc -Vv\n    cargo -V\n    uname -a\n    cksum data/bench.txt\n    cksum target/release/record_analyzer\n} > experiment-env.txt\n\n# Два прогревочных запуска.\nfor ((i = 0; i < 2; i++)); do\n    target/release/record_analyzer data/bench.txt > /dev/null\ndone\n\nTIMEFORMAT='real=%R user=%U sys=%S'\n: > timings.txt\nfor ((i = 1; i <= 10; i++)); do\n    printf 'run=%s ' \"$i\" >> timings.txt\n    { time target/release/record_analyzer data/bench.txt > /dev/null; } \\\n        2>> timings.txt\ndone", "explanation": "Сравнение собранных бинарников на фиксированном входе при названных условиях."},
    ],
  },
  {
    heading: "Часть 17. Заблуждения, ловушки, антипаттерны — сводка",
    blocks: [
      {"kind": "p", "text": "**17.1. Таблица заблуждений**"},
      {"kind": "p", "text": "После нескольких работающих программ возникает опасное сокращение: «Rust проверил — значит, правильно». Но компилятор, типы, тесты и измерения отвечают на **разные вопросы**. Эта таблица помогает сначала выбрать проверку, а уже затем менять программу."},
      {"kind": "list", "items": ["№1. Компилируется — значит, работает правильно — Компиляция не доказывает соответствие задаче; Как проверить или где разобрано: Различие гарантий и поведения: части 0–1", "№2. Тип результата определяет арифметику — Типы операндов и операций важны до присваивания результата; Как проверить или где разобрано: Арифметика и преобразования: часть 2", "№3. Флаги гибче `enum` — Независимые флаги допускают бессмысленные сочетания; `enum` описывает допустимые варианты; Как проверить или где разобрано: Моделирование состояний: часть 3", "№4. Присваивание копирует значение — Для не-`Copy` типов оно обычно перемещает владение; Как проверить или где разобрано: Move и последующее использование: часть 4", "№5. Borrow checker нужен только из-за реаллокации — Он проверяет совместимость доступов, а не только стабильность адреса; Как проверить или где разобрано: Конфликт чтения и изменения: часть 5", "№6. Lifetime-аннотация продлевает жизнь значения — Она выражает отношения допустимых заимствований; Как проверить или где разобрано: Возврат заимствованного результата: часть 6", "№7. `impl Trait` означает любой тип — В возвращаемой позиции это один скрытый конкретный тип; в позиции аргумента — обобщение; Как проверить или где разобрано: Различия generic, `impl Trait`, `dyn Trait`: часть 7", "№8. `map` уже выполнил преобразование — Итератор ленив: работу запускает потребление; Как проверить или где разобрано: Итераторы: часть 8", "№9. `unwrap` обработал ошибку — Он выбрал панику при неуспехе; Как проверить или где разобрано: `Result`, `?`, `AppError`: часть 9", "№10. Targets пакета разделяют приватность — Library и binary — разные crates, приватные детали библиотеки недоступны CLI; Как проверить или где разобрано: Устройство `record_analyzer`: часть 10", "№11. `RefCell` снимает ограничения — Проверка заимствований переносится во время исполнения; Как проверить или где разобрано: Динамические заимствования: часть 11", "№12. `Arc` делает содержимое безопасным — Он обеспечивает потокобезопасный подсчёт ссылок, а не произвольный доступ к `T`; Как проверить или где разобрано: Канал и `Arc<Mutex<_>>`: часть 12", "№13. `await` не влияет на ресурсы — Сохранённые значения могут оставаться занятыми во время приостановки; Как проверить или где разобрано: Tokio и состояние future: часть 13", "№14. `unsafe` локален по последствиям — Обязательства могут связывать конструктор, методы и уничтожение объекта; Как проверить или где разобрано: Аудит unsafe-оболочки: часть 14", "№15. Happy path проверяет контракт — Нужны проверки ошибок, границ и недопустимых способов использования; Как проверить или где разобрано: Unit, integration, doc, `compile_fail`: часть 15", "№16. `cargo run` — benchmark — Один запуск смешивает разные затраты и не даёт устойчивого сравнения; Как проверить или где разобрано: Протокол измерения: часть 16", "Утечки гарантированно отсутствуют — Memory safety допускает утечки, например сильные циклы `Rc`; Как проверить или где разобрано: Часть 11", "Deadlock отсутствует — Отсутствие гонок данных не доказывает прогресс; Как проверить или где разобрано: Блокировки: часть 12", "`&T` означает абсолютную неизменяемость — Внутренняя изменяемость допускает контролируемое изменение через общий доступ; Как проверить или где разобрано: `RefCell`, `Mutex`: части 11–12", "`T: 'static` означает жизнь до конца процесса — Ограничение не запрещает уничтожить значение раньше; Как проверить или где разобрано: Lifetimes и границы задач: части 6, 12–13", "Zero-cost означает отсутствие аллокаций — Отсутствие обязательной надбавки абстракции не отменяет стоимость выбранного представления; Как проверить или где разобрано: Части 7, 11, 16", "Все future — `Send` — Это зависит от сохранённого состояния; Как проверить или где разобрано: Часть 13", "Отмена future откатывает эффекты — Уничтожение локального состояния не отменяет уже выполненные внешние действия; Как проверить или где разобрано: Часть 13", "Miri или тесты доказывают корректность `unsafe` — Они обнаруживают нарушения на проверенных исполнениях, но не заменяют доказательство контракта; Как проверить или где разобрано: Части 14–16"]},
      {"kind": "p", "text": "**17.2. Ловушки**"},
      {"kind": "p", "text": "Здесь прежние номера служат адресами для возвращения к объяснению, а не новой классификацией ошибок. **Симптом не равен причине**: одинаковая паника может происходить из-за ошибочного входа, нарушенного протокола доступа или неверно выбранной границы ответственности."},
      {"kind": "list", "items": ["№1. Компилируется — значит, работает — Неверный ответ без ошибок сборки; Причина: Проверена не бизнес-задача; Проверка / исправление: Проверить ожидаемые результаты; Часть: 1", "№2. Тип результата определяет арифметику — Неожиданное значение или переполнение; Причина: Преобразование сделано слишком поздно; Проверка / исправление: Разобрать тип каждой операции; Часть: 2", "№3. Флаги гибче `enum` — Невозможные состояния приходится отлавливать; Причина: Представление допускает лишние комбинации; Проверка / исправление: Выразить варианты через `enum`; Часть: 3", "№4. Присваивание копирует — Использование после move отвергнуто; Причина: Владелец уже сменился; Проверка / исправление: Отметить перемещение; выбрать borrow или осмысленный `clone`; Часть: 4", "№5. Borrow checker только про реаллокацию — `reserve` не устраняет ошибку; Причина: Конфликтуют разрешения доступа; Проверка / исправление: Найти последнее использование ссылки; Часть: 5", "№6. Lifetime-аннотация продлевает жизнь — Возвращаемая ссылка отвергнута; Причина: Владелец уничтожается раньше потребителя; Проверка / исправление: Изменить владельца или возвращаемый тип; Часть: 6", "№7. `impl Trait` — любой тип — Разные ветви возврата несовместимы; Причина: Требуется один конкретный возвращаемый тип; Проверка / исправление: Выбрать общий тип, `enum` либо `dyn`; Часть: 7", "№8. `map` уже выполнил — Ожидаемый эффект отсутствует; Причина: Итератор не потреблён; Проверка / исправление: Проследить потребителя; Часть: 8", "№9. `unwrap` обработал — Обычный плохой ввод вызывает панику; Причина: Ошибка не получила прикладного представления; Проверка / исправление: Вернуть или преобразовать `Result`; Часть: 9", "№10. Targets разделяют приватность — CLI не видит функцию библиотеки; Причина: Перепутаны пакет и crate; Проверка / исправление: Проверить публичную границу; Часть: 10", "№11. `RefCell` снимает ограничения — Паника динамического заимствования; Причина: Несовместимые guard существуют одновременно; Проверка / исправление: Сократить область доступа; Часть: 11", "№12. `Arc` делает содержимое безопасным — Поток не принимает значение; Причина: Требования к содержимому не выполнены; Проверка / исправление: Проверить `Send` / `Sync`, выбрать протокол; Часть: 12", "№13. `await` не влияет на ресурсы — Зависание или future не `Send`; Причина: Ресурс сохранён через приостановку; Проверка / исправление: Убрать guard из сохраняемого состояния; Часть: 13", "№14. `unsafe` локален — Безопасный вызов допускает UB; Причина: Оболочка не обеспечивает обязательство; Проверка / исправление: Аудировать весь жизненный цикл; Часть: 14", "№15. Happy path проверяет контракт — Ошибочный ввод ломает библиотеку; Причина: Непроверены отрицательные случаи; Проверка / исправление: Добавить соответствующий слой тестов; Часть: 15", "№16. `cargo run` — benchmark — «Ускорение» исчезает при повторе; Причина: Несопоставимые условия; Проверка / исправление: Повторить протокол части 16; Часть: 16"]},
      {"kind": "p", "text": "**17.3. Антипаттерны**"},
      {"kind": "p", "text": "Антипаттерн обычно начинается как локально разумная экономия времени. Вред появляется, когда временное решение становится правилом для всей программы."},
      {"kind": "list", "items": ["`clone` как универсальное лекарство — Быстро убирает конфликт владения; Накопленный ущерб: Лишние копии и расходящиеся версии состояния; Замена: Сначала определить владельца и нужную длительность доступа", "`Rc<RefCell<_>>` вокруг всего состояния — Легко соединять любые объекты; Накопленный ущерб: Неявный граф зависимостей, runtime-паники, циклы; Замена: Явное владение; разделение состояния; `Weak` для обратных связей", "Публичность всех деталей — CLI и тестам всё доступно; Накопленный ущерб: Внутреннее устройство становится обязательством API; Замена: Минимальная публичная граница", "Повсеместный `unwrap` — Не нужно проектировать ошибки; Накопленный ущерб: Ожидаемый отказ превращается в панику; Замена: Типизированные ошибки и контекст на границе", "Преждевременный async — Выглядит готовностью к масштабу; Накопленный ущерб: Состояние приостановки и отмена без полезного выигрыша; Замена: Сначала определить реальную конкурентность ожиданий", "Необоснованный `unsafe` — Кажется обходом лишних проверок; Накопленный ущерб: Недоказанные обязательства у безопасных клиентов; Замена: Безопасное представление либо письменное доказательство необходимости и корректности", "Оптимизация без измерения — Удаляется визуально «дорогой» код; Накопленный ущерб: Сложность без подтверждённого эффекта; Замена: Базовый замер, гипотеза, сопоставимое повторение"]},
      {"kind": "p", "text": "`clone`, внутренняя изменяемость и async сами по себе не плохи. Проблема — **отсутствие объяснения**, какую задачу они решают и какую цену вводят."},
      {"kind": "p", "text": "**17.4. Диагностический маршрут**"},
      {"kind": "p", "text": "При ошибке доступа сначала определите **владельца** значения. Затем отметьте места перемещения, создания ссылок и их последних использований. Не ориентируйтесь только на фигурные скобки: компилятор учитывает фактическую потребность в заимствовании."},
      {"kind": "p", "text": "Теперь сформулируйте требуемый доступ: чтение, исключительное изменение, передача владения или совместное владение. Сравните его с уже существующим доступом. Это превращает сообщение компилятора из препятствия в описание несовместимых требований."},
      {"kind": "p", "text": "Выберите один из трёх уровней исправления. **API** меняется, если функция забирает данные без необходимости или возвращает неудобную зависимость. **Область доступа** сокращается, если ссылка либо guard удерживается дольше работы. **Представление данных** меняется, если независимые части искусственно соединены или владельцы не соответствуют задаче."},
      {"kind": "p", "text": "После изменения снова предскажите поведение, соберите программу и выполните проверки контракта. Исчезнувшая ошибка компиляции — необходимый результат, но не доказательство сохранения смысла. Не принимайте первое компилирующееся исправление автоматически: сравните копирование, длительность блокировки и удобство клиента API."},
    ],
  },
  {
    heading: "Часть 18. Как думают великие (ментальные модели)",
    blocks: [
      {"kind": "p", "text": "**18.1. М1: место доказательства**"},
      {"kind": "p", "text": "**М1** спрашивает, где обеспечивается утверждение: в типе, проверке времени исполнения, тесте или ручном обосновании. Новый пример: загрузчик ограничивает размер текста до разбора. Тип `String` не доказывает соблюдение лимита; нужна проверка на входной границе."},
      {"kind": "p", "text": "Два знакомых предсказания: компиляция не обнаружит неверную формулу отчёта; успешный тест не докажет все допустимые вызовы unsafe-оболочки."},
      {"kind": "p", "text": "Вопрос вам: кто должен гарантировать лимит, если разборщик вызывается не только из CLI? **Предел модели:** она размещает обязательство, но не доказывает, что конкретная проверка достаточна."},
      {"kind": "p", "text": "**18.2. М2: граф владения**"},
      {"kind": "p", "text": "**М2** рассматривает владельцев, заимствованные рёбра и момент уничтожения. Новый пример: индекс хранит диапазоны байтов внутри принадлежащего ему исходного текста вместо ссылок на собственные поля. Так зависимость становится проверяемыми координатами, хотя корректность диапазонов ещё требуется обеспечить."},
      {"kind": "p", "text": "Два знакомых предсказания: после move старое имя нельзя использовать; обратное ребро `Weak` не удерживает объект живым."},
      {"kind": "p", "text": "Вопрос вам: что произойдёт с диапазонами после редактирования исходного текста? **Предел модели:** правильное освобождение памяти не гарантирует актуальность индекса и отсутствие логических ошибок."},
      {"kind": "p", "text": "**18.3. М3: разрешения во времени**"},
      {"kind": "p", "text": "**М3** описывает не «ссылки вообще», а совместимость доступов в конкретные интервалы. Новый пример: при обработке двух независимых частей буфера API разделения среза выражает непересечение, которое не следует просто из двух разных чисел индекса."},
      {"kind": "p", "text": "Два знакомых предсказания: запас ёмкости `Vec` не разрешает конфликтующий `push`; несовместимые заимствования `RefCell` обнаруживаются во время исполнения."},
      {"kind": "p", "text": "Вопрос вам: можно ли закончить чтение первой части до изменения второй, не сохраняя ссылку? **Предел модели:** разрешённый доступ не доказывает правильность вычисления; блокировки требуют ещё и анализа порядка ожидания."},
      {"kind": "p", "text": "**18.4. М4: типовые обязательства**"},
      {"kind": "p", "text": "**М4** читает тип как набор обещаний производителя и требований потребителя. Новый пример: `ReportState::Ready(Summary)` позволяет потребителю получить итог только из завершённого состояния, не сверяя независимый флаг с `Option`."},
      {"kind": "p", "text": "Два знакомых предсказания: `enum` исключает некоторые бессмысленные сочетания; возвращаемый `impl Trait` не объединяет произвольные конкретные типы ветвей."},
      {"kind": "p", "text": "Вопрос вам: какое обязательство останется непроверенным, если конструктор `Summary` публично принимает любые числа? **Предел модели:** типы не выражают автоматически все арифметические, временные и прикладные инварианты."},
      {"kind": "p", "text": "**18.5. М5: приостановка**"},
      {"kind": "p", "text": "**М5** предлагает мысленно остановить async-функцию в точке ожидания и перечислить сохранённое состояние. Новый пример: задача резервирует локальный номер отчёта, затем ждёт таймер и только потом публикует результат. Отмена может оставить номер использованным без готового отчёта."},
      {"kind": "p", "text": "Два знакомых предсказания: guard может удерживать ресурс через `await`; не-`Send` состояние, сохранённое через ожидание, способно сделать future не-`Send`."},
      {"kind": "p", "text": "Вопрос вам: является ли пропуск номера допустимым контрактом? **Предел модели:** схема приостановки не устанавливает гарантии внешнего хранилища и не обещает справедливость планировщика."},
      {"kind": "p", "text": "**18.6. Как совместить модели**"},
      {"kind": "p", "text": "Пусть задача принимает данные, сохраняет часть состояния через `await`, обновляет общий результат и допускает отмену."},
      {"kind": "p", "text": "**М1** размещает проверку входа и доказательство правил отмены. **М2** выясняет, кто владеет данными во время ожидания. **М3** обнаруживает лишний guard и конфликт доступа. **М4** проверяет требования `Send` и представимость незавершённого результата. **М5** показывает точку, где отмена оставит уже выполненный эффект без следующего шага."},
      {"kind": "p", "text": "Проверяйте кейс по всем пяти направлениям: отсутствие ошибки в одном не закрывает вопросы остальных. **Модели — не полная спецификация**."},
    ],
  },
  {
    heading: "Часть 19. Упражнения и проекты",
    blocks: [
      {"kind": "p", "text": "**19.1. Уровень 1: синтаксис и коллекции**"},
      {"kind": "p", "text": "**Сводка числовых записей.** Вычислите сумму, минимум и максимум, явно определив поведение на пустом вводе и при переполнении. **Профессиональное требование — определённый контракт:** проверки пустого набора, одного элемента и границ типа дают документированный результат."},
      {"kind": "p", "text": "**Частотный словарь.** Подсчитайте слова и отсортируйте результат; самостоятельно определите, что считается словом и различаются ли регистры. **Требование — воспроизводимость:** Unicode-ввод обработан согласно выбранному правилу, одинаковые частоты разрешаются детерминированно."},
      {"kind": "p", "text": "**19.2. Уровень 2: абстракции, ошибки и тесты**"},
      {"kind": "p", "text": "**Библиотека разбора конфигурации.** Создайте публичную функцию и типизированные ошибки с достаточным контекстом. **Требование — надёжная граница:** unit-, integration- и doc-тесты проходят, ожидаемый некорректный ввод не вызывает паник."},
      {"kind": "p", "text": "**Два представления отчёта.** Опишите пользовательский трейт и продемонстрируйте generic-вызов и вызов через `dyn`; обоснуйте выбор для неоднородного набора обработчиков. **Требование — объяснимый API:** тесты проверяют оба представления, документация различает диспетчеризацию и владение."},
      {"kind": "p", "text": "**19.3. Уровень 3: владение, заимствование и границы исполнения**"},
      {"kind": "p", "text": "**Заимствующий разборщик записей.** Для записи вида `kind:text` создайте результат, ссылающийся на вход, затем владеющую версию. **Требование — явная зависимость данных:** покажите проверкой компиляции, какая версия пригодна после уничтожения буфера."},
      {"kind": "p", "text": "**Граф с обратными связями.** Постройте небольшой граф на `Rc` и `Weak`, сделайте освобождение наблюдаемым. **Требование — управляемый жизненный цикл:** проверка фиксирует уничтожение узлов; пояснение отличает утечку сильного цикла от нарушения memory safety."},
      {"kind": "p", "text": "**Два конкурентных подсчёта.** Реализуйте одну задачу через канал и через `Arc<Mutex<_>>`. **Требование — проверяемая эквивалентность:** результаты совпадают с последовательным эталоном независимо от порядка сообщений; отдельно перечислены оставшиеся риски зависания и ошибок протокола."},
      {"kind": "p", "text": "**Async-операция с отменой.** Организуйте управляемую точку ожидания и отмену, перечислите сохранённые значения и уже совершённые эффекты. **Требование — определённое завершение:** тест наблюдает очистку локального состояния, а ни один mutex guard не пересекает `await`."},
      {"kind": "p", "text": "**19.4. Уровень 4: портфолио-проекты**"},
      {"kind": "p", "text": "**CLI-анализатор текстовых записей.** Развивайте `record_analyzer` как library и binary: ограничьте размер ввода, добавьте контекст ошибок, документацию и CI. **Требование — воспроизводимая поставка:** свежая копия проходит проверки, превышение лимита отвергается контролируемо, release-измерение содержит вход, версии и методику."},
      {"kind": "p", "text": "**Ограниченный асинхронный конвейер.** Организуйте чтение файлов, преобразование записей и передачу через очередь заданной ёмкости, без сети. Используйте стандартную библиотеку и уже введённый Tokio; для асинхронных файлов и каналов при необходимости явно включите его features `fs` и `sync`, сохранив зафиксированную версию."},
      {"kind": "p", "text": "**Требование — ограниченные ресурсы и завершение:** задайте также предел размера записи и числа задач; тесты насыщают очередь, вводят ошибочный файл и проверяют итоговый отчёт без зависания. Одна ограниченная очередь не считается доказательством ограниченности всей памяти."},
      {"kind": "p", "text": "**19.5. Вызовы на понимание**"},
      {"kind": "p", "text": "Каждый ответ сдавайте в форме **предсказание — причина — проверка**. Код не исправляйте до записи предсказания."},
      {"kind": "list", "items": ["**Восемь фрагментов.** Предскажите, какие самостоятельные программы ниже компилируются, затем проверьте на закреплённом toolchain через `rustc --edition=2024 fragment_N.rs`. Критерий — объяснение каждого результата, не только число совпадений."]},
      {"kind": "p", "text": "`fragment_1.rs`:"},
      {"kind": "p", "text": "`fragment_2.rs`:"},
      {"kind": "p", "text": "`fragment_3.rs`:"},
      {"kind": "p", "text": "`fragment_4.rs`:"},
      {"kind": "p", "text": "`fragment_5.rs`:"},
      {"kind": "p", "text": "`fragment_6.rs`:"},
      {"kind": "p", "text": "`fragment_7.rs`:"},
      {"kind": "p", "text": "`fragment_8.rs`:"},
      {"kind": "list", "items": ["**Ёмкость против доступа.** Объясните, почему `reserve` не разрешает конфликтующий `push`. Критерий — объяснение без ссылки исключительно на перемещение буфера.", "**Короткая жизнь `'static`.** Покажите значение с `T: 'static`, уничтожаемое раньше конца программы. Критерий — наблюдаемое уничтожение и точное толкование ограничения.", "**Два общих контейнера.** Сравните `Arc<RefCell<T>>` и `Arc<Mutex<T>>`. Критерий — раздельный анализ владения, доступа и требований к `T`.", "**Отмена.** Перечислите уничтожаемое состояние future и неотменяемые уже выполненные эффекты. Критерий — конкретный сценарий, включая отличие уничтожения future от простого удаления handle задачи.", "**Безопасная сигнатура, unsound-реализация.** Предложите небольшой контрпример и найдите недостающее обязательство оболочки. Критерий — письменный разбор; потенциально вызывающий UB код не запускайте как обычный тест.", "**Цена исправления.** Исправьте одну ошибку заимствования двумя способами. Критерий — сравнение аллокаций, копирования, времени удержания доступа и удобства API."]},
    ],
    examples: [
      {"title": "Фрагмент 1: move и использование", "code": "fn main() {\n    let a = String::from(\"record\");\n    let b = a;\n    println!(\"{a} {b}\");\n}", "explanation": "Не компилируется: значение уже перемещено."},
      {"title": "Фрагмент 2: Copy", "code": "fn main() {\n    let a = 7_u32;\n    let b = a;\n    println!(\"{a} {b}\");\n}", "explanation": "Компилируется: `u32` копируется при присваивании."},
      {"title": "Фрагмент 3: конфликт доступа", "code": "fn main() {\n    let mut values = vec![1, 2];\n    let first = &values[0];\n    values.push(3);\n    println!(\"{first}\");\n}", "explanation": "Не компилируется: `push` перекрывает живое заимствование."},
      {"title": "Фрагмент 4: NLL", "code": "fn main() {\n    let mut values = vec![1, 2];\n    let first = &values[0];\n    println!(\"{first}\");\n    values.push(3);\n}", "explanation": "Компилируется: последнее использование ссылки уже прошло."},
      {"title": "Фрагмент 5: ссылка на локальное", "code": "fn make() -> &'static str {\n    let text = String::from(\"kind:text\");\n    text.as_str()\n}\n\nfn main() {\n    println!(\"{}\", make());\n}", "explanation": "Не компилируется: возвращаемая ссылка указывает на уничтожаемое значение."},
      {"title": "Фрагмент 6: T: 'static'", "code": "fn accept<T: 'static>(value: T) {\n    drop(value);\n}\n\nfn main() {\n    accept(String::from(\"temporary\"));\n}", "explanation": "Компилируется: владеющий `String` не содержит коротких заимствований."},
      {"title": "Фрагмент 7: impl Iterator в ветвях", "code": "fn items(short: bool) -> impl Iterator<Item = u8> {\n    if short {\n        0_u8..2\n    } else {\n        vec![1_u8, 2].into_iter()\n    }\n}\n\nfn main() {\n    for item in items(true) {\n        println!(\"{item}\");\n    }\n}", "explanation": "Не компилируется: непрозрачный тип допускает один конкретный тип."},
      {"title": "Фрагмент 8: Arc над RefCell", "code": "use std::{cell::RefCell, sync::Arc, thread};\n\nfn main() {\n    let value = Arc::new(RefCell::new(0));\n    let other = Arc::clone(&value);\n    thread::spawn(move || {\n        *other.borrow_mut() += 1;\n    })\n    .join()\n    .unwrap();\n}", "explanation": "Не компилируется: `RefCell` не реализует `Sync`."},
    ],
  },
  {
    heading: "Часть 20. Ресурсы",
    blocks: [
      {"kind": "p", "text": "**20.1. Обязательный минимум**"},
      {"kind": "p", "text": "**The Rust Programming Language** — https://doc.rust-lang.org/book/. Учебник после частей 0–7: читать для связного повторения; не ожидать исчерпывающей спецификации."},
      {"kind": "p", "text": "**std reference** — https://doc.rust-lang.org/std/. Справочник после частей 4–8: читать для контрактов конкретных API; не ожидать архитектуры вашего приложения."},
      {"kind": "p", "text": "**The Cargo Book** — https://doc.rust-lang.org/cargo/. Справочник после частей 10, 16: читать для targets, features и профилей; не ожидать объяснения заимствований."},
      {"kind": "p", "text": "**The Rust Reference** — https://doc.rust-lang.org/reference/. Справочник после частей 6–7, 14: читать для точных правил языка; не ожидать пошагового первого знакомства."},
      {"kind": "p", "text": "**20.2. Книги**"},
      {"kind": "p", "text": "**Rust by Example** — https://doc.rust-lang.org/rust-by-example/. Учебник после частей 1–8: читать для коротких экспериментов; не ожидать полного проекта."},
      {"kind": "p", "text": "**The Rustonomicon** — https://doc.rust-lang.org/nomicon/. Углубление после части 14: читать для обязательств небезопасного кода; не ожидать автоматического доказательства вашей оболочки."},
      {"kind": "p", "text": "**Asynchronous Programming in Rust** — https://rust-lang.github.io/async-book/. Углубление после части 13: читать для устройства async; не ожидать актуальности каждого runtime-примера — **актуальность проверять**."},
      {"kind": "p", "text": "**The Rust Edition Guide** — https://doc.rust-lang.org/edition-guide/. Справочник после части 16: читать для различий editions; не ожидать универсальной миграции зависимостей."},
      {"kind": "p", "text": "**20.3. Статьи и доклады**"},
      {"kind": "p", "text": "**Fearless Concurrency** — https://blog.rust-lang.org/2015/04/10/Fearless-Concurrency/. Углубление после части 12: читать для мотивации конкурентной модели; не ожидать обещания отсутствия deadlock."},
      {"kind": "p", "text": "**Rust 1.0 announcement** — https://blog.rust-lang.org/2015/05/15/Rust-1.0/. Историческое углубление после части 0: читать для контекста стабильности; не ожидать перечня современных возможностей."},
      {"kind": "p", "text": "**Tokio spawning tutorial** — https://tokio.rs/tokio/tutorial/spawning. Учебник после части 13: читать для границ задач, `Send` и `'static`; не ожидать полноценного протокола отмены приложения."},
      {"kind": "p", "text": "**20.4. Практика**"},
      {"kind": "p", "text": "**Rustlings** — https://github.com/rust-lang/rustlings. Практический учебник после частей 1–9: использовать для исправления небольших программ; не ожидать проверки проектирования системы."},
      {"kind": "p", "text": "**Rust Playground** — https://play.rust-lang.org/. Экспериментальная площадка после части 1: использовать для минимального примера; не ожидать совпадения с локальным окружением."},
      {"kind": "p", "text": "**Exercism Rust** — https://exercism.org/tracks/rust. Практический учебник после частей 3–9: использовать для самостоятельных решений; не ожидать переноса всех навыков в production без проектов."},
      {"kind": "p", "text": "**20.5. Инструменты**"},
      {"kind": "p", "text": "**rustup** — https://rust-lang.github.io/rustup/. Справочник после части 16: читать для управления toolchain; не ожидать фиксации системного окружения."},
      {"kind": "p", "text": "**Clippy** — https://doc.rust-lang.org/clippy/. Справочник после части 15: читать для lint-диагностики; не ожидать доказательства корректности."},
      {"kind": "p", "text": "**rustfmt** — https://github.com/rust-lang/rustfmt. Справочник после части 10: читать для форматирования; не ожидать улучшения архитектуры."},
      {"kind": "p", "text": "**rust-analyzer** — https://rust-analyzer.github.io/. Справочник после части 1: читать для редакторной диагностики; не ожидать замены сборки и тестов."},
      {"kind": "p", "text": "**Miri** — https://github.com/rust-lang/miri. Углубление после частей 14–15: читать для поиска UB на исполненных путях; не ожидать полного доказательства soundness."},
      {"kind": "p", "text": "**Tokio docs** — https://docs.rs/tokio/. Справочник после части 13: читать для контрактов runtime и примитивов; не ожидать совпадения открытой версии с `Cargo.lock` без проверки."},
    ],
  },
  {
    heading: "Заключение: план на 8 недель",
    blocks: [
      {"kind": "p", "text": "Рассчитывайте на **6–8 часов в неделю**: чтение, самостоятельный эксперимент и разбор расхождений между прогнозом и результатом. Если времени меньше, растяните одну строку плана на две недели. Ускоряться лучше за счёт знакомого синтаксиса, а не пропуска проверки понимания."},
      {"kind": "p", "text": "В начале занятия запишите ожидаемое поведение программы. В конце сохраните минимальный пример и объясните, какое правило подтвердилось либо оказалось понято неверно. Это полезнее коллекции больших фрагментов, которые однажды удалось заставить работать."},
      {"kind": "list", "items": ["1 — 0–3; 19.1; Практика: Создан Cargo-проект; check/build/run; статистика и enum-состояния; Проверяемый результат: Отличены гарантии компиляции от контракта; определён пустой ввод", "2 — 4–5; Практика: Предсказание move и borrow-ошибок; два изменения непересекающихся данных без clone; Проверяемый результат: Названы владелец и интервалы доступа", "3 — 6–7; 19.2; Практика: Заимствующая и владеющая версии API; generic и &dyn Trait; Проверяемый результат: Объяснены время жизни результата и выбор диспетчеризации", "4 — 8–10; 19.1–19.2; Практика: Library + CLI для разбора и группировки записей; Проверяемый результат: Ошибки проходят через публичную границу без ожидаемых паник", "5 — 11–12; 19.3; Практика: Граф с Weak; канал и Arc/Mutex; Проверяемый результат: Различены освобождение, отсутствие гонки данных и прогресс", "6 — 13–14; 19.3; Практика: Async-пример без guard через await; аудит unsafe-оболочки; Проверяемый результат: Перечислены сохранённое состояние и обязательства безопасности", "7 — 15–17; 19.4; Практика: Портфолио-проект: fmt, Clippy, тесты, release-замер; Проверяемый результат: Для каждой проверки названы возможности и пределы", "8 — 18–20; повтор 13, 15–16; Практика: Минимальный ограниченный async-конвейер; вызовы на понимание; Проверяемый результат: Предсказан новый случай и проверены отмена, ошибки, завершение"]},
      {"kind": "p", "text": "Не превращайте этот график в экзамен на скорость. Если задание компилируется, но вы не можете объяснить, почему ссылка допустима или что сохранится через `await`, вернитесь к минимальному примеру. Если объяснение уверенное, но проверка ему противоречит, сохраните расхождение: это точная следующая учебная задача."},
      {"kind": "p", "text": "Завершённый маршрут не означает готовности писать произвольный `unsafe` или проектировать любую конкурентную систему. Он даёт рабочий способ двигаться дальше: формулировать обязательство, находить место его обеспечения и выбирать проверку с известными ограничениями. Зафиксированные версии и воспроизводимые эксперименты позволяют возвращаться к выводам, а не доверять памяти."},
      {"kind": "p", "text": "**«Знает» и «владеет» различаются предсказанием.** Знающий узнаёт знакомую конструкцию и вспоминает правило. Владеющий предсказывает вместо угадывания: понимает устройство, ограничения и причины, поэтому объясняет поведение в ещё не встречавшемся случае — и умеет проверить своё объяснение."},
    ],
  },
];
