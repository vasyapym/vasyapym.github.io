export type TopicStatus = "queued" | "in-progress" | "revisit" | "applied";

export type FeedbackKind =
  | "clear"
  | "too-broad"
  | "too-abstract"
  | "already-familiar"
  | "needs-example"
  | "needs-exercise";

export type LessonTheory = {
  readonly problem: string;
  readonly model: string;
  readonly mechanics: string;
  readonly pitfalls: readonly string[];
  readonly whenNot: string;
};

export type LessonExample = {
  readonly title: string;
  readonly code: string;
  readonly explanation: string;
};

export type TopicCard = {
  readonly id: string;
  readonly title: string;
  readonly summary: string;
  readonly concepts: readonly string[];
  readonly practicePrompt: string;
  readonly checkPrompt: string;
  readonly tier?: number;
  readonly complexity?: number;
  readonly proofBrief?: string;
  readonly references?: readonly string[];
  readonly objectives?: readonly string[];
  readonly lesson?: LessonTheory;
  readonly examples?: readonly LessonExample[];
};

export type PracticeArea = {
  readonly id: string;
  readonly title: string;
  readonly description: string;
  readonly topics: readonly TopicCard[];
  readonly tier?: number;
  readonly dependencies?: readonly string[];
};

export const FEEDBACK_LABELS: Readonly<Record<FeedbackKind, string>> = {
  clear: "Clear",
  "too-broad": "Too broad",
  "too-abstract": "Too abstract",
  "already-familiar": "Already familiar",
  "needs-example": "Needs an example",
  "needs-exercise": "Needs an exercise",
};

const linuxTopics: readonly TopicCard[] = [
  {
    id: "linux-cli-terminal",
    title: "Терминал и командная строка",
    summary: "Разобраться в связке «терминал → shell → prompt», чтобы вся дальнейшая карта опиралась на понятную рабочую среду.",
    concepts: ["terminal", "shell", "prompt", "Ctrl+C", "история команд"],
    objectives: [
      "Отличать эмулятор терминала от shell и понимать роль prompt",
      "Свободно пользоваться базовыми жестами: ввод, прерывание, история, автодополнение",
      "Разворачивать disposable Linux-среду для безопасных экспериментов",
    ],
    lesson: {
      problem: "Серверы почти всегда живут без графического интерфейса: доступ к ним — это SSH и чёрное окно с текстом. Пока командная строка пугает, любая диагностика сводится к «перезагрузи и молись». Освоить терминал — значит получить прямой, скриптуемый доступ ко всей системе.",
      model: "Держи разделение: эмулятор терминала — это окно; shell (bash, zsh) — программа внутри него, которая читает твои строки и запускает другие программы. Prompt вида vasya@server:~$ — приглашение: «предыдущая команда разобрана, жду следующую». Всё введённое shell сначала разбирает и только потом выполняет.",
      mechanics: "Строка уходит на выполнение после Enter. Ctrl+C прерывает активную команду, Ctrl+D закрывает поток ввода (часто это выход), Ctrl+L очищает экран. Стрелка вверх листает историю, Tab автодополняет имена команд и файлов. Для практики нужна disposable среда: WSL2 на Windows, виртуальная машина или контейнер docker run -it ubuntu bash. Ломать там можно всё — пересоздал и продолжил.",
      pitfalls: [
        "Копировать команду вместе с ведущим $: он обозначает prompt из учебника, а не часть команды.",
        "Экспериментировать сразу на основной системе: сначала disposable-среда, потом смелость.",
        "Ждать от Ctrl+C поведения «отмены как в GUI»: это посылка сигнала SIGINT активной программе, а не undo.",
      ],
      whenNot: "Терминал не заменяет браузер, GUI-редактор или просмотр картинок. Но любая операция, которую делаешь больше двух раз, почти всегда быстрее и надёжнее делается из shell.",
    },
    examples: [
      {
        title: "Первые команды: кто я, где я",
        code: "whoami                    # имя текущего пользователя\nhostname                  # имя машины\npwd                       # текущий каталог (print working directory)\necho \"Привет, $(whoami)\"  # подстановка результата команды в строку",
        explanation: "echo печатает текст; конструкция $(...) выполняет команду и подставляет её вывод — первый шаг к композиции программ.",
      },
      {
        title: "Disposable Linux за минуту (если есть Docker)",
        code: "docker run -it --rm ubuntu bash\n# --rm удалить контейнер после выхода\napt update && apt install -y curl   # внутри контейнера ты root",
        explanation: "Контейнер — идеальная песочница курса: сломал — вышел, контейнер исчез, следующий запуск начинает с чистого листа.",
      },
    ],
    practicePrompt: "Разверни disposable Linux-среду (WSL2, VM или контейнер), прогони примеры: попробуй Tab, стрелку вверх и прерви долгий sleep 60 через Ctrl+C.",
    checkPrompt: "Своими словами объясни, чем отличаются терминал, shell и prompt, и что происходит между нажатием Enter и запуском программы.",
    tier: 1,
    complexity: 1,
    references: ["The Linux Command Line — William Shotts (свободно доступна)", "man bash"],
  },
  {
    id: "linux-filesystem-hierarchy",
    title: "Иерархия файловой системы",
    summary: "Научиться читать дерево каталогов Linux, чтобы на любом незнакомом сервере понимать, где лежат программы, настройки и данные.",
    concepts: ["/", "/home", "/etc", "/var", "/usr", "/tmp", "/dev", "/proc", "mount"],
    objectives: [
      "Знать назначение основных веток: /etc, /home, /var, /usr, /tmp, /dev, /proc, /sys, /opt",
      "Ориентироваться в чужой системе без поиска «где тут диск C:»",
      "Различать обычные каталоги и виртуальные файловые системы ядра",
    ],
    lesson: {
      problem: "В Windows у каждого диска своя буква; в Linux — одно дерево от корня /. Приходя на сервер, ты не знаешь ни программ, ни настроек, но знаешь карту: конфиги в /etc, логи в /var/log, дома пользователей в /home. Эта карта экономит часы на каждом незнакомом хосте.",
      model: "Представь единое дерево, растущее из /. Отдельные диски не создают новых букв — они монтируются в существующую ветку. Принцип «всё есть файл» доведён до конца: устройства живут в /dev как файлы, состояние ядра и процессов видно через /proc и /sys — тоже как файлы.",
      mechanics: "Основные ветки: /home — домашние каталоги; /etc — текстовые конфигурации системы; /var — изменяемые данные: логи, очереди, кэши; /usr — установленные программы; /tmp — временные файлы, стираются при перезагрузке; /opt — сторонний софт; /dev — файлы устройств; /proc и /sys — виртуальные ФС, окно в память ядра. df -h показывает, какие реальные устройства смонтированы в какие точки дерева.",
      pitfalls: [
        "Чистить «мусор» в /etc или /usr: там нет мусора, каждое удаление — потенциальная поломка системы.",
        "Хранить важное в /tmp: содержимое пропадает при перезагрузке и часто читается всеми пользователями.",
        "Считать, что /proc занимает место на диске: файлы генерируются ядром на лету и имеют нулевой размер на носителе.",
      ],
      whenNot: "Не нужно заранее запоминать каждую вложенную папку — man hier подскажет по ходу. Наизусть нужен только верхний уровень.",
    },
    examples: [
      {
        title: "Осмотреться в системе",
        code: "ls /            # корень дерева\nls /etc         # примеры конфигураций\nls /var/log     # журналы\ndf -h           # какие устройства куда смонтированы",
        explanation: "df сопоставляет файловые системы и точки монтирования: дерево одно, носителей много.",
      },
      {
        title: "Виртуальные файловые системы",
        code: "grep -c processor /proc/cpuinfo   # число логических CPU\nls /sys/block                     # блочные устройства глазами ядра\nman hier                          # справочник по всей иерархии",
        explanation: "/proc и /sys выглядят как файлы, но читаются прямо из памяти ядра — сейчас достаточно знать, что они существуют.",
      },
    ],
    practicePrompt: "Обойди ls-ом /, /etc, /var/log, /usr/bin и /proc; найди два конфигурационных файла и один лог; объясни вывод df -h.",
    checkPrompt: "Куда ты положишь собственный скрипт пользователя, общесистемную настройку сервиса и файл на одну минуту — и почему именно туда?",
    tier: 1,
    complexity: 1,
    references: ["man hier", "Filesystem Hierarchy Standard (FHS)"],
  },
  {
    id: "linux-shell-fundamentals",
    title: "Основы shell: команды, аргументы, кавычки",
    summary: "Понять, как shell разбирает командную строку, чтобы пробелы в именах файлов перестали быть необъяснимой магией.",
    concepts: ["аргумент", "опция", "quoting", "expansion", "$(команда)"],
    objectives: [
      "Читать любую команду: имя, опции, аргументы",
      "Правильно квотировать строки и различать двойные и одинарные кавычки",
      "Использовать подстановку переменных $VAR и команд $(...)",
    ],
    lesson: {
      problem: "Классическая ловушка новичка: cat мой файл.txt падает с двумя ошибками, потому что shell разбил строку на три слова. Пока непонятно, КАК shell разбирает строку, каждая нестандартная ситуация — пробел, пустая переменная, спецсимвол — выглядит как поломка вселенной.",
      model: "Shell проходит стадии: разбивает строку на слова по пробелам, раскрывает подстановки ($VAR, $(cmd)) и лишь затем запускает программу с готовыми аргументами. Кавычки управляют разбором: двойные разрешают подстановки внутри себя, одинарные отключают всё, обратный слэш экранирует один следующий символ.",
      mechanics: "Опции бывают короткие (-l, клеятся: -la) и длинные (--all); аргумент — то, с чем команда работает. today=$(date +%F) сохраняет вывод команды в переменную. Сомневаешься в разборе — прогони команду через echo: echo rm \"$file\" покажет ровно те слова, которые получила бы rm.",
      pitfalls: [
        "Подстановка без кавычек: file=\"мой отчёт.txt\"; cat $file передаст ДВА аргумента. Правильно: cat \"$file\".",
        "Одинарные кавычки ничего не разворачивают: echo '$HOME' напечатает буквально $HOME.",
        "Путать опцию и аргумент: в ls -l /etc флаг -l меняет поведение, /etc — объект операции.",
      ],
      whenNot: "Для ветвлений, циклов и обработки данных этого слоя недостаточно — дальше в карте есть уроки про скрипты и текстовую обработку. Но правила разбора и кавычек действуют везде без исключений.",
    },
    examples: [
      {
        title: "Анатомия команды",
        code: "ls -lah /etc                   # имя=ls, опции l/a/h, аргумент /etc\ncp --preserve=all a.txt b.txt  # длинная опция со значением",
        explanation: "-l подробный список, -a включая скрытые, -h человекочитаемые размеры. Склейка -lah равна трём отдельным опциям.",
      },
      {
        title: "Кавычки и подстановки",
        code: "user=\"$(whoami)\"\necho \"$user\"     # значение переменной\necho '$user'     # буквально $user\necho \"Сегодня $(date +%F)\"",
        explanation: "$(cmd) выполняется, $var читает переменную; двойные кавычки пропускают обе формы внутрь, одинарные — ничего.",
      },
      {
        title: "Репетиция разбора через echo",
        code: "file=\"мой отчёт.txt\"\necho cat $file     # видно два аргумента — беда\necho cat \"$file\"   # один аргумент — то, что нужно",
        explanation: "Приём на всю жизнь: сомневаешься, как разберётся команда — покажи её же через echo и посмотри на итоговые слова.",
      },
    ],
    practicePrompt: "Создай файл с пробелами в имени и прочитай его тремя способами: без кавычек, в двойных, в одинарных; зафиксируй, где и почему ломается.",
    checkPrompt: "Во что развернётся cp $src $dst при src=\"my docs\", и как написать эту строку правильно?",
    tier: 1,
    complexity: 2,
    references: ["Bash Reference Manual, разделы Expansion и Quoting", "man bash"],
  },
  {
    id: "linux-file-operations",
    title: "Операции с файлами и каталогами",
    summary: "Повседневный минимум: навигация, создание, копирование, перемещение, удаление и поиск файлов — то, чем состоит 90% работы в CLI.",
    concepts: ["cd", "ls", "cp", "mv", "rm", "mkdir", "touch", "find"],
    objectives: [
      "Свободно перемещаться абсолютными и относительными путями",
      "Выполнять полный цикл: создать, скопировать, переместить, переименовать, удалить",
      "Находить файлы по имени, размеру и времени изменения через find",
    ],
    lesson: {
      problem: "Без проводника работа с файлами кажется медленной — ровно до момента, когда надо переименовать двести файлов или найти лог среди тысяч. CLI проигрывает мышке в единичных кликах и разгромно выигрывает в массовости и повторяемости.",
      model: "У shell всегда есть текущий рабочий каталог — точка отсчёта относительных путей. Абсолютный путь начинается с /; .. — родитель, . — здесь, ~ — дом. mv — одновременно переименование и перенос: в обоих случаях меняется только запись в каталоге, данные не двигаются.",
      mechanics: "Базовый набор: pwd, cd -, ls -lhA, mkdir -p путь/из/нескольких/уровней, touch file (создать пустой или обновить время), cp -r для каталогов, mv старое новое, rm -ri для осторожного удаления. Поиск: find . -name '*.log' -mtime -7 — лог-файлы, менявшиеся за последние семь дней. У rm нет корзины: удаление окончательно.",
      pitfalls: [
        "rm -rf с переменной: при dir=\"\" команда rm -rf \"$dir\"/ развернётся в rm -rf /. Проверяй значения перед удалением.",
        "mv молча перезаписывает существующий файл: используй mv -i или сначала проверь цель.",
        "Шаблон find без кавычек: -name '*.log', иначе shell раскроет звёздочку сам и find получит мусор.",
      ],
      whenNot: "Когда критерии сложнее имени и размера (содержимое, права) — find остаётся инструментом, но в связке с другими утилитами (уроки про пайпы и текст). Для интерактивного блуждания по большим деревьям иногда честнее mc.",
    },
    examples: [
      {
        title: "Полный цикл операций",
        code: "mkdir -p ~/lab/{src,docs,backup}   # структура одной командой\ncd ~/lab/src && touch main.rs readme.md\ncp readme.md ../docs/\nmv ../docs/readme.md ../docs/intro.md\nls -R ~/lab",
        explanation: "Фигурные скобки разворачиваются в несколько путей (brace expansion): {src,docs} = src docs — экономит половину команд.",
      },
      {
        title: "Поиск через find",
        code: "find . -type f -name '*.log'     # файлы по маске\nfind /var/log -size +10M         # крупнее десяти мегабайт\nfind . -maxdepth 2 -type d       # каталоги не глубже двух уровней",
        explanation: "find идёт по дереву и проверяет условия; -type различает файлы и каталоги; размер и время — самые ходовые фильтры.",
      },
    ],
    practicePrompt: "Собери дерево проекта трёхуровневой mkdir -p, создай файлы, скопируй и переименуй их, затем найди find-ом всё, менявшееся за последние сутки.",
    checkPrompt: "Чем mv отличается от cp -r плюс rm, и почему mv внутри одного раздела мгновенен даже для гигабайтного файла?",
    tier: 1,
    complexity: 1,
    references: ["man find", "GNU Coreutils manual (cp, mv, rm, mkdir)"],
  },
  {
    id: "linux-permissions",
    title: "Права доступа к файлам",
    summary: "Читать и осознанно менять rwx-биты, чтобы лечить Permission denied точечно, а не chmod 777 по всей системе.",
    concepts: ["rwx", "owner/group/others", "chmod", "chown", "umask"],
    objectives: [
      "Разбирать вывод ls -l: тип, биты прав, владелец, группа",
      "Менять права символьным (u+x) и числовым (644) способом",
      "Диагностировать отказ доступа: какой тройке какого бита не хватило",
    ],
    lesson: {
      problem: "Permission denied — самая частая ошибка начинающего, а типовой «фикс» chmod 777 открывает файл вообще всем, превращая систему прав в декорацию. Понимание девяти битов переводит отказ из мистики в конкретный вопрос: владельцу, группе или остальным — чтения, записи или исполнения не хватило?",
      model: "У каждого файла три тройки rwx: владелец (u), группа (g), остальные (o). На файле x означает «можно запустить»; на каталоге семантика иная: r — видеть имена, w — создавать и удалять записи, x — входить внутрь и обращаться к содержимому. Процесс сравнивает свой UID с владельцем и движется по тройкам сверху вниз: совпал владелец — действует первая тройка, остальная математика игнорируется.",
      mechanics: "chmod u+x deploy.sh добавляет исполнение владельцу, g-w снимает запись с группы, o=r оставляет остальным только чтение. Числовой вид складывает биты r=4, w=2, x=1: 644 = rw-r--r--, 755 = rwxr-xr-x. Владельца и группу меняет chown user:group file. umask определяет, какие биты НЕ выдаются новым файлам: типичная 022 даёт файлы 644 и каталоги 755.",
      pitfalls: [
        "Лечить отказы chmod 777: механизм защиты стёрт, а настоящая причина (не тот пользователь, не тот процесс) осталась.",
        "Путать x на файле и на каталоге: исполняемость скрипта и возможность войти в каталог — разные биты.",
        "Искать причину только в битах: процесс может работать от другого пользователя — права смотрят глазами процесса, а не твоими.",
      ],
      whenNot: "chmod бессилен на файловых системах с фиксированными правами (например, смонтированный NTFS) и не заменяет ACL там, где нужно «этому пользователю можно, остальным нельзя» сверх одной группы.",
    },
    examples: [
      {
        title: "Читать вывод ls -l",
        code: "ls -l script.sh\n# -rwxr-x--- 1 vasya devs 220 авг 23 10:00 script.sh\n#   owner=rwx group=r-x others=---",
        explanation: "Первая колонка: '-' обычный файл, 'd' каталог, 'l' ссылка; далее три тройки битов и владелец/группа.",
      },
      {
        title: "Осознанный chmod",
        code: "chmod u+x deploy.sh     # владелец сможет запускать\nchmod 644 report.csv    # rw-r--r--: читают все, пишет владелец\nchmod g+w ~/shared      # группа может добавлять файлы в каталог",
        explanation: "Символьная форма меняет конкретные биты, числовая задаёт картину целиком; обе полезны — выбирай по задаче.",
      },
    ],
    practicePrompt: "Пройди chmod по шагам: создай файл, сними исполнение у всех, добавь себе, затем выстави 750; после каждого шага смотри ls -l и формулируй, что изменилось.",
    checkPrompt: "Ты владелец файла с правами 664, но echo x >> file отказано. Назови две причины вне битового поля и способ проверить каждую.",
    tier: 1,
    complexity: 2,
    references: ["man chmod", "The Linux Command Line — Shotts, глава о правах"],
  },
  {
    id: "linux-users-groups",
    title: "Пользователи и группы",
    summary: "Понять, кем система считает процесс: UID/GID, /etc/passwd и /etc/group — фундамент прав доступа и владельцев сервисов.",
    concepts: ["UID", "GID", "/etc/passwd", "/etc/group", "adduser", "su"],
    objectives: [
      "Соотносить имена пользователей и групп с числовыми UID/GID",
      "Читать семь полей /etc/passwd и записи /etc/group",
      "Создавать пользователей и включать их в группы штатными командами",
    ],
    lesson: {
      problem: "Права из прошлого урока отвечают на вопрос «кому можно», но «кому» — это конкретные числа. Когда сервис работает от www-data, а файл принадлежит тебе, конфликт виден только тому, кто понимает, как система хранит личность. Без этого понимания каждая вторая проблема прав выглядит случайностью.",
      model: "Ядро различает процессы по числам: UID и GID плюс список дополнительных групп. Имена — слой удобства: /etc/passwd отображает имя в UID, /etc/group — имя группы в GID со списком членов. Владелец файла хранится в inode как ЧИСЛО, поэтому удаление пользователя оставляет его файлы «сиротами», у которых вместо имени — голый номер.",
      mechanics: "id показывает свои UID, GID и все группы; whoami — только имя. Запись passwd: login:x:UID:GID:comment:home:shell — например www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin: системные пользователи имеют nologin вместо шелла — под них нельзя войти, но от них работают сервисы. Создание: adduser vasya (интерактивно) или useradd -m -s /bin/bash vasya. Включение в группу: usermod -aG docker vasya.",
      pitfalls: [
        "Править /etc/passwd руками: одна опечатка — и в систему не войдёт никто; для этого есть usermod и vipw с проверкой.",
        "Забыть -a в usermod -aG: без неё список дополнительных групп ЗАМЕНЯЕТСЯ, и ты вылетаешь из всех остальных групп.",
        "Ожидать мгновенного членства в группе: оно считывается при рождении процесса — текущие сессии надо перелогинить.",
      ],
      whenNot: "Для разовой смены личности в терминале есть su, для точечных повышений — sudo (следующий урок). Заводить себе постоянную работу под root «чтобы проще» — антипаттерн из обоих миров.",
    },
    examples: [
      {
        title: "Кто я для системы",
        code: "id          # uid=1000(vasya) gid=1000(vasya) groups=...,27(sudo),998(docker)\nwhoami      # только имя\ngroups      # имена всех групп",
        explanation: "Первый gid — основная группа; остальные — дополнительные. Именно их список проверяется против group-бита прав файлов.",
      },
      {
        title: "Разбор /etc/passwd",
        code: "grep -E '^(root|www-data)' /etc/passwd\n# root:x:0:0:root:/root:/bin/bash\n# www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin",
        explanation: "x значит «пароль лежит в /etc/shadow»; nologin запрещает вход, но не мешает процессам работать от имени пользователя.",
      },
      {
        title: "Добавить пользователя в группу",
        code: "sudo usermod -aG docker vasya\nid vasya     # проверить состав групп\n# выйти и зайти заново — новая группа попадёт в процессы сессии",
        explanation: "-aG = append to supplementary Groups. Классика: после добавления в docker-группу «не работает» только потому, что сессия старая.",
      },
    ],
    practicePrompt: "Создай пользователя developer, добавь его в свою основную группу, зайди под ним через su - developer и проверь id-ем, какие файлы твоего дома ему теперь видны и почему.",
    checkPrompt: "Почему после удаления пользователя его старые файлы показывают число вместо имени, и какой командой найти такие файлы?",
    tier: 1,
    complexity: 2,
    references: ["man 5 passwd", "man 8 usermod"],
  },
  {
    id: "linux-sudo-privileges",
    title: "sudo и управление привилегиями",
    summary: "Повышать привилегии точечно и осознанно: sudo, правила /etc/sudoers, разница с su — без постоянной жизни под root.",
    concepts: ["sudo", "root", "UID 0", "visudo", "NOPASSWD"],
    objectives: [
      "Выполнять одиночные команды от root через sudo",
      "Читать правила /etc/sudoers и понимать роль visudo",
      "Различать sudo, sudo -i и su - и выбирать подходящий сценарий",
    ],
    lesson: {
      problem: "Постоянная работа под root означает, что каждая опечатка получает максимальный радиус поражения, а система лишается аудита «кто это сделал». sudo позволяет быть обычным пользователем и подниматься до root ровно на одну команду — с записью факта в журнал.",
      model: "sudo — не «режим бога», а проверяемая сделка: механизм сверяет твою учётку и команду с правилами /etc/sudoers, при успехе запускает процесс с эффективным UID 0 и логирует событие. Root — это просто UID 0: ядро пропускает для него большинство проверок прав.",
      mechanics: "sudo команда — разовое повышение (пароль запоминается на несколько минут); sudo -l покажет твои разрешения; sudo -i открывает полноценную root-сессию с окружением root. Правила читаются так: %sudo ALL=(ALL:ALL) ALL — членам группы sudo можно всё; vasya ALL=(ALL) NOPASSWD: /usr/bin/apt — без пароля, но только apt. Редактировать файл ТОЛЬКО через visudo: он проверяет синтаксис до записи.",
      pitfalls: [
        "Править /etc/sudoers обычным редактором: синтаксическая ошибка отрезает возможность повышаться вообще — visudo существует именно поэтому.",
        "sudo echo text > /root/file не даёт root-овой записи: перенаправление выполняет ТВОЙ shell. Решение: echo text | sudo tee /root/file.",
        "Копировать NOPASSWD:ALL из интернета: это беспарольный root по определению.",
      ],
      whenNot: "sudo не предназначен для графических приложений и не заменяет продуманную модель прав: сервису с особыми доступами заводят собственного пользователя, а не выдают root.",
    },
    examples: [
      {
        title: "Точечное повышение",
        code: "sudo apt update     # одна команда под root\nsudo -l             # что мне разрешено\nsudo -i             # начать root-сессию; exit — вернуться",
        explanation: "Каждая sudo-команда попадает в журнал авторизаций — в отличие от постоянной root-сессии, след действий сохраняется.",
      },
      {
        title: "Запись в файл под root",
        code: "echo 'vm.swappiness=10' | sudo tee /etc/sysctl.d/99-swap.conf\nsudo tee -a /var/log/my.log <<< 'строка в конец файла'",
        explanation: "tee пишет файл ОТ ИМЕНИ root, потому что sudo действует на tee; твой shell лишь кормит его текстом на stdin.",
      },
    ],
    practicePrompt: "Посмотри sudo -l; выполни одну команду через sudo и одну внутри sudo -i; найди обе записи в журнале (journalctl _COMM=sudo или /var/log/auth.log).",
    checkPrompt: "Что окажется в файле после sudo echo secret > /etc/important.conf, почему — и как записать правильно?",
    tier: 1,
    complexity: 2,
    references: ["man 8 sudo", "man 5 sudoers"],
  },
  {
    id: "linux-processes",
    title: "Процессы: PID, fork/exec, дерево",
    summary: "Увидеть жизнь программы глазами системы: PID, родитель и потомок, fork/exec/wait, zombie и orphan — модель, через которую читается вся диагностика.",
    concepts: ["PID", "PPID", "fork", "exec", "wait", "zombie", "orphan"],
    objectives: [
      "Объяснять появление процесса цепочкой fork → exec → wait",
      "Читать дерево процессов через ps/pstree и /proc/<PID>",
      "Распознавать zombie и orphan по состоянию, а не по догадкам",
    ],
    lesson: {
      problem: "Жалобы «не запускается», «зависло», «осталось висеть после закрытия окна» описывают состояния жизненного цикла процесса. Без модели fork/exec/wait ты видишь симптомы и перезапускаешь наугад; с ней — читаешь систему как открытую книгу.",
      model: "Процесс появляется единственным способом: родитель вызывает fork (копия себя), ребёнок делает exec (загрузку новой программы), родитель собирает результат вызовом wait. У каждого процесса есть PID и PPID. Если ребёнок завершился, а родитель не вызвал wait — остаётся zombie: запись в таблице процессов. Если родитель умер раньше — ребёнок-сирота усыновляется init/systemd (PID 1), который дожимает wait за него.",
      mechanics: "ps --forest или pstree -p рисуют родство. Каталог /proc/<PID> содержит всё состояние процесса: cmdline, status (строка PPid), fd/. Переменная $$ — PID текущего shell, $! — PID последнего фонового задания. Пример: sleep 60 & ; ps -o pid,ppid,stat,cmd -p $! покажет родство и состояние (S — спит в ожидании события).",
      pitfalls: [
        "Убивать родителя ради «убить всё дерево»: дети не умирают автоматически — они становятся сиротами под опекой PID 1.",
        "Пытаться убить zombie: он уже завершён; таблицу чистит wait родителя — лечится завершением самого родителя.",
        "Считать наследование окружения двусторонним: ребёнок получает копию при рождении, изменения назад не передаются.",
      ],
      whenNot: "Эта модель описывает жизнь процессов, но не их устройство внутри: адресные пространства, планировщик и потоки — материал финального урока карты (процессы vs потоки) и дальнейших глубин.",
    },
    examples: [
      {
        title: "Родство процессов",
        code: "sleep 60 &\necho \"PID фонового задания: $!\"\nps -o pid,ppid,stat,cmd -p $!\npstree -p $$   # поддерево текущего shell",
        explanation: "$! — PID последнего фонового задания; STAT S = спит; PPid указывает на породивший shell.",
      },
      {
        title: "Паспорт процесса в /proc",
        code: "grep -E '^(Name|Pid|PPid|State)' /proc/$$/status",
        explanation: "/proc/<PID>/status — сырые данные, которыми пользуется сам ps: имя, состояние, родители.",
      },
    ],
    practicePrompt: "Запусти три фоновых sleep разной длительности, построй их дерево pstree -p, загляни в /proc/<pid>/status; затем закрой родительский shell и объясни по ps, кто усыновил детей.",
    checkPrompt: "В выводе ps процесс со STAT Z. Почему kill -9 ему бессилен и какие два действия решают проблему?",
    tier: 1,
    complexity: 2,
    references: ["The Linux Programming Interface — Kerrisk, гл. 24–26", "man 2 fork; man 2 wait"],
  },
  {
    id: "linux-process-management",
    title: "Управление процессами: ps, top, kill",
    summary: "Находить и останавливать процессы прицельно: поиск по имени и ресурсам, сигналы TERM и KILL, job control — вместо перезагрузки наугад.",
    concepts: ["ps", "top", "htop", "pgrep", "kill", "SIGTERM", "SIGKILL", "jobs"],
    objectives: [
      "Находить процессы по имени, пользователю и потреблению ресурсов",
      "Останавливать процессы сигналами, понимая разницу TERM и KILL",
      "Управлять заданиями shell: фон, приостановка, возврат",
    ],
    lesson: {
      problem: "Порт занят неизвестно кем, процесс съел память, тестовый сервер не останавливается. Типовой ответ новичка — перезагрузить машину; инженерный — найти виновника по доказательствам и отправить ему правильный сигнал.",
      model: "Сигнал — асинхронное уведомление процессу. SIGTERM (дефолтный kill) — вежливая просьба завершиться: приложение успевает сохранить данные и закрыть сокеты. SIGKILL — приказ ядра без права обжалования: процесс исчезает немедленно, без уборки. Отсюда дисциплина: сначала TERM, пауза, и только потом KILL.",
      mechanics: "Поиск: ps aux | grep nginx; pgrep -af python (имена и аргументы); top/htop — сортировка по CPU/MEM вживую. Действие: kill 12345 (TERM); kill -9 12345 (KILL — крайняя мера); pkill -f 'my-script.sh' по шаблону. Job control: cmd & — запустить в фоне; jobs — список; Ctrl+Z — приостановить текущее; bg — продолжить в фоне; fg %1 — вернуть наверх.",
      pitfalls: [
        "pkill по короткому имени убивает по подстроке: сначала pgrep -a — посмотри, КОГО собрался убить.",
        "kill -9 первым шагом: приложение теряет шанс сохранить данные; после KILL остаются занятые порты и битые файлы.",
        "Перепутать аргументы: kill принимает PID, pkill — шаблон имени; kill %1 в скрипте не сработает (%N живёт только в интерактивном shell).",
      ],
      whenNot: "Если процесс в состоянии D (непрерываемый сон — обычно ожидание диска/NFS), сигналы встают в очередь и не помогают: причина на уровне ввода-вывода, а не вежливости.",
    },
    examples: [
      {
        title: "Найти и остановить",
        code: "pgrep -af sleep              # кто такие sleep (имя + аргументы)\nkill 12345                   # вежливый SIGTERM\nsleep 1; ps -p 12345 || echo 'завершился сам'\nkill -9 12345 2>/dev/null    # KILL, только если пережил TERM",
        explanation: "Порядок TERM → проверка → KILL — стандартная дисциплина; её же реализуют systemd и оркестраторы контейнеров.",
      },
      {
        title: "Job control",
        code: "tail -f /var/log/syslog &   # запустить в фоне\njobs                        # задания текущего shell\nkill %1                     # завершить задание №1\nfg %1                       # вернуть в передний план",
        explanation: "%N — номер задания в этом shell, не PID; удобно интерактивно, но в скриптах оперируют PID.",
      },
    ],
    practicePrompt: "Запусти пару yes > /dev/null &, найди самого прожорливого в top, заверши сначала TERM-ом, убедись по ps; повтори с KILL и сравни поведение.",
    checkPrompt: "Процесс пережил TERM, но погиб от KILL. Что приложение не успело сделать во втором случае и почему для базы данных это критично?",
    tier: 1,
    complexity: 2,
    references: ["man 7 signal", "man 1 ps"],
  },
  {
    id: "linux-stdio",
    title: "Стандартный ввод, вывод и ошибки",
    summary: "Три потока каждого процесса и умение вести их раздельно: результат — в файл, ошибки — в журнал, шум — в /dev/null.",
    concepts: ["stdin", "stdout", "stderr", "перенаправление", "/dev/null"],
    objectives: [
      "Знать, что у процесса есть дескрипторы 0/1/2 и что это файловые дескрипторы",
      "Перенаправлять stdout и stderr раздельно и вместе",
      "Кормить команды через < и отсекать шум через /dev/null",
    ],
    lesson: {
      problem: "Сборка заливает экран кашей вперемешку с ошибками, а в файл попадает половина. Причина: stdout и stderr — два разных потока, и одиночный > ловит только первый. Пока потоки не различены, ты теряешь либо результат, либо ошибки — всегда что-то одно.",
      model: "Каждый процесс при рождении получает три открытых дескриптора: 0 stdin (вход), 1 stdout (результат), 2 stderr (диагностика). По умолчанию оба выходных смотрят в терминал, но это лишь договорённость: любой из трёх можно подключить к файлу, устройству или другой программе. Ошибки вынесены отдельно, чтобы результат оставался машинно-чистым.",
      mechanics: "> file — stdout в файл с перезаписью, >> — дозапись, < file — stdin из файла, 2> file — stderr отдельно, 2>&1 — «stderr туда же, куда смотрит stdout». >/dev/null 2>&1 выбрасывает всё. /dev/null — чёрная дыра: чтение даёт конец файла, запись исчезает. wc -l < file считает строки без передачи имени аргументом.",
      pitfalls: [
        "Порядок имеет значение: cmd >out 2>&1 верно, а cmd 2>&1 >out отправит stderr в терминал — копирование происходит в момент применения.",
        "> обнуляет файл ДО запуска команды: grep pattern big.log > big.log уничтожит данные.",
        "Ожидать stderr в пайпе: | несёт только stdout — ошибки пролетят мимо следующей стадии.",
      ],
      whenNot: "Привычное 2>/dev/null прячет настоящие сбои. Гаси им известный шум (например, permission denied при find по чужим каталогам), а не всё подряд.",
    },
    examples: [
      {
        title: "Развести потоки по файлам",
        code: "ls /etc/passwd /nope > out.txt 2> err.txt\ncat out.txt    # только успешный вывод\ncat err.txt    # только сообщение об ошибке\nls /etc/passwd /nope > all.txt 2>&1   # всё вместе",
        explanation: "Одна команда — два потока; теперь результат идёт в обработку, ошибки — в журнал, независимо друг от друга.",
      },
      {
        title: "Тишина и кормление",
        code: "find /etc -name '*.conf' 2>/dev/null | head\nwc -l < /etc/passwd\nsort < words.txt > sorted.txt",
        explanation: "2>/dev/null гасит ожидаемый шум прав; < подставляет данные на вход, освобождая аргументы для других целей.",
      },
    ],
    practicePrompt: "Возьми ls с существующим и несуществующим путём: разведи stdout и stderr в разные файлы, затем сведи в один; проверь содержимое каждого варианта.",
    checkPrompt: "Чем отличаются script >log 2>&1 и script 2>&1 >log — куда попадёт ошибка в каждом случае?",
    tier: 1,
    complexity: 2,
    references: ["man bash, раздел REDIRECTION", "TLPI — глава о файловых дескрипторах"],
  },
  {
    id: "linux-pipes-redirection",
    title: "Конвейеры: пайпы, tee, xargs",
    summary: "Собирать маленькие команды в конвейеры параллельных процессов и не терять ошибки и статусы вдоль цепочки.",
    concepts: ["pipe", "tee", "xargs", "process substitution", "PIPESTATUS"],
    objectives: [
      "Строить конвейеры из 3–4 стадий и видеть поток данных между процессами",
      "Делить поток через tee и превращать строки в аргументы через xargs",
      "Знать, чей exit code получает $? после пайпа, и как поймать падение середины",
    ],
    lesson: {
      problem: "Одна утилита редко решает задачу целиком: выбрать строки, извлечь поле, посчитать, отсортировать. Философия Unix — маленькие программы, каждая отлично делает одно дело, соединённые конвейером. Но без понимания механики конвейер молча теряет ошибки и статусы — и диагностика начинает лгать.",
      model: "Пайп — не «файлик между командами», а буфер ядра, соединяющий stdout одного процесса со stdin другого. Стадии работают ОДНОВРЕМЕННО, данные текут без промежуточных файлов. Exit status пайпа — статус последней стадии, поэтому падение середины по умолчанию невидимо; в bash его ловят через PIPESTATUS или set -o pipefail.",
      mechanics: "Классическая цепочка: grep error app.log | awk '{print $3}' | sort | uniq -c | sort -rn | head. tee file делит поток: копия в файл, продолжение дальше (часто sudo tee для записи под root). xargs превращает строки stdin в аргументы: cat urls.txt | xargs -n1 curl -O. Process substitution: diff <(sort a) <(sort b) сравнивает отсортированные версии без временных файлов.",
      pitfalls: [
        "Верить $? после пайпа: он отражает только последнюю команду; в скриптах включай set -o pipefail.",
        "cat file | grep x — бесполезный cat: grep читает файл сама; пайп оправдан, когда источник действительно поток.",
        "Подставлять сотни имён файлов как аргументы напрямую (rm $(find ...)): переполнение и катастрофа на опечатке; для этого существует xargs.",
      ],
      whenNot: "Конвейеры свыше пяти-шести стадий перестают читаться: если логика ветвится — это уже скрипт (урок 13). Конвейер — для линейной обработки потока, а не для бизнес-логики.",
    },
    examples: [
      {
        title: "Топ самых частых команд из истории",
        code: "history | awk '{print $2}' | sort | uniq -c | sort -rn | head\n#   210 git\n#    84 cd\n#    51 ls",
        explanation: "Читай слева направо: взять второе слово каждой строки → отсортировать → сгруппировать с подсчётом → упорядочить по числу → топ-10.",
      },
      {
        title: "tee и xargs",
        code: "make 2>&1 | tee build.log | grep -i error\n# весь лог в файле, ошибки — на экран\nfind . -name '*.tmp' -print0 | xargs -0 rm -v",
        explanation: "tee ставит тройник в поток; пара -print0/-0 делает конвейер устойчивым к пробелам в именах файлов.",
      },
    ],
    practicePrompt: "Построй конвейер фильтр → поле → агрегат → топ-5 поверх любого лога; сохрани полную копию потока через tee; намеренно сломай среднюю стадию и проследи за exit code.",
    checkPrompt: "Почему падение первой стадии не меняет $? пайпа и что нужно включить в bash, чтобы меняло?",
    tier: 1,
    complexity: 2,
    references: ["man bash (PIPESTATUS, process substitution)", "POSIX Shell Command Language"],
  },
  {
    id: "linux-text-processing",
    title: "Обработка текста: grep, sed, awk",
    summary: "grep выбирает строки, sed заменяет, awk разбирает поля: минимальный набор, покрывающий большую часть работы с логами и конфигами.",
    concepts: ["grep", "sed", "awk", "regex", "sort", "uniq", "cut", "tr"],
    objectives: [
      "Выбирать строки grep-ом: по подстроке, regex, с инверсией и рекурсией",
      "Точечно править текст sed-ом и извлекать поля awk/cut",
      "Собирать агрегаты связкой sort|uniq и знать границу применимости regex",
    ],
    lesson: {
      problem: "Логи на сотни мегабайт, конфиг в тысячу строк, CSV без заголовков — глазами не читаются. Текстовые утилиты превращают их в выборки и отчёты за секунды; в отличие от «открыл в редакторе» — повторяемо и автоматизируемо.",
      model: "Разделение труда: grep — фильтр строк (выбор), sed — потоковый редактор (замена и удаление по адресу), awk — обработчик записей и полей (колоночная логика). Простые случаи не заслуживают regex: cut режет по разделителю, tr перекодирует символы, sort+uniq считают и схлопывают дубли.",
      mechanics: "grep -i снимает чувствительность к регистру, -v инвертирует, -n даёт номера строк, -r ищет по каталогу, -E включает расширенный regex. sed 's/старое/новое/g' file — замена всех вхождений; -i правит файл на месте (без бэкапа!). awk -F: '{print $1, $7}' /etc/passwd — первая и седьмая колонки по разделителю двоеточие. Агрегат: sort | uniq -c | sort -rn.",
      pitfalls: [
        "Жадный regex .* захватывает больше задуманного: для содержимого кавычек бери '\"[^\"]*\"', а не '\".*\"'.",
        "sed -i различается между GNU (Linux) и BSD (macOS): у BSD требует аргумент -i ''. Переносные скрипты ломаются именно тут.",
        "Разбирать структурированные форматы (JSON, YAML) регулярками хрупко: есть jq — используй его; awk оставь табличному тексту.",
      ],
      whenNot: "Когда источник имеет стабильный структурный формат, бери специализированный парсер: jq для JSON, xmllint для XML. Regex-обработка — для человеческого текста и разовых выборок.",
    },
    examples: [
      {
        title: "Отчёт по веб-логу",
        code: "grep ' 500 ' access.log \\\n  | awk '{print $1}' \\\n  | sort | uniq -c | sort -rn | head\n#  47 203.0.113.7\n#  12 198.51.100.2",
        explanation: "Выбрать строки с HTTP 500 → взять IP (первое поле) → посчитать уникальные → топ. Секунды на миллионах строк.",
      },
      {
        title: "Точечная правка и простые выборки",
        code: "sed 's/^#*PermitRootLogin.*/PermitRootLogin no/' sshd_config\ncut -d: -f1,7 /etc/passwd | tail -5\ntr '[:upper:]' '[:lower:]' < mixed.txt",
        explanation: "sed правит строку независимо от комментариев перед ней; cut и tr решают мелочи вообще без regex.",
      },
    ],
    practicePrompt: "На любом многострочном логе собери топ-5 частых значений выбранного поля, затем sed-ом замаскируй IP-адреса на X.X.X.X и сохрани отчёт в новый файл.",
    checkPrompt: "Нужно достать значение timeout из JSON-конфига. Почему связка grep+awk — плохой план и что использовать вместо?",
    tier: 1,
    complexity: 2,
    references: ["man grep; man sed; man awk", "GNU manuals (grep, sed, gawk)"],
  },
  {
    id: "linux-shell-scripting",
    title: "Скрипты на shell",
    summary: "Превратить последовательность команд в надёжный инструмент: shebang, переменные, условия, циклы, функции и строгий режим set -euo pipefail.",
    concepts: ["shebang", "set -euo pipefail", "if/for/while", "функции", "$@", "exit code"],
    objectives: [
      "Оформлять исполняемый скрипт: shebang, chmod +x, корректный запуск",
      "Писать ветвления, циклы и функции без классических дыр с кавычками",
      "Включать строгий режим и обрабатывать ошибки вместо игнорирования",
    ],
    lesson: {
      problem: "Скрипт из рабочих команд живёт до первого нестандартного случая: пустая переменная, упавшая команда в середине, имя файла с пробелом. Разница между наброском и инструментом — несколько дисциплин: строгий режим, кавычки вокруг всего, явные проверки входа.",
      model: "Скрипт — та же интерактивная сессия shell, записанная в файл: выполняется сверху вниз. set -e останавливает выполнение при первой ошибке, -u ругается на неопределённые переменные, -o pipefail заставляет пайп возвращать статус упавшей стадии. Вместе set -euo pipefail — стартовый дефолт любого серьёзного скрипта.",
      mechanics: "Шапка #!/usr/bin/env bash выбирает интерпретатор; chmod +x делает файл исполняемым; запуск ./script.sh args. Условия: if [[ -f \"$file\" ]]; then ... fi; проверки -f/-d/-z/-eq. Циклы: for f in *.log; do ... done или while read -r line; do ... done < input. Функция: log() { echo \"[$(date +%T)] $*\" >&2; }. Выход: exit 0 — успех, ненулевой код — ошибка.",
      pitfalls: [
        "[ $x = y ] без кавычек: при пустом x — синтаксическая ошибка. Только [[ ... ]] и \"$vars\".",
        "Скрипт, сохранённый на Windows с CRLF: bash видит \\r в конце строк и выдаёт загадочные ошибки; лечит dos2unix.",
        "Игнорировать статусы команд: без set -e скрипт радостно продолжит работу с испорченным состоянием после падения середины.",
      ],
      whenNot: "Скрипты за пару сотен строк или со сложными структурами данных — сигнал брать Python или Go. Shell силён в склейке процессов и слаб в логике и типах.",
    },
    examples: [
      {
        title: "Каркас надёжного скрипта",
        code: "#!/usr/bin/env bash\nset -euo pipefail\n\nusage() { echo \"usage: $0 <source-dir> <dest-dir>\" >&2; exit 2; }\n[[ $# -eq 2 ]] || usage\nsrc=\"$1\"; dst=\"$2\"\n[[ -d \"$src\" ]] || { echo \"нет каталога: $src\" >&2; exit 1; }\n\nlog() { echo \"[$(date +%H:%M:%S)] $*\" >&2; }\nlog \"архивирую $src -> $dst\"\ntar -czf \"$dst/backup-$(date +%F).tar.gz\" -C \"$src\" .\nlog \"готово\"",
        explanation: "Логи идут в stderr (&2), чтобы stdout остался чистым для данных; tar -C архивирует содержимое без абсолютных путей внутри архива.",
      },
      {
        title: "Безопасное чтение строк",
        code: "while IFS= read -r line; do\n  process_one \"$line\"\ndone < list.txt",
        explanation: "IFS= сохраняет пробелы по краям, -r отключает раскрытие backslash-последовательностей, кавычки защищают от разбиения — канонический безопасный read.",
      },
    ],
    practicePrompt: "Напиши cleanup.sh с set -euo pipefail: принимает каталог и число дней, в dry-run печатает find-ом файлы старше N дней, ведёт лог функцией в stderr и проверяет аргументы.",
    checkPrompt: "Почему grep foo file || true спасает скрипт с set -e, а голый grep — нет? В каких случаях какое поведение правильное?",
    tier: 1,
    complexity: 3,
    references: ["Bash Reference Manual", "ShellCheck / Google Shell Style Guide"],
  },
  {
    id: "linux-env-vars",
    title: "Переменные окружения",
    summary: "Environment как контракт между процессами: наследование, export, PATH/HOME/LANG — и почему «у меня работало» зависит от окружения.",
    concepts: ["environment", "export", "PATH", "HOME", "LANG", "printenv"],
    objectives: [
      "Различать переменные shell и экспортированные переменные окружения",
      "Смотреть и задавать окружение: printenv, env, одноразовые префиксы",
      "Объяснять наследование: откуда ребёнок берёт env и почему обратно не передаёт",
    ],
    lesson: {
      problem: "Команда работает в терминале и падает в cron, CI или systemd. Почти всегда причина — окружение: интерактивный shell носит богатый набор переменных, сервис получает почти пустой. Не понимая механизма, ты лечишь симптомы перезапусками.",
      model: "Environment — таблица строк KEY=value, которую процесс получает КОПИЕЙ от родителя при рождении. Дальше каждый меняет только свою копию: экспорт внутри скрипта не вернётся вызвавшему shell. export помечает переменную как передаваемую детям; без export она видна только самому shell. Одноразовая форма VAR=x cmd задаёт переменную только для этой команды.",
      mechanics: "printenv — всё окружение; printenv HOME — одно значение. Ключевые переменные: PATH (поиск команд — следующий урок), HOME, USER, SHELL, LANG (локаль), EDITOR. Наследование проверяется элементарно: bash -c 'echo $MY_VAR' видит MY_VAR только при экспорте. Файлы инициализации: ~/.bashrc для интерактивных shell, ~/.profile для входа — правки действуют в НОВЫХ сессиях или после source.",
      pitfalls: [
        "Присвоить без export и ждать, что увидит запускаемая программа: MY_VAR=1; ./app — app не увидит ничего.",
        "Ждать, что source ~/.bashrc в одном окне изменит другие терминалы: окружение живёт в каждом процессе отдельно.",
        "Пытаться «экспортировать наверх»: скрипт физически не может изменить переменные родителя — только напечатать то, что родитель выполнит сам.",
      ],
      whenNot: "Environment — не хранилище секретов: значения видны в /proc/<pid>/environ и утекают во все дочерние процессы. И это плоский список строк, а не структура данных.",
    },
    examples: [
      {
        title: "Три состояния переменной",
        code: "MY_VAR=hello                  # локальная: только этот shell\nbash -c 'echo $MY_VAR'        # пусто — не экспортирована\nexport MY_VAR\nbash -c 'echo $MY_VAR'        # hello — попала в окружение\nONE_SHOT=42 bash -c 'echo $ONE_SHOT'   # 42 — только для этой команды",
        explanation: "Локальная, экспортированная, одноразовая. Ребёнок получает копию в момент запуска — и больше ничего.",
      },
      {
        title: "Окружение как конфигурация",
        code: "LANG=C ls --help | head -3     # вывод на английском\nprintenv | grep -i proxy       # программы читают настройки из env\nenv -i bash -c 'printenv'      # почти пустое окружение",
        explanation: "env -i показывает, сколько «магии» даёт окружение интерактивной сессии — ровно эта разница кусает при переходе в systemd или cron.",
      },
    ],
    practicePrompt: "Продемонстрируй три состояния переменной через bash -c; затем запусти знакомую команду через env -i и опиши, что перестало работать и почему.",
    checkPrompt: "Скрипт печатает export NEW_VAR=1, надеясь изменить окружение вызывающего shell. Почему это не сработает и как правильно передать значение наверх?",
    tier: 1,
    complexity: 2,
    references: ["man 7 environ", "Bash Reference Manual (export, env)"],
  },
  {
    id: "linux-path-lookup",
    title: "Поиск команд и PATH",
    summary: "Откуда shell берёт команды: обход PATH слева направо, builtins, кэш hash — диагностика command not found и «запускается не та версия».",
    concepts: ["PATH", "which", "type", "command -v", "hash", "builtin"],
    objectives: [
      "Объяснять, как shell находит исполняемый файл для введённого имени",
      "Диагностировать command not found и конфликт версий инструмента",
      "Безопасно расширять PATH собственными каталогами",
    ],
    lesson: {
      problem: "Файл точно существует, но shell отвечает command not found; или python запускается не тот. Обе тайны решаются одним знанием: shell не сканирует диск, а идёт по списку каталогов PATH слева направо и берёт ПЕРВОЕ совпадение.",
      model: "Порядок разрешения имени: alias → функция → builtin (встроенная в сам shell: cd, echo) → обход каталогов $PATH. Текущий каталог в PATH сознательно не входит: ./script пишут явно — так shell не подсовывает тебе поддельную команду из чужого каталога.",
      mechanics: "type -a python покажет ВСЕ варианты в порядке приоритета; which cmd — первый из PATH; command -v cmd — переносимый способ, работающий и для builtin. Расширение пути: export PATH=\"$HOME/bin:$PATH\" ставит свои каталоги вперёд. hash — кэш найденных путей: после переустановки бинарника сбрасывается hash -r.",
      pitfalls: [
        "PATH=/opt/new/bin вместо PATH=$PATH:/opt/new/bin: стёр весь прежний список — отвалилась половина команд.",
        "После обновления бинарника shell продолжает запускать старый путь из кэша: лечится hash -r или новым shell.",
        "Две версии инструмента в разных каталогах: побеждает стоящий левее; расклад показывает type -a.",
      ],
      whenNot: "PATH ищет только исполняемые файлы по имени. Поиск любых файлов по свойствам и содержимому — территория find (урок 4) и grep (урок 12).",
    },
    examples: [
      {
        title: "Кто на самом деле выполнится",
        code: "type -a python3     # все кандидаты по приоритету\nwhich -a node       # все совпадения в PATH\ncommand -v cd       # cd — builtin, внешнего файла нет",
        explanation: "Первое совпадение выигрывает. Любая диагностика «не той версии» начинается с type -a.",
      },
      {
        title: "Добавить свои инструменты",
        code: "mkdir -p ~/bin && cp mytool ~/bin/\necho 'export PATH=\"$HOME/bin:$PATH\"' >> ~/.bashrc\nsource ~/.bashrc && type -a mytool",
        explanation: "~/bin добавлен ПЕРЕД системными путями: твои версии имеют приоритет, системные остаются доступны.",
      },
    ],
    practicePrompt: "Положи скрипт hello в ~/bin, добейся запуска по имени из любого каталога; создай одноимённый в каталоге, стоящем в PATH раньше, и объясни по type -a, кто победил.",
    checkPrompt: "После обновления пакета продолжает работать старая версия утилиты. Назови две вероятные причины и команду проверки для каждой.",
    tier: 1,
    complexity: 2,
    references: ["man bash (COMMAND EXECUTION)", "man 1 which"],
  },
  {
    id: "linux-man-pages",
    title: "Man-страницы и документация",
    summary: "Добывать ответы из самой системы: структура man-страниц, секции, поиск — чтобы зависеть от документации своей версии, а не чужого блога.",
    concepts: ["man", "apropos", "--help", "секции", "/usr/share/doc"],
    objectives: [
      "Читать man-страницы, понимая структуру SYNOPSIS/DESCRIPTION/OPTIONS",
      "Различать секции и запрашивать нужную: man 5 passwd против man 1 passwd",
      "Искать по ключевым словам через apropos/man -k",
    ],
    lesson: {
      problem: "Ответ из блога 2014 года против man-страницы твоей фактической версии — доверять стоит второму, особенно когда флаги различаются между системами (sed -i у BSD и GNU). Умение читать встроенную документацию превращает незнакомую команду в понятную за пять минут без интернета.",
      model: "Man-страница — справочник, а не учебник: коротко и точно. Секции пронумерованы: 1 — пользовательские команды, 5 — форматы файлов, 8 — администрирование, 2/3 — системные и библиотечные вызовы. Одно имя может жить в нескольких секциях: man passwd покажет команду, man 5 passwd — формат файла /etc/passwd.",
      mechanics: "man ls открывает страницу в пейджере less: /pattern — поиск вперёд, n — следующее совпадение, q — выход. SYNOPSIS задаёт грамматику: [опции] необязательны, <arg> обязателен, ... значит повторяемость. apropos network или man -k network ищут по описаниям всех установленных страниц. Быстрая шпаргалка — cmd --help; длинная документация пакетов лежит в /usr/share/doc/<package>.",
      pitfalls: [
        "Пытаться прочитать man bash целиком: это около тысячи страниц; ищи раздел прямо в пейджере, например /^REDIRECTION.",
        "Закрывать man через Ctrl+C: пейджер остаётся висеть; правильный выход — клавиша q.",
        "Игнорировать номер секции: man printf и man 3 printf рассказывают про разные вещи — builtin shell против функции C.",
      ],
      whenNot: "man описывает систему и её утилиты; для API языков и фреймворков ищи их собственную документацию. tldr-выкладки хороши для напоминания синтаксиса, но первоисточник фактов — man.",
    },
    examples: [
      {
        title: "Навигация внутри страницы",
        code: "man rsync\n# внутри пейджера:\n#   /recursive   — найти слово\n#   n            — следующее совпадение\n#   q            — выйти\nman 5 crontab     # формат файла crontab, а не команда crontab",
        explanation: "Поиск по странице быстрее прокрутки; указание секции спасает от чтения не той страницы.",
      },
      {
        title: "Найти то, имени чего не знаешь",
        code: "apropos compress          # какие утилиты жмут данные?\nman -k 'regular expression'\nls --help | head -20      # шпаргалка без полного man",
        explanation: "apropos ищет по кратким описаниям всех страниц — стартовая точка, когда неизвестно даже имя команды.",
      },
    ],
    practicePrompt: "В man rsync найди опцию --partial через поиск и выпиши её форму из SYNOPSIS; затем через apropos открой две незнакомые утилиты и резюмируй каждую одной строкой.",
    checkPrompt: "Чем man 5 passwd отличается от man 1 passwd, и как заранее узнать, в каких секциях живёт интересующее тебя имя?",
    tier: 1,
    complexity: 1,
    references: ["man man", "man-pages(7) — соглашения проекта Linux man-pages"],
  },
  {
    id: "linux-package-management",
    title: "Управление пакетами: общая модель",
    summary: "За разными менеджерами одна модель: репозитории, зависимости, база установленного — основа воспроизводимого и безопасного состояния хоста.",
    concepts: ["package manager", "repository", "dependency", ".deb", ".rpm"],
    objectives: [
      "Объяснять, что дают репозитории по сравнению с ручной установкой софта",
      "Различать низкоуровневые инструменты (dpkg, rpm) и фронтенды (apt, dnf)",
      "Аудитить: что установлено, какому пакету принадлежит файл, откуда пакет",
    ],
    lesson: {
      problem: "Софт, скачанный с сайтов, никто не обновляет: дыры безопасности остаются навсегда, зависимости конфликтуют, начисто удалить невозможно. Пакетный менеджер превращает установку в транзакцию с метаданными, подписанными репозиториями и историей изменений.",
      model: "Модель едина для всех семейств: репозиторий (каталог пакетов с подписанными метаданными) → менеджер разрешает зависимости → устанавливает и записывает в локальную базу. Низкоуровневый слой (dpkg, rpm) работает с одиночным файлом пакета; фронтенд (apt, dnf) знает репозитории и умеет договариваться о зависимостях. Всё, что ставится мимо менеджера, — технический долг хоста.",
      mechanics: "Глаголы одинаковы в обоих мирах: поиск (apt search / dnf search), информация (apt show / dnf info), установка, удаление, обновление индексов и пакетов. Обратные вопросы: «чья это программа?» — dpkg -S путь или rpm -qf путь; «что внутри пакета?» — dpkg -L pkg или rpm -ql pkg.",
      pitfalls: [
        "curl ... | sudo bash из случайного места: ты дал root и доверился непроверенному коду; предпочитай официальные подписанные репозитории.",
        "Ставить пакеты pip/npm поверх системных без изоляции: версии сталкиваются с менеджером пакетов; используй виртуальные окружения.",
        "Забывать обновления безопасности: неподновлённый хост — самый дешёвый эксплойт; для этого существуют unattended-upgrades.",
      ],
      whenNot: "Контейнеры и языковые менеджеры закрывают свою часть спектра, но хост всё равно живёт на пакетах: ядро, openssl и sshd приходят именно оттуда.",
    },
    examples: [
      {
        title: "Одинаковые глаголы в обоих мирах",
        code: "apt search 'http server'    # мир Debian/Ubuntu\ndnf search 'http server'    # мир Fedora/RHEL\napt show nginx; dnf info nginx",
        explanation: "Синтаксис различается, но модель «индексы → разрешение зависимостей → транзакция» одна.",
      },
      {
        title: "Откуда этот файл?",
        code: "dpkg -S /usr/bin/curl    # Debian-мир: чей файл\nrpm -qf /usr/bin/curl    # RPM-мир: то же самое\ndpkg -L curl | head      # состав пакета",
        explanation: "Обратные запросы — база аудита: понять, что за бинарник перед тобой и чем его обновлять.",
      },
    ],
    practicePrompt: "В своей среде выясни число установленных пакетов, владельца /usr/bin/env и три конфигурационных файла его пакета; продублируй команды для второго мира (deb/rpm).",
    checkPrompt: "Коллега ставит инструменты «бинарником с сайта». Какие три проблемы проявятся на хосте в течение года?",
    tier: 1,
    complexity: 2,
    references: ["Debian Administrator's Handbook, гл. 6", "Red Hat documentation (package management)"],
  },
  {
    id: "linux-apt-debian",
    title: "APT и экосистема Debian",
    summary: "Рабочий цикл apt: update перед установкой, upgrade против full-upgrade, purge против remove, аудит состояния через dpkg.",
    concepts: ["apt", "dpkg", ".deb", "sources.list", "purge", "autoremove"],
    objectives: [
      "Правильно выполнять цикл обновления: apt update → upgrade",
      "Управлять пакетами: install/remove/purge/search/show/hold",
      "Аудитить состояние: dpkg -l/-L/-S и источники пакетов",
    ],
    lesson: {
      problem: "apt install падает с has no installation candidate или молча ставит древнюю версию — потому что индексы давно не обновлялись. А remove, оказавшийся нечистым, оставляет конфиги, которые всплывают через год. Лечится пониманием двухслойной модели: apt принимает решения, dpkg исполняет.",
      model: "sources.list (и каталог sources.list.d/) говорит, ОТКУДА брать пакеты. apt update обновляет локальные ИНДЕКСЫ — ничего не устанавливая; apt upgrade применяет новые версии согласно свежим индексам. remove убирает бинарники, purge — ещё и системные конфиги. dpkg работает только с .deb-файлами и базой: сам в сеть не ходит и зависимости не разрешает.",
      mechanics: "Здоровый цикл: sudo apt update && apt list --upgradable && sudo apt upgrade. Разведка: apt show nginx — версия, зависимости, размер; apt-cache policy nginx — какие версии откуда доступны. Заморозка: sudo apt-mark hold nginx. Аудит: dpkg -l | grep nginx; dpkg -L nginx; dpkg -S /etc/nginx/nginx.conf.",
      pitfalls: [
        "Забыть apt update перед install: кандидат на установку устарел или отсутствует.",
        "Не различать upgrade и full-upgrade: второй может УДАЛЯТЬ пакеты ради согласования зависимостей — читай сводку перед Enter.",
        "Использовать remove там, где нужен purge: конфиги остаются; список недоудалённых виден как статус rc в dpkg -l.",
      ],
      whenNot: "Не смешивай apt с ручным dpkg -i: dpkg не подтянет зависимости, и база останется в полусломанном состоянии (лечится apt -f install, но лучше туда не попадать).",
    },
    examples: [
      {
        title: "Здоровый цикл обновления",
        code: "sudo apt update\napt list --upgradable\nsudo apt upgrade\napt-cache policy nginx   # какие версии доступны и откуда",
        explanation: "policy показывает candidate — версию, которую поставит install; полезно смотреть перед установкой спорных пакетов.",
      },
      {
        title: "Аудит и чистота базы",
        code: "dpkg -l | awk '/^rc/{print $2}'          # пакеты с остатками конфигов\nsudo apt purge $(dpkg -l | awk '/^rc/{print $2}')\nsudo apt autoremove                       # сироты-зависимости",
        explanation: "Статус rc = removed + Config-files остались; регулярная чистка держит базу честной.",
      },
    ],
    practicePrompt: "Проведи полный цикл: update → list --upgradable → установи небольшой пакет → изучи apt show и dpkg -L → удали purge-ом → найди остатки в dpkg -l.",
    checkPrompt: "Почему apt install без предварительного update может поставить уязвимую версию, хотя исправленная уже лежит в репозитории?",
    tier: 1,
    complexity: 2,
    references: ["man apt", "Debian Administrator's Handbook, гл. 6"],
  },
  {
    id: "linux-rpm-dnf",
    title: "RPM и DNF: второй большой мир",
    summary: "dnf как умный фронтенд, rpm как низкий уровень и база установленного: история транзакций, поиск файлов по репозиториям, откат.",
    concepts: ["dnf", "yum", "rpm", ".rpm", "dnf history"],
    objectives: [
      "Выполнять жизненный цикл пакета в RPM-мире: search/install/remove/update",
      "Аудитить через rpm: -q/-qi/-ql/-qf",
      "Использовать dnf history для анализа и отката изменений",
    ],
    lesson: {
      problem: "Инструкции для Fedora/RHEL пишут yum, rpm -ivh или dnf вперемешку; новичок смешивает уровни и получает ад недостающих зависимостей после ручной установки rpm. Граница та же, что между dpkg и apt: rpm не ходит в сеть за зависимостями, dnf — ходит.",
      model: "rpm — формат пакета, низкоуровневый инструмент и база данных установленного (/var/lib/rpm). dnf (наследник yum) — фронтенд: репозитории в /etc/yum.repos.d/, разрешение зависимостей, транзакции. Каждое действие записывается в dnf history с возможностью undo. На современных дистрибутивах команда dnf; yum оставлен как алиас.",
      mechanics: "dnf search/install/remove/update/info nginx. Поиск по содержимому: dnf provides '*/ifconfig' — какой пакет даёт этот файл, причём ищет и среди НЕустановленных пакетов репозиториев. Аудит: rpm -q nginx — версия; rpm -qi nginx — подробности; rpm -ql nginx — файлы; rpm -qf /usr/bin/ss — чей файл. История: dnf history list, dnf history info <id>, dnf history undo <id>.",
      pitfalls: [
        "Ставить пакет командой rpm -ivh file.rpm вручную: зависимости разруливать придётся самому; правильно — dnf install ./file.rpm, он докачает недостающее.",
        "Смешивать сторонние репозитории без приоритетов: версии начинают перебивать друг друга; настраивай exclude/priority.",
        "Считать dnf history undo серебряной пулей: изменения конфигурационных файлов при откате не гарантированы.",
      ],
      whenNot: "dnf не управляет контейнерными образами и языковыми зависимостями — у тех свои менеджеры. Но принцип «всё через транзакции с историей» стоит переносить и туда.",
    },
    examples: [
      {
        title: "Кто даёт этот файл?",
        code: "dnf provides '*/ss'\nrpm -qf /usr/sbin/ss\nrpm -qi iproute | head -6",
        explanation: "provides ищет даже среди неустановленных пакетов — главный инструмент вопроса «как называется пакет с утилитой X».",
      },
      {
        title: "История транзакций",
        code: "dnf history list | head\ndnf history info last\ndnf history undo last-1    # откатить конкретную транзакцию (внимательно)",
        explanation: "Каждая транзакция имеет id; undo позволяет отыграть изменения назад — того нет в apt-мире из коробки.",
      },
    ],
    practicePrompt: "В Fedora/Rocky-контейнере: найди пакет с ifconfig через dnf provides, установи его, проверь владельца через rpm -qf, затем отмени установку через dnf history undo.",
    checkPrompt: "Чем dnf install ./tool.rpm принципиально лучше rpm -ivh tool.rpm при недостающих зависимостях — и что произойдёт в каждом случае?",
    tier: 1,
    complexity: 2,
    references: ["man dnf", "man rpm; Red Hat documentation"],
  },
  {
    id: "linux-processes-vs-threads",
    title: "Процессы vs потоки",
    summary: "Финальная рамка concurrency на Linux: чем поток отличается от процесса, где каждый живёт в реальных сервисах и что показывают ulimit и /proc/*/task.",
    concepts: ["thread", "clone", "LWP", "race condition", "ulimit", "адресное пространство"],
    objectives: [
      "Объяснять разницу: изоляция и цена fork против общая память и риски потоков",
      "Видеть потоки в системе: ps -eLf, /proc/<pid>/task, htop",
      "Связывать модель с реальностью: воркеры nginx против потоков JVM и лимиты ядра",
    ],
    lesson: {
      problem: "Документация сервисов говорит worker processes или thread pool, а sysctl и ulimit пестрят лимитами на процессы и потоки. Без модели непонятно, почему nginx масштабируется процессами, Java — потоками, и почему нельзя убить один поток отдельно.",
      model: "Процесс — контейнер ресурсов: своё адресное пространство, дескрипторы, идентичность. Поток — ветка исполнения ВНУТРИ процесса: память и ресурсы общие, свои — лишь стек и регистры. Ядро Linux создаёт и то и другое одним вызовом clone() с разными флагами — разница в том, сколько ресурсов разделять. Баланс цены: fork дороже, зато изолирован (краш одного процесса не валит соседей); потоки дёшевы и быстро обмениваются данными, но общая память означает race conditions, а падение одного потока убивает весь процесс.",
      mechanics: "Потоки видны как отдельные записи: ps -eLf (колонка LWP — light weight process; PID у группы общий, LWP свой у каждого); ls /proc/<pid>/task — по подкаталогу на поток; htop переключается на показ потоков клавишей H. Лимиты: ulimit -u — максимум процессов пользователя; /proc/sys/kernel/threads-max — глобальный предел потоков; kernel.pid_max — максимум PID.",
      pitfalls: [
        "Думать, что поток можно завершить отдельно: kill адресует процесс — группу потоков целиком.",
        "Ждать бесплатности от общей памяти: счётчик, который увеличивают два потока без синхронизации, молча теряет обновления — это race condition.",
        "Множить потоки ради скорости: переключения контекста и конкуренция за общие структуры съедают выигрыш; у сервиса есть разумный размер пула.",
      ],
      whenNot: "Это рамка для чтения архитектуры, а не руководство к выбору модели в своём коде: там решают язык и runtime — Go прячет goroutines поверх потоков, Node живёт event loop в одном потоке. Эти глубины — за границей текущей карты.",
    },
    examples: [
      {
        title: "Увидеть потоки",
        code: "ps -eLf | head                    # колонки PID и LWP\nls /proc/$$/task                   # потоки текущего shell (обычно один)\ncat /proc/sys/kernel/threads-max   # глобальный лимит потоков",
        explanation: "Однопоточные процессы дают одну запись task; многопоточные (JVM, chrome) — десятки с общим PID.",
      },
      {
        title: "Два стиля в природе",
        code: "ps -eo pid,nlwp,cmd | grep -E 'nginx|java' | grep -v grep\n# nginx:  несколько процессов-воркеров, nlwp ≈ 1\n# java:   один процесс, nlwp = десятки",
        explanation: "nginx строит масштабирование на изолированных воркерах, JVM — на пуле потоков с общей памятью. Обе модели рабочие — цена разная.",
      },
    ],
    practicePrompt: "Найди самый многопоточный процесс в системе (ps -eo pid,nlwp,cmd | sort -k2 -rn | head), загляни в его /proc/<pid>/task и объясни, зачем каждому потоку отдельная запись.",
    checkPrompt: "Сервис держит 10 000 соединений: приведи по одному аргументу за «процесс на соединение» и за «пул потоков», и укажи, какой системный лимит упрётся первым в каждом случае.",
    tier: 1,
    complexity: 3,
    references: ["The Linux Programming Interface — Kerrisk, гл. 28–32", "man 2 clone"],
  },
];

export const curriculum: readonly PracticeArea[] = [
  {
    id: "linux",
    title: "Linux",
    description: "Практический курс командной строки: от терминала и файловой системы до скриптов, пакетов и модели процессов.",
    tier: 1,
    dependencies: [],
    topics: linuxTopics,
  },
];
