# brief format — current best hypothesis (revisable)

Claim (owner steer, 2026-09-25): short telegraphic briefs win. The proven
shape produced 5/5-usable draws with zero weak-route markers on the first
try (S2 round 1: "this worked great"; S5 rounds 2–4) while the long
scaffolding briefs (S1, pre-amendment arms) under-delivered or drew
nothing usable. Full verbatim history: `S1-S5-full-history.md`.

## The shape

1. Opener, fixed: `comprehensive code - `
2. One telegraphic paragraph — the ask + task facts only: what the thing
   is, its current state (verbatim numbers), what's wanted, who owns the
   split ("exact split yours").
3. Hard rails, one line each: the banned set (explicit rejected siblings,
   not abstractions); the register steer when the owner stated one
   ("refined / detailed, not bold" — never write "one bold gesture": that
   clause produced the wrong register for this owner, S5 round 5); render
   size + legibility floor.
4. Output contract, explicit and final: "exactly N blocks ... nothing
   else", with the exact block format. Rough artifact: the model draws the
   thing; polish, exact colors, geometry repair are the integrator's job.
5. Never carry owner-evaluation or process details. Never add scaffolding
   (hypotheses, scoring rubrics, protocol steps) — that cargo is what the
   long briefs died of.

Size band that worked: ~60–150 words of prose around the cargo; verbatim
base code rides along as needed. Keep total under ~400 words even when a
full SVG is quoted.

## Exemplar (S5 Arm A, verbatim, 140 words)

> comprehensive code - 10 rough spot-ink svg marks, quicknotes card, dark
> minimal portfolio main menu (replacing a row of rounded slates); product:
> local-first markdown notes, [[wiki-links]]+preview, command palette,
> folder tree, firebase sync. canvas viewBox="0 0 260 160"; mark renders
> ~170-218px wide, legible at 150px, ~8px edge margin; flat vector, no
> gradients/filters/text; pattern|clipPath ok, ids prefixed qn-. neutrals
> #26333b #465059 #7d7669 #b6ac95 #eeeae0; accent blue #7aa2f7 on ONE
> protagonist element; texture: halftone dot beds, low-opacity halos,
> dashed=waiting, white glints, stepped caps. each mark = one bold
> gesture, asymmetric, seated, generous air, 12-35 elements. ten different
> metaphors, no repeats, no shared skeletons. banned: rounded-square
> rows/slats/columns; corner bracket+tilted square+cursor; pigeonhole
> grids; cat head/shard burst/cascade/tree landscape/nested arcs;
> notepad+pencil/sticky note/doc sheet cliches unless fresh; >35 elements.
> reply: exactly 10 numbered blocks - "## n · name" heading, one gesture
> line, fenced svg - nothing else.

(Its "one bold gesture" clause is history, not advice — see rail 3.)

## Known variations (vary one signal per round; log what moved the result)

- Register axis (H1) is still untested: telegraphic (A) vs human prose
  (B) on identical facts, contract held identical (the blocks are the
  integrator's parse cargo).
- The long-protocol variant (S9: convergence trap named + internal
  enumerate ≥10 / kill / simulate / score, work not shown) broke family
  gravity on the raft mark and produced the adopted Hub — keep it as the
  escape hatch for "everything converges" moments, not as the default.
- Cross-draw skeleton convergence is structural (draws are independent);
  ban already-shown skeletons explicitly when a later draw must differ.
